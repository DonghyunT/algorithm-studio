# 🤖 [AGENTS.md] 알고리즘 랩 AI 어시스턴트 행동 지침 및 인수인계 프로토콜

> **문서 목적**: 본 프로젝트에 참여하는 모든 AI 에이전트(어시스턴트)가 준수해야 하는 **기술 스택 제약, 수업 환경 맞춤 규칙, 2022 개정 정보 보편 교육과정 아키텍처, 그리고 멀티 디바이스(노트북 ↔ 데스크탑 ↔ 교단 PC) 원드라이브 원스톱 협업 인수인계 프로토콜**을 정의합니다.  
> **적용 대상**: Claude, Gemini, Antigravity, Cursor, Copilot 등 모든 LLM 기반 코딩 어시스턴트  
> **최종 갱신일**: 2026-09-06

---

## 1. 프로젝트 개요 및 교육적 비전 (Project Identity)

* **프로젝트명**: 정보 알고리즘 스튜디오 (Information Algorithm Studio)
* **대상 교육과정**: 2022 개정 중학교 정보 교과 '알고리즘과 프로그래밍' 단원 (특정 출판사 종속 없음, 전국 공통 표준)
* **주요 대상**: 중학교 2학년 (학생 눈높이에 맞춘 쉽고 직관적인 용어 사용)
* **핵심 교육 목표**:
  학생들이 추상적인 개념을 직관적으로 체득할 수 있도록 **[📖 1. 핵심 개념 ➔ 📝 2. 쏙쏙 퀴즈 ➔ 🚀 3. 실전 실습실 ➔ 띵커보드 3초 제출]**로 이어지는 단절 없는 원스톱 학습 경험을 제공합니다.
* **코딩 연계성**:
  프로그래밍 실습(엔트리 Block Coding)으로 자연스럽게 전환될 수 있도록 순서도 기호와 캔버스를 **엔트리 표준 카테고리 색상(🟢 시작/끝, 🟠 자료, 🔵 판단, 🟣 동작)** 및 도트 모눈 캔버스와 1:1 대응시킵니다.

---

## 2. 절대 기술 제약 수칙 (Strict Technical Invariants)

⚠️ **아래 규칙은 프로젝트의 생명선이며, 어떤 상황에서도 위반해서는 안 됩니다.**

1. **Zero Dependency / 순수 웹 표준 (Pure Vanilla Stack)**:
   * **Node.js, npm, Webpack, Vite, React 등 일체의 번들러나 모던 프레임워크를 도입하지 않습니다.**
   * 별도의 빌드 단계(`npm run build` 등) 없이, 브라우저에서 `index.html`을 더블 클릭하거나 Netlify Drop에 폴더째 드래그하면 즉시 100% 정상 구동되어야 합니다.
   * 기술 스택: `HTML5` + `CSS3 (Tailwind CDN + css/flowchart.css)` + `Vanilla JavaScript (ES6+)`.
2. **외부 라이브러리 최소화 및 무결성 유지**:
   * 이미지 생성(띵커보드 제출 카드 등) 시 외부 캡처 라이브러리(`html2canvas` 등)의 CORS/보안 이슈를 피하기 위해 **순수 HTML5 Canvas API**로 직접 드로잉합니다.
3. **원본 백업 폴더 훼손 절대 금지**:
   * `백업용/` 폴더 내의 모든 원본 파일(`알고리즘 웹앱.html`, `순서도 만들기.html` 등)은 절대 수정하거나 삭제하지 않습니다.
4. **특정 출판사/단원 번호 하드코딩 금지**:
   * 다른 교과서를 사용하는 학교에서도 범용으로 활용할 수 있도록 출판사명(길벗 등)이나 고정 단원 번호(1단원, 2단원, 3단원)를 사용하지 않고, **[문제 추상화], [알고리즘 설계], [순서도 연구소]**의 핵심 교육과정 주제명으로 표기합니다.
5. **중2 학생 친화적 용어 사용**:
   * '디버깅'과 같은 낯선 외래어 대신 **'오류 찾기와 수정', '알고리즘 설계', '명령어 레시피'** 등 직관적인 우리말 표현을 우선합니다.
6. **API 키 및 통신 예외 처리**:
   * Upstage Solar AI 연동 키는 `js/data/config.js`에서 중앙 관리합니다.
   * 불필요한 하드코딩 오프라인 룰 엔진은 지양하며, 통신 에러 시 학생 친화적인 안내 메시지를 표시합니다.

---

## 3. 학교 컴퓨터실 환경 맞춤 데이터 규칙

1. **데이터 저장소는 오직 `sessionStorage`만 사용**:
   * 학교 컴퓨터실은 여러 학급(1반, 2반, 3반...)이 같은 컴퓨터를 순차적으로 공유하며, 컴퓨터 재부팅 시 롤백(순간복구) 솔루션이 가동되는 환경입니다.
   * `localStorage`는 재부팅 전까지 하드디스크에 남아 다음 시간 다른 반 학생에게 이전 작업물이 노출되는 혼란을 야기하므로 **절대 사용을 금지**합니다.
   * **수업 중 실수로 `F5(새로고침)`를 눌렀을 때 작업 내용이 날아가는 것만 방지**하도록 `sessionStorage`를 사용하며, 브라우저 탭/창을 닫으면 깨끗하게 자동 초기화되도록 구성합니다.

---

## 4. 사이트 구조 및 학습 동선 아키텍처

모든 코스는 학생이 스스로 개념을 다지고 실습으로 전진할 수 있는 **3단계 학습 파이프라인**을 따릅니다:

```text
[글로벌 3대 코스 체계]
├── 💡 1코스. 문제 추상화 (생각 다이어트 연구소)
│    ├── 📖 1. 핵심 개념 (상태 분석 4요소, 핵심 추출, 문제 분해)
│    ├── 📝 2. 쏙쏙 퀴즈 (중2 눈높이 맞춤 3문항)
│    └── 🚀 3. 실전 실습실 (30초 맛보기 튜토리얼 ➔ 실전 4단계 워크북)
│
├── 🤖 2코스. 알고리즘 설계 (샌드위치 로봇 연구소)
│    ├── 📖 1. 핵심 개념 (알고리즘 5대 조건, 순차·선택·반복 구조)
│    ├── 📝 2. 쏙쏙 퀴즈 (알고리즘 조건 및 구조 3문항)
│    └── 🚀 3. 실전 실습실 (샌드위치 명령어 레시피 & 5대 조건 오류 수정)
│
└── 📐 3코스. 순서도 연구소 (4대 표준 기호 & 엔트리 연계 5단계 로드맵)
     ├── 📖 1. 핵심 개념 (4대 표준 기호와 엔트리 블록 매핑)
     ├── 📝 2. 쏙쏙 퀴즈 (단말·입출력·처리·판단 기호 3문항)
     └── 🚀 3. 실전 실습실 (5단계 원스톱 로드맵)
          ├── Step 1. 🍜 순차 퍼즐 (라면 끓이기 레시피 조립)
          ├── Step 2. 🎡 선택 퍼즐 (놀이기구 탑승 판정기 조건 분기)
          ├── Step 3. 🔒 반복 퍼즐 (비밀번호 검증 루프백)
          ├── Step 4. 🛠️ 예시 공방 (지각 방지 등교 알고리즘 자연어 ➔ 엔트리 순서도 자동 매핑)
          └── Step 5. 🚀 나만의 백지 공방 (엔트리 4색 블록 자유 캔버스, 유도등 연결, 띵커보드 3초 제출)
```

---

## 5. 🔄 멀티 디바이스 (OneDrive 원스톱) 협업 인수인계 프로토콜

선생님이 **노트북, 연구실 데스크탑, 교단 컴퓨터** 등 어떤 장소에서든 이 OneDrive 폴더를 열고 즉시 개발 워크플로우를 이어갈 수 있도록 다음 규칙을 철저히 준수합니다.

### 🌐 환경 독립성 보장 (Any Computer, Zero-Setup)
1. **별도 설치 불필요 (No npm, No build)**:
   - 다른 컴퓨터에서 열었을 때 `npm install`이나 복잡한 빌드 명령을 실행할 필요가 전혀 없습니다.
   - 그냥 VS Code / Antigravity / 에디터에서 해당 폴더를 열고 `index.html`을 라이브 서버로 열거나 브라우저에서 더블 클릭하면 즉시 모든 기능이 실행됩니다.
2. **OneDrive 동기화 확인**:
   - 다른 PC로 이동했을 때는 Windows 작업 표시줄의 OneDrive 구름 아이콘에 "동기화 완료(체크 표시)"가 떠 있는지 확인하여 최신 파일 상태를 유지합니다.

### 🤖 AI 어시스턴트의 행동 규칙 (다른 컴퓨터에서 첫 프롬프트를 받았을 때)
1. **1순위 문서 정독**:
   선생님이 *"작업 이어가자"*고 하면, AI는 대화를 시작하기 전에 반드시 프로젝트 루트의 **`AGENTS.md`**와 **`PRD.md`**를 최우선으로 정독한다.
2. **동기화 상태 확인**:
   파일이 최신 상태로 로컬에 로드되었는지 확인하고, 불필요하게 이전 코드를 덮어쓰거나 롤백하지 않는다.
3. **첫 응답 브리핑 포맷**:
   > *"선생님, AGENTS.md와 PRD.md를 바탕으로 인수인계를 완료했습니다! 현재 [문제 추상화], [알고리즘 설계], [순서도 연구소]의 3단계 학습 파이프라인 및 엔트리 스타일 캔버스가 완비되어 있습니다. 오늘 진행할 작업을 말씀해 주시면 바로 착수하겠습니다!"*

### 💡 선생님을 위한 1초 프롬프트 팁
* 다른 컴퓨터에서 새 대화창을 열었을 때, 복잡한 설명 없이 아래 한 문장만 입력하시면 됩니다:
  > **`"AGENTS.md랑 PRD.md 읽고 작업 이어가자"`**

---

## 6. AI 어시스턴트 협업 골든 룰 (Golden Rules)

1. **선생님 승인 후 코딩 원칙 (No Coding Without Approval)**:
   * 설계나 구조 변경이 필요한 작업은 먼저 구현 계획을 설명하고, 선생님의 명시적인 지시(**"빌드해줘"**, **"진행해줘"**)가 있을 때 코드를 수정합니다.
2. **선생님의 생각이 1순위 (PRD 자동 동기화)**:
   * 선생님이 피드백이나 방향 수정을 주시면, 코드를 수정함과 동시에 **`PRD.md`도 AI가 스스로 함께 업데이트**합니다. 선생님께 PRD 수정을 요구하지 않습니다.
3. **실화면 자가 검증 (Self-Verification)**:
   * 코드를 수정한 후에는 가상 브라우저(Headless Chrome 스크린샷 등)로 실화면 렌더링 상태와 콘솔 에러를 반드시 검증한 후 완료 보고를 올립니다.
