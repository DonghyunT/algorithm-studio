# 📋 [PRD] 정보 알고리즘 스튜디오 제품 요구사항 명세서
> **Product Requirements Document (PRD) — Single Source of Truth (SSOT)**  
> **프로젝트명**: 정보 알고리즘 스튜디오 (Information Algorithm Studio)  
> **대상 교육과정**: 2022 개정 중학교 정보 '알고리즘과 프로그래밍' 단원 (전국 표준 공통)  
> **문서 위상**: 프로젝트 기능, 화면, 알고리즘 엔진, UI 수치 및 구현 현황의 **단일 진실 공급원 (SSOT)**  
> **최종 갱신일**: 2026-09-07 (AGENTS.md 헌장 개편 및 INTENT.md 연동 완비)  
> **핵심 원칙**: Zero-Dependency (Vanilla Stack), 단원당 2차시 완결형 배움의 리듬(INTENT.md 연계), 3단계 학습 파이프라인 [개념 ➔ 퀴즈 ➔ 실습], 4대 표준 기호 & 제어 구조, 멀티 디바이스 원드라이브 원스톱 워크플로우

---

## 1. 프로젝트 비전 및 배경

### 1.1 배경 및 문제의식
* 기존 알고리즘 실습은 단순 빈칸 채우기나 정형화된 예제에 머물러 있어, 중학교 2학년 학생들이 스스로 일상의 문제를 발견하고 알고리즘으로 구조화하는 **컴퓨팅 사고력(추상화 & 알고리즘 설계)** 신장에 한계가 있었음.
* 특정 출판사(길벗 등)나 고정된 단원 번호(1/2/3단원)에 의존할 경우, 다른 교과서를 채택한 학교나 다른 차시 운영 학교에서 활용하기 어려운 제약이 존재함.
* '디버깅'과 같은 낯선 외래어는 학생들에게 심리적 장벽을 주므로, 중2 눈높이에 맞춘 **'오류 찾기와 수정'**, **'알고리즘 설계'**, **'명령어 레시피'** 등의 자연스러운 용어 정립이 필요함.
* 프로그래밍 단원으로 이어질 때 학생들의 인지적 연속성을 위해, **자연어로 작성한 알고리즘을 4대 교과서 표준 순서도 기호(🟣 단말, 🟢 입출력, 🔵 처리, 🟠 판단)**로 직접 구성하고 시각화하는 도구가 필요함.

### 1.2 핵심 해결 방향
1. **보편적 2022 개정 정보 교육과정 3대 코스 체계**:
   - `💡 1코스. 문제 추상화`: 문제 이해 및 상태 분석(초기·현재·목표 상태, 조건) ➔ 핵심 요소 추출(생각 다이어트 워크북).
   - `🤖 2코스. 알고리즘 설계`: 로봇 요리 레시피를 통한 알고리즘 5대 조건(입력·출력·명확성·유한성·수행가능성) 검증 및 오류 수정.
   - `📐 3코스. 순서도 연구소`: 4대 표준 기호(단말·입출력·처리·판단)와 3대 제어구조(순차·선택·반복) ➔ 교과서 표준 순서도 5단계 원스톱 로드맵.
2. **단원별 2차시 완결형 배움의 리듬 (INTENT.md 기반)**:
   - **[1차시: 탐색과 직관]**: `[📖 1. 핵심 개념]`(개념 감각화) ➔ `[📝 2. 쏙쏙 퀴즈]`(오개념 해소) ➔ `[🚀 3. 맛보기/기초 퍼즐]`(효능감 형성).
   - **[2차시: 몰입과 표현]**: `[🛠️ 실전 심화 공방]`(자연어 기획 ➔ 백지 조립) ➔ `[🧪 가상 실행 & AI 거울 검사]`(논리 성찰) ➔ `[📮 띵커보드 3초 나눔]`(학급 공유).
3. **교과서 표준 직각 꺾임(Manhattan Orthogonal Routing) 순서도 엔진**:
   - 베지어 곡선의 꼬임 문제를 해결하고, 실제 교과서 자판기/반복문 예시 수준의 **직각 꺾임 Manhattan Orthogonal 라우팅** 구현.
   - 좌/우 외곽 우회 루프백 선(`M x1 y1 H loopX V (entryY) H x2 V y2`)을 통해 화살표 촉이 타겟 기호 상단에 수직으로 정확히 안착.
   - 분기선별 교과서형 `[예]`, `[아니오]`, `[분기]` 텍스트 배지 라벨 렌더링 및 굵은 클릭 영역 제공.
4. **추상화 기반 Solar AI 처방전 모달 (3코스 Step 5)**:
   - 1코스 상태 분석(초기/현재/목표 상태)과 퀵 추천 태그(음료 자판기, 등교 버스, 분리수거, 25분 집중 알람)를 선택하면, Upstage Solar AI가 맞춤 순서도 처방전(목표, 조건, 변수, 예/아니오 액션)을 실시간으로 설계해 줌.
5. **기초 퍼즐 슬롯 복구 및 안전 드롭**:
   - Step 1~3 기초 퍼즐에서 슬롯 위에 다른 블록을 덮어쓸 때 기존 블록이 영구 증발하던 버그를 해결하여, 블록 팔레트로 안전 복귀.
6. **띵커보드(ThinkerBoard) 3초 원클릭 제출**:
   - 전 과정을 담은 [알고리즘 포트폴리오 카드]를 순수 Canvas API로 렌더링하여 클립보드 복사(`Ctrl + V`) 또는 비상용 이미지 다운로드 지원.
7. **멀티 디바이스 (OneDrive) 원스톱 연속성**:
   - 어느 컴퓨터(노트북, 연구실 PC, 교단 PC)에서든 별도 npm 빌드 없이 바로 열어 작업할 수 있는 무결점 인수인계 환경 보장.

---

## 2. 학습 동선 및 화면 구성

```text
[글로벌 1단 헤더] 🏫 정보 알고리즘 스튜디오 | [🗺️ 로드맵] [💡 문제 추상화] [🤖 알고리즘 설계] [📐 순서도 연구소] [🔒 교사용] | [☰ 전체 메뉴 ▾]
   │
   ├─ [🎮 마우스 호버/클릭 시] 넥슨 스타일 심플 텍스트 목차 드롭다운 메뉴 (Mega Menu)
   │     ├─ [열 1] 문제 추상화: 핵심 개념, 쏙쏙 퀴즈, 30초 맛보기, 생각 다이어트 실전 워크북
   │     ├─ [열 2] 알고리즘 설계: 핵심 개념, 쏙쏙 퀴즈, 샌드위치 로봇 조리 레시피
   │     ├─ [열 3] 순서도 연구소: 핵심 개념, 쏙쏙 퀴즈, Step 1.순차, Step 2.선택, Step 3.반복, Step 4.예시, Step 5.백지
   │     └─ [열 4] 안내 및 바로가기: 전체 학습 로드맵, 선생님 클래스룸
   │
   ▼
[본문 초슬림 일체형 서브 바 (38~40px / 세로 공간 150px+ 절약)]
   좌측: 브레드크럼 (예: 📐 순서도 연구소 / 5단계 실습) + [📖 개념] [📝 퀴즈] [🚀 실습]
   우측: 실습 세부 모드 알약 (1코스: 맛보기 vs 워크북 / 3코스: 1.순차 ~ 5.나만의백지)
   ↓
[1코스. 문제 추상화 실습실]
   ├── [1단계: 30초 맛보기] 폰트 확대(text-lg/xl), 현재·목표·조건 팁 박스, 1:1 맞춤 연동 시나리오
   └── [2단계: 실전 워크북] 4대 관심 카테고리, 다중 조건 등록, 생각 다이어트 노이즈 제거, 처방전 발행
   ↓
[2코스. 알고리즘 설계 실습실]
   ├── 샌드위치 로봇 조리 레시피 (명령어 오류 찾기와 수정)
   └── 5대 조건(입력·출력·명확성·유한성·수행가능성) 누적 큐 실행 및 가상 시뮬레이션
   ↓
[3코스. 순서도 연구소 실습실 (교과서 표준 직각 라우팅 & 와이드 스튜디오)]
   ├── 🍜 Step 1. 순차 퍼즐 (라면 끓이기 조립 / 슬롯 교체 복구 지원)
   ├── 🎡 Step 2. 선택 퍼즐 (놀이기구 탑승 판정기 조건 분기)
   ├── 🔒 Step 3. 반복 퍼즐 (비밀번호 검증 루프백 / 기본값 '1234' 일반 텍스트 노출, [정답(7777)] 퀵 버튼)
   ├── 🛠️ Step 4. 예시 공방 (등교 알고리즘 ➔ 1.35배 확대 순서도 뷰, viewBox="0 0 700 680")
   └── 🚀 Step 5. 나만의 백지 공방 (초와이드 캔버스, 직각 꺾임선 라우팅, Solar AI 처방전, 띵커보드 제출)
```

---

## 3. 세부 기능 구현 명세

### 3.0 🎮 넥슨 스타일 심플 텍스트 목차 드롭다운 메뉴 (`index.html`, `js/core/navigation.js`)
* 화려한 그라데이션 카드, 칩, 진입 버튼 등 복잡한 시각적 군더더기를 전면 배제하고, 넥슨 메이플스토리 포털과 동일한 **간결한 4열 수직 텍스트 목차(Table of Contents)**로 전면 개편.
* 마우스 호버(240ms 지연) 및 `[전체 메뉴]` 버튼 클릭 시 상단 헤더 바로 아래 4열(문제 추상화, 알고리즘 설계, 순서도 연구소, 안내 및 바로가기)의 깔끔한 텍스트 리스트가 펼쳐지며, 클릭 시 해당 위치로 즉시 점프 후 닫힘.

### 3.1 📖 단원별 독립 개념관 (`js/core/concept.js`)
* 특정 출판사나 특정 도구 종속 없이, 2022 개정 교육과정 공통 표준 기호(⬭ 단말, ▱ 입출력, ▭ 처리, ◇ 판단)와 3대 제어 구조(순차, 선택, 반복)를 라이트 플로우 애니메이션으로 학습.

### 3.2 📝 단원별 쏙쏙 퀴즈 2×2 대형 무스크롤 대시보드 (`js/labs/lab-quiz.js`)
* `max-w-6xl` 와이드 컨테이너로 확장하고 `(2×2 콤팩트 풀이)` 부가 문구를 제거하여 깔끔하고 큼직한 풀이 환경 제공.
* 문제 카드 내부 여백(`p-6`)과 폰트 크기를 키워 시각적 답답함 해소.

### 3.3 📐 교과서 표준 순서도 엔진 & 직각 꺾임 Manhattan Routing (`js/labs/lab-flowchart.js`)
1. **맨해튼 직각 꺾임 라우팅 (Manhattan Orthogonal Routing)**:
   - 베지어 곡선을 배제하고 교과서 스타일 수직/수평 직각 꺾임선으로 렌더링.
   - 역방향 루프백(위쪽 블록으로 되돌아가는 연결선): 좌측 또는 우측 외곽으로 크게 우회한 뒤 타겟 기호 상단에서 수직 진입(`H loopX V entryY H x2 V y2`).
   - 종료 단말 기호를 제외한 모든 블록(처리, 입출력, 판단)에서 상위 블록으로 되돌아가는 연결 허용.
2. **단일 가상 스테이지 줌 아키텍처 (`#free-flowchart-stage`)**:
   - 캔버스 줌 배율(60%~140%) 조정 시 블록 DOM 좌표계와 SVG 좌표계의 불일치로 인한 화살표 분리/어긋남 현상을 원천 차단.
   - 단일 가상 작업대 컨테이너에만 `transform = scale(zoom)`을 적용하고, 마우스 드래그 및 포트 연결선 계산 시 줌 배율 역산(`clientX / zoom`)을 일원화하여 어떤 배율에서도 블록과 화살표가 1px의 오차 없이 완벽하게 자석 결합 유지.
3. **다단계 자연어 처방전 엔진 (샌드위치 5단계 레시피 등 획일성 타파)**:
   - 획일적인 1분기(`입력-판단-처리`) 템플릿을 탈피하고, 다단계 자연어 레시피 배열(`steps: [{type: 'seq'|'sel'|'loop', ...}]`) 지원.
   - 샌드위치 조리(식빵 굽기 ➔ 잼 바르기 ➔ 속재료 올리기 ➔ 치즈 데우기 ➔ 빵 덮기) 등 실제 교과서 및 일상 문제의 다단계 제어구조 모델 구축.
   - 모달 내 동적 단계 목록(`pres-steps-list`), 단계 추가(`+ 순차`, `+ 선택`, `+ 반복`), 삭제, 실시간 편집 지원.
   - Upstage Solar AI 프롬프트 역시 3~5단계 입체적 다단계 레시피를 생성하도록 고도화.
4. **학생 주도성 보장 (처방전 ➔ 자연어 기획서 전용 반영)**:
   - 처방전 적용 시 캔버스에 6개 순서도 기호를 미리 다 그려주던 자동완성 방식을 폐지.
   - 좌측 [자연어 기획서]에만 완벽한 다단계 레시피 카드를 채워주고, 우측 캔버스는 기본 시작/종료 단말 기호만 배치된 백지 상태로 두어 학생이 직접 기호 보관함에서 블록을 꺼내 조립하도록 유도.
5. **분기 라벨 배지 렌더링**:
   - [예](초록색), [아니오](주황색), [분기](주황색) 배지를 선 출발점에 시각적으로 명확하게 표시.
   - 16px 두께의 투명 클릭 영역(Hit path)을 함께 생성하여 선 삭제 클릭 조작감 대폭 향상.
6. **Step 4 예시 공방 확대**:
   - SVG 캔버스 `viewBox="0 0 700 680"`, 중심축 350px, 블록 폭 230px, 테두리 3.5px로 1.35배 대형화.
8. **정량적 알고리즘 대조 엔진 & Solar AI 정밀 검사 (`js/labs/lab-flowchart.js`)**:
   - 무조건적인 칭찬 피드백을 방지하고, JS 단에서 자연어 단계 수(순차/선택/반복)와 캔버스 기호(단말/입출력/처리/판단)의 개수, 순서, 루프백 결손 여부를 1차 정량 계산(`analyzeAlgorithmConsistency()`).
   - 계산된 결손 팩트를 AI 프롬프트에 주입하여, 블록이 누락되었을 경우 학생이 어떤 기호를 추가해야 하는지 중2 눈높이에서 구체적으로 짚어주는 맞춤형 보완 가이드 제공.
9. **인터랙티브 가상 실행 검증 엔진 (`js/labs/lab-flowchart.js`)**:
   - `[▶ 실행 검증]` 클릭 시 자료(입출력) 블록 자동 스캔 ➔ 가상 입력값 설정 모달(`flowchart-sim-input-modal`) 팝업 후 일괄 수집.
   - 시작 단말부터 순서대로 블록(`.flowchart-block-simulating`) 및 연결 화살표(`.flowchart-path-simulating`)에 네온 펄스 하이라이트 애니메이션 적용.
   - 판단(마름모) 블록 도달 시 `[✓ 참 (예)]` / `[✗ 거짓 (아니오)]` 선택 오버레이 팝업을 띄워 학생이 직접 분기 경로를 결정하며 컴퓨터의 조건 판단 알고리즘 체득.
10. **엔트리식 3분할 스튜디오 최적화 및 캔버스 시야 100% 확보**:
   - 문제 재료 바구니를 전면 제거하고 '처방전 채우기' 버튼을 캔버스 툴바(정렬 버튼 좌측)로 재배치.
   - 대형 휴지통(`bottom: 14px; right: 14px;`)과 AI 튜터 챗봇 FAB(`bottom: 14px; right: 94px;`)을 우측 하단에 나란히 배치하여 캔버스 작업 공간 간섭 완전 해소.

### 3.4 📐 엔트리형 3분할 무간격(Zero-Gap) 일체형 스튜디오 (`index.html`, `css/flowchart.css`)
* **화면 공간 100% 활용 무간격 레이아웃**:
  - 기존의 개별 패널 분리 카드 방식(`gap: 12px`, 둥근 카드 여백)을 전면 제거.
  - 엔트리(PlayEntry)와 동일하게 외곽 단일 프레임(`height: calc(100vh - 120px)`) 안에 좌측(자연어 기획서 340px) ➔ 중앙(기호 보관함 120px) ➔ 우측(순서도 캔버스 1fr) 3개 패널이 `gap: 0` 및 1px 경계선(`border-r border-slate-200`)으로 빈틈없이 밀착.
  - 우측 캔버스는 불필요한 안쪽 패딩 없이 도트 모눈 그리드가 100% 꽉 채우며, 우측 하단에 엔트리 스타일 줌 컨트롤(-, 100%, +) 및 빨간색 휴지통 드롭존 배치.

### 3.5 🎨 draw.io 스타일 순서도 인터랙션 & 텍스트 잘림 없는 자연어 기획서 (`js/labs/lab-flowchart.js`)
1. **draw.io 스타일 4방향 자석 포트 & 실시간 러버밴드 가이드**:
   - 4대 기호(단말, 입출력, 처리, 판단) 모두의 상/하/좌/우 4방향 테두리에 14px 자석 포트 탑재 (블록 마우스 호버 시 은은하게 점등).
   - 포트 드래그 시 마우스 커서를 따라가는 실시간 직각 가이드 점선 화살표(Rubber-band preview) 렌더링.
   - 타겟 포트 반경 25px 이내 접근 시 녹색 링 자석 스냅(Magnet Snap) 및 마우스 업 시 즉시 직각 결합.
   - 블록 클릭 시 파란색 선택 테두리(`selected-block`) 활성화 및 키보드 `Delete`/`Backspace` 키로 블록 및 연결선 즉시 삭제 지원.
2. **자연어 기획서 글자 잘림 원천 해결 (Auto-expanding Textarea)**:
   - 1줄 고정 `<input>`을 자동 높이 조절 `<textarea rows="1">` 및 `autoResizeNlTextarea()`로 전면 교체.
   - 선택(sel) 카드는 상단 조건(가로 100%) + 하단 [예]/[아니오] 액션(각각 가로 100%), 반복(loop) 카드는 조건(100%) + 반복 행동(100%)으로 전면 리디자인하여 아무리 긴 글도 줄바꿈되어 100% 완벽하게 노출.

### 3.6 🚀 수업 최적화 및 순서도 스튜디오 8대 고도화 (`js/core/`, `js/labs/`, `css/`, `index.html`)
1. **교사용 클래스룸 0000 힌트 제거 완전 잠금 (`js/core/classroom.js`)**:
   - `promptTeacherPin()`에서 `(초기 기본값: 0000)` 및 alert의 `(기본 비밀번호: 0000)` 문구를 전면 삭제하여 수업 중 학생들의 무단 진입 원천 차단.
2. **상단 메뉴 1.5초(1,500ms) 호버 딜레이 (`js/core/navigation.js`, `index.html`)**:
   - 마우스가 상단 메뉴 영역을 스쳐 지나갈 때 메뉴가 화면을 가리는 현상을 방지하기 위해 1,500ms 지연 후 오픈되도록 개선.
3. **지능형 최단 경로 Manhattan 라우팅 (`js/labs/lab-flowchart.js`)**:
   - 기존의 무조건 우측 외곽(`Math.max(x1, x2) + 75`) 선회 방식을 개선하여, 목표 블록이 좌측에 있을 때(`x2 < x1 - 30`) 좌측 최단 우회선(`Math.min(x1 - 40, x2 - 55)`)으로 자연스럽게 루프백 연결.
   - 출발 포트 방향(상/하/좌/우) 100% 존중.
4. **캔버스 줌 배율 제거 및 100% 표준 배율 고정 (`index.html`, `js/labs/lab-flowchart.js`)**:
   - 줌 컨트롤 UI를 전면 제거하고 1.0 절대 좌표계로 고정하여 줌 스케일링 관련 잔여 버그 원천 박멸.
5. **대형 휴지통 드롭존 구축 (`index.html`, `css/flowchart.css`, `js/labs/lab-flowchart.js`)**:
   - 캔버스 우측 하단에 `68x68px` 크기의 대형 휴지통 구역(`entry-large-trash-dropzone`)을 배치하여 블록 D&D 드롭 삭제 및 선택 클릭 삭제 지원.
6. **기호 보관함 엔트리 실물 블록 디자인 & D&D & 더블클릭 (`index.html`, `css/flowchart.css`)**:
   - 4대 표준 기호(단말 캡슐, 자료 평행사변형, 판단 마름모, 동작 직사각형)의 실물 블록 디자인 적용.
   - 드래그 앤 드롭으로 캔버스 마우스 좌표에 즉시 생성 지원.
   - 단일 클릭 오작동을 방지하고 더블클릭(`ondblclick`) 시 캔버스에 추가되도록 개선.
7. **Solar AI 검사 결과 하단 잘림 해결 (`js/labs/lab-flowchart.js`)**:
   - 피드백 카드에 `max-h-[170px] overflow-y-auto custom-scroll`을 적용하여 내부 스크롤 보장.
   - AI 프롬프트에 중2 눈높이 3문장 이내(200자 내외) 요약 작성 지침 부여.
8. **처리 블록 우측/좌측 포트 루프백 검사 오류 해결 (`js/labs/lab-flowchart.js`)**:
   - AI 전달용 순서도 연결 데이터를 자연어 친화적 리포트로 변환.
   - 처리 블록의 우측/좌측 포트에서 출발한 루프백 연결선도 정당한 반복 구조로 인정하고 칭찬하도록 프롬프트 지침 명시.

### 3.7 💡 AI 정량 검사·인터랙티브 가상 실행·띵커보드 토폴로지 완비 (`js/`, `css/`, `index.html`)
1. **JS 룰 엔진 기반 정량적 사전 대조 (`analyzeAlgorithmConsistency()`)**:
   - 캔버스 블록 수가 자연어 기획 카드에 비해 결손되거나 부족한 경우, AI가 무조건 칭찬하지 않고 블록 종류/개수/제어 구조(선택/반복) 누락을 구체적으로 지적하는 2단계 검증(정량 룰 엔진 + Solar AI 질적 피드백) 파이프라인 구축.
2. **인터랙티브 가상 실행 검증 파이프라인 (`runInteractiveSimulation()`)**:
   - 입출력(자료) 블록이 포함된 경우, 실행 전 **일괄 사전 입력 모달(`flowchart-sim-input-modal`)**을 띄워 학생이 가상 데이터를 한 번에 입력.
   - 시작 블록부터 연결선을 따라 600ms 간격으로 블록 펄스 하이라이트 및 연결선 유도등 애니메이션 진행.
   - 판단(decision) 블록 도달 시 가로형 와이드 알약 팝업(`.sim-choice-overlay`, `display: flex; flex-direction: row; white-space: nowrap`)을 띄워 학생이 직접 [✓ 참(예)] / [✗ 거짓(아니오)] 분기를 클릭하여 테스트.
3. **상주 챗봇 버튼 위치 최적화 (방안 A 적용)**:
   - 우측 하단 대형 휴지통(`right: 14px, bottom: 14px`)과 나란히 좌측에 밀착(`bottom-[14px] right-[94px]`) 배치하여 작업 공간 침범 및 가림 현상 완벽 해소.
4. **띵커보드 문제 분석 및 목표 상태 10초 실시간 점검/수정**:
   - 띵커보드 제출 팝업 내에 `thinker-cur-status`(현재 상태) 및 `thinker-goal-status`(목표 상태) 입력 필드 제공.
   - 추상화 처방전이 없더라도 자연어 카드의 1단계와 마지막 단계에서 스마트 기본값을 추론하여 자동 채움(더 이상 등교 기본값으로 고정되지 않음). 학생이 직접 즉시 편집 가능.
5. **순서도 다이어그램 토폴로지 정렬 (`getFlowchartOrderedBlocks()`)**:
   - 블록 생성 순서(배열 인덱스)에 의존하던 기존 방식에서 탈피하여, BFS 화살표 연결선 탐색 및 미연결 블록 Y좌표 정렬을 통해 `[시작]` ➔ `[중간 블록들]` ➔ `[종료]` 순서를 엄격히 보장.
   - 띵커보드 카드 미리보기 및 Canvas 다운로드 이미지에서 "시작 바로 다음 종료가 나오는 현상" 원천 박멸.

### 3.8 🌟 순서도 완주 검증 · AI 검사 전용 모달 · 조건부 합격 도장 · 피커부 챗봇
1. **시작~종료 완주 검증 강화 (`runInteractiveSimulation()`, `playFreeFlowchartSimulation()`)**:
   - 연결선이 없거나 시작 블록에서 출발하지 않는 경우 시뮬레이션을 즉시 차단하고 친절한 안내 제공 (`⚠️ [연결선 없음]`, `⚠️ [시작 기호 미연결]`).
   - 가상 실행 시 중간 유효 명령 블록(`intermediateSteps`)의 거침 여부와 최종 도달 블록이 `종료` 단말인지 엄격 판정.
   - '시작 ➔ 곧바로 종료' 연결 시 명령 블록 추가 유도, 중간 경로 끊김 시 멈춘 기호 이름을 짚어주는 경고 제공. 온전히 완주했을 때만 `window.isFlowchartSimValidated = true` 및 성공 축하 알림.
2. **Solar AI 검사 결과 전용 모달화 (방안 A, `#flowchart-ai-audit-modal`)**:
   - 340px 좌측 기획서 패널 하단에 카드가 생겨 편집 공간을 좁히던 구조를 전면 개편.
   - 좌측 자연어 카드 목록의 상하 100% full-height를 영구 보존하고, 검사 버튼 클릭 시 중앙에 널찍한 **[전용 리포트 모달]** 팝업을 띄워 📊 정량 대조표, 💡 중2 맞춤 처방, 💮 합격/보완 배너, 액션 버튼을 쾌적하게 제공.
3. **띵커보드 조건부 공식 합격 인증 도장 (게이미피케이션)**:
   - AI 검사를 통과하지 않았는데 무조건 합격 도장을 주던 맹점을 제거.
   - 실제 Solar AI 정식 검사를 통과한 학생(`window.isFlowchartAiPassed === true`)에게만 `💮 공식 합격 인증` 도장을 부여.
   - 미통과/미수행 학생에게는 `⏳ AI 검사 전 (실습 중)` 배지와 함께 모달 내 `[ 🤖 지금 AI에게 검사받기 ]` 원클릭 버튼을 제공하여 자기주도적 오류 수정 동기 부여.
   - HTML5 Canvas 이미지 및 미리보기에 조건부 스탬프(통과 시 에메랄드 공식 인장, 미통과 시 앰버 실습 중 알림) 실시간 동기화.
   - HTML5 Canvas 이미지 및 미리보기에 조건부 스탬프(통과 시 에메랄드 공식 인장, 미통과 시 앰버 실습 중 알림) 실시간 동기화.
4. **우측 엣지 챗봇 피커부(Peek-a-boo) 반숨김 인터랙션 (`.chatbot-peek-tab`, `#tutor-fab-btn`)**:
   - 항상 캔버스 위에 플로팅되어 시야를 가리던 챗봇 버튼을 화면 우측 벽면에 50% 숨겨둔 깔끔한 알약 탭 형태(`transform: translateX(52%)`)로 리디자인.
   - 마우스를 올리거나 포커스 시 부드러운 스프링 효과와 함께 **"쇽!"** 화면 안쪽으로 튀어나오며(`transform: translateX(0)`), 대화창 활성화 시에도 안정적으로 펼쳐짐.
   - 캔버스 작업 공간 및 하단 대형 휴지통과 1픽셀도 겹치지 않는 무간섭 시야 확보.

### 3.9 🎨 엔트리 스타일 무한 캔버스 드래그 팬(Pan) · 단말 스마트 가드 · 처방전 원자화
1. **엔트리 스타일 캔버스 무한 드래그 팬(Pan) & 화면 중앙 복귀 (`canvasPanX`, `canvasPanY`, `resetCanvasPan`)**:
   - 좁은 캔버스 영역의 한계를 극복하기 위해, 엔트리(PlayEntry)와 동일하게 캔버스의 빈 영역을 마우스로 클릭하고 드래그하면 캔버스 전체가 상하좌우 부드럽게 무한 이동(Pan).
   - 도트 모눈 그리드(20px radial-gradient background-position)와 내부 Stage 레이어가 1:1 완벽 동기화.
   - 캔버스 상단 툴바에 `[ 🎯 화면 중앙 ]` 원클릭 복귀 버튼을 제공하여 학생들이 이동 중 길을 잃지 않도록 지원.
   - 드래그 중 마우스 커서 grab/grabbing 전환 및 텍스트 선택 방지.
2. **단말(시작/종료) 기호 자유 삭제 허용 & 똑똑한 추가 제어 (스마트 가드)**:
   - 기존에 `blk-start`, `blk-end`의 임의 삭제를 차단하여 발생하던 캔버스 교착 및 시뮬레이션 오류 원천 해결.
   - 모든 블록(단말 포함)의 삭제(Delete 키, Backspace 키, X버튼, 휴지통 드래그)를 100% 자유롭게 허용.
   - 단말 기호 팔레트 추가 시 스마트 가드: 이미 `시작`과 `종료`가 모두 캔버스에 존재할 경우 중복 생성을 차단하고 친절한 유도 알림(`💡 순서도에는 '시작'과 '종료' 기호가 각각 하나씩만 있으면 충분해요!`) 표시. 만약 둘 중 하나가 누락된 상태라면 자동으로 누락된 단말 기호(시작 또는 종료)를 생성하여 학생의 혼란 방지.
3. **AI 추상화 처방전 원자적 다이어트 ("One Action, One Box" 원칙)**:
   - 자연어 카드에 문장형 설명이 장황하게 나열되던 문제를 해결하기 위해, 1단계당 15자 이내의 단일 행위(원자적 행동)로 대폭 축약.
   - Solar AI 시스템 프롬프트 및 기본 프리셋(샌드위치, 횡단보도, 컵라면 등)을 10~15자 내외의 명료한 행동 레시피("식빵 2장 꺼내기", "딸기잼 바르기" 등)로 전면 정제.
   - 처방전 모달의 현재/목표 상태 및 단계별 입력창을 자동 줄바꿈 및 높이 조절 `<textarea>`로 개선하여 텍스트 잘림 현상 방지.
4. **처방전 커스텀 상태 띵커보드 100% 실시간 반영**:
   - 학생이 처방전에서 직접 작성한 [현재 상태]와 [목표 상태](예: "화장실이 급한 상태" ➔ "일을 보고 시원해진 상태")가 띵커보드 제출 팝업 및 최종 제출 카드에 누락 없이 1:1로 온전히 연동되도록 상태 파이프라인 동기화.

### 3.10 📐 띵커보드 보고서 텍스트 겹침 해결 · 실제 캔버스 1:1 미니어처 캡처 · 구조적 합격 인증 게이트웨이 · 중2 학생 친화적 용어 대개편 (`js/labs/lab-flowchart.js`, `index.html`)
1. **띵커보드 보고서 텍스트 겹침 및 우측 잘림 완벽 해결 (`drawWrappedText()`)**:
   - 기존의 섹션 1(추상화) 2열 고정 좌표(`현재 상태` x=50, `핵심 변수` x=440) 충돌 버그를 세로형 3단 레이아웃([현재 상태] ➔ [목표 상태] ➔ [핵심 변수 & 조건])으로 전면 개편.
   - 텍스트 폭을 실시간 측정하여 단어/글자 단위로 지능형 자동 줄바꿈을 수행하는 `drawWrappedText(ctx, text, x, y, maxWidth, lineHeight, maxLines)` 헬퍼 구현.
   - 섹션 1과 섹션 2(자연어 기획서) 카드 너비(730px) 안에서 최대 2~3줄 줄바꿈을 지원하여 아무리 긴 설명문도 글자 겹침이나 테두리 밖 잘림 없이 100% 명확히 인쇄.
2. **순서도 다이어그램: 가상 5블록 수직 뼈대 폐지 ➔ 실제 캔버스 1:1 미니어처 캡처 엔진 탑재 (순수 Canvas 2D)**:
   - 외부 캡처 라이브러리(`html2canvas`)를 배제하고 순수 Canvas 2D API로 `freeBlocks`와 `freeConnections` 전체의 바운딩 박스를 실시간 측정.
   - 섹션 3 다이어그램 영역(W=780, H=360)에 최적의 여백(30px)을 두고 중앙 정렬되는 Auto-scale & Center fit 렌더링 알고리즘 적용.
   - 도트 모눈 배경 위에 단말(타원), 자료(평행사변형), 판단(마름모), 처리(직사각형) 4대 표준 기호와 엔트리 색상, Manhattan 직각 연결선, 4방향 인식 화살표 촉, `[예]`/`[아니오]` 분기 배지를 고해상도 벡터로 1:1 완벽 축소 렌더링.
   - 학생이 캔버스에 실제로 배치하고 연결한 순서도 작품이 1px의 오차도 없이 보고서에 그대로 영구 박제.
3. **구조적 합격 인증 제출 게이트웨이 (3단 완주 검증 체계)**:
   - AI 설계 검사를 통과하지 않은 미완성 상태의 무분별한 띵커보드 제출을 차단하고 자기주도적 성찰을 유도.
   - **게이트웨이 1 (상단 툴바 버튼)**: 초기 상태에서는 `[ 🔒 AI 검사 후 제출 ]`로 잠겨 있으며, AI 검사를 통과(`window.isFlowchartAiPassed === true`)해야만 반짝이는 에메랄드 `[ 💮 띵커보드 제출 ]` 버튼으로 전환.
   - **게이트웨이 2 (모달 진입 차단)**: 미통과 상태에서 제출 시도 시 즉각 친절한 안내 팝업을 띄우고 `diagnoseFreeAlgorithmWithSolarAI()`(AI 검사 모달)로 자동 연결.
   - **게이트웨이 3 (모달 내부 이중 잠금)**: 미통과 시 하단 [이미지 복사] / [다운로드] 버튼을 `disabled` 처리하고 자물쇠 배너 노출.
4. **중2 학생 눈높이에 맞춘 용어 대개편 ('감수' ➔ '검사 / 점검')**:
   - 중2 학생들에게 생소하고 어려운 한자어 '감수(監修)'를 학생들의 일상적인 교실 언어인 **'AI 설계 검사'**, **'AI에게 검사받기'**, **'AI 점검 결과'**, **'AI 검사 통과 (합격 인증)'**으로 전면 교체 (`AGENTS.md` 제3조 6항 준수).

### 3.11 🔍 AI 설계 검사 구멍(Dead End) 원천 방어 · 캔버스 무한 작업대 확장 · 띵커보드 학번/이름 UX 개편 (`js/labs/lab-flowchart.js`, `index.html`, `css/flowchart.css`)
1. **AI 설계 검사 그래프 완주 도달성(Full Reachability & Dead End) 검증 엔진**:
   - **판단(Decision) 기호 분기 완결성 검사**: 모든 마름모 판단 기호는 최소 2개의 독립적인 나가는 연결선('예' 및 '아니오')을 반드시 구비해야 함 (`⚠️ [판단 분기 누락]`).
   - **비단말 블록 나가는 선 필수 검사**: `종료` 단말을 제외한 모든 블록은 나가는 화살표가 필수 (`⚠️ [미완성 경로 발견]`).
   - **양방향 도달성(Forward & Backward Reachability) 알고리즘**: 시작에서 출발하여 종료로 도달할 수 없는 모든 중간 끊김 분기(막다른 길)를 검출하여 AI 검사 통과를 원천 차단 (`⚠️ [종료 미도달 분기 발견]`).
   - **고립/부유 블록 탐지**: 시작 블록의 실행 흐름과 연결되지 않은 미아 블록을 감지하여 휴지통 정리 유도.
2. **캔버스 무한 작업 공간 확장 & 휴지통 드롭 UX 개선**:
   - 기존의 `stage.clientWidth` 화면 크기 제한을 완전히 제거하여 `-2500px ~ +4500px`의 광활한 가상 작업대 지원. 캔버스를 멀리 이동해도 블록이 화면 중간에서 멈추던 '보이지 않는 벽' 영구 해소.
   - 기호 보관함 클릭 시 현재 뷰포트 시야의 정중앙에 블록이 스마트 자동 스폰.
   - 휴지통 오버 시 드래그 중인 블록이 투명해지며 축소(`scale(0.8), opacity(0.45)`)되는 삭제 예고 피드백 및 드롭 시 즉각 안전 삭제.
3. **띵커보드 제출 학번/이름 UX 대개편 ('홍길동 박멸')**:
   - 하드코딩된 기본값(`20315`, `홍길동`)을 완전 삭제하고 초기 공란(`""`)으로 초기화.
   - 띵커보드 제출 모달 오픈 시 학번 입력창으로 즉시 커서 자동 이동(`autofocus`) 및 눈길을 사로잡는 인디고 네온 펄스 링(`student-input-highlight`) 부여.
   - 학번/이름 미입력 상태에서 이미지 복사/다운로드 시도 시 제출을 즉시 차단하고, 입력창 흔들림(`input-shake`) 및 친절한 안내 알림 표시.
   - 동일 브라우저 세션 동안은 `sessionStorage`에 학번/이름을 자동 보존하여 학생의 재입력 편의 제공.

---

## 4. 멀티 디바이스 원드라이브 워크플로우 가이드

1. **설치 제로 (Zero-Setup)**:
   - 본 프로젝트는 순수 HTML/CSS/JS 기반이므로, 노트북이나 데스크탑 어디서든 폴더를 열고 `index.html`을 브라우저에 띄우면 100% 정상 작동합니다.
2. **동기화 확인**:
   - 컴퓨터를 바꿀 때는 OneDrive 작업 표시줄 아이콘의 동기화 완료 여부를 확인합니다.
3. **AI 인수인계**:
   - 새 컴퓨터에서 AI를 켤 때: **`"AGENTS, INTENT, PRD 읽고 작업 이어가자"`** 한 문장으로 즉시 모든 맥락이 동기화됩니다.

