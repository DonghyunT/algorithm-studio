# 정보 알고리즘 스튜디오 제품 요구사항 명세서

> 2026-09-22 제품 정비 및 운영 고도화:
> 1. **컴퓨터실 시계 오차(Clock Skew) 방어**: 특정 PC의 로컬 시계 왜곡으로 인한 조기 제출 사고 방지를 위해 서버 시간 오프셋 자동 동기화 및 `getNow()` 일원화.
> 2. **교사용 학생 관리 모달 데스크탑 2단 분할 개편**: 좌측 스마트 액션 그룹(프로필, 실시간 잔여시간, 상태별 복귀/연장/마감) 및 우측 답안 검토/채점 분리.
> 3. **제출 취소 및 답안 100% 보존 복귀 (`reopenStudentExam`)**: 조기 제출 학생의 답안 보존 및 추가 시간(+5분/+10분) 부여, 자동 아카이빙.
> 4. **실시간 리스너 영구 지속성 및 타이머 연속성 보장**: 새로고침/재접속 시 리스너 누락 방지(`attachAssessmentListeners`), 개별 시간 연장 즉시 반영, 강제 정상 제출 마감 실시간 전환, 진행 중 평가 뒤늦은 입장 시 즉시 시험 화면 직행.
> 5. **제출 완료 화면 UX 개선**: 문항별 답안/정답 확인 버튼의 점수 카드 섬 통합 및 하단 로드맵/로그아웃 버튼 재배치.
> 6. **관제탑 실시간 점수 회차 격리**: V4 캐시를 `attemptId`로 묶고 V3 모의평가 및 늦게 도착한 이전 회차 응답과 분리.

> 최종 갱신: 2026-09-24 · 기능 요구사항과 상태의 단일 기준(SSOT)
> 관제탑 점수 회차 격리 수정은 PR #5로 main에 병합했고 Vercel Production 반영과 기존 운영 주소의 JS 일치를 확인했습니다. 교사용 보관 회차 읽기 전용 조회는 PR #7로 main에 병합했으며, 병합 커밋의 Vercel 상태가 성공했습니다. 해당 변경의 로컬 검사는 정적 검사 73개, 단위 테스트 130개, 데모 브라우저 화면 확인이 통과했습니다. 실제 운영 로그인·Firestore 자료 접근은 확인하지 않았습니다. 배포 근거와 제한은 [배포 기록](docs/DEPLOYMENT.md)을, 실제 DB 구성은 [데이터베이스 설계](docs/DATABASE.md)를 확인합니다.
>
> **현재 운영 상태 정정(2026-09-18, 선생님 확인):** V4 실전평가도 현재 운영에 적용되어 사용 중입니다. V3는 모의평가 및 기존 회차 호환 경로로 유지합니다. 회차 재현·비공개 보관·교실 전체 검증은 별도 확인 대상입니다.

## 1. 목적과 문서 기준

### 2026-09-18 처방전 팝업 표시 정정 (운영 반영 완료)

운영 반영: 사용자 승인 후 제품 커밋 `c58e95d`를 main에 병합·푸시했습니다. 운영 HTML이 로컬 수정본과 일치하며, Edge 1440×900에서 팝업 실제 표시·닫기 및 BODY 직속 배치를 확인했습니다. 페이지·콘솔 오류 0건. 아래는 배포 전 로컬 검증 기록입니다.

`main c6aed98`의 운영 화면에서 처방전 모달의 숨김 클래스는 해제되지만, 누락된 HTML 닫는 태그 때문에 숨겨진 `classroom-live-detail-modal` 안에 포함되어 실제 크기가 0×0임을 확인했습니다. 기존 정상 노출 기록은 이 문제의 검증 근거로 사용할 수 없습니다. `codex/fix-prescription-modal-nesting`에서 `index.html`의 닫는 태그 1개를 복구했습니다. 로컬 Edge 시연에서 1440/1024/900/768/390px로 창 너비를 변경하며 팝업 표시·중앙 가림 여부·닫기를 확인했고, 처방전 적용 시 자연어 카드 5개와 단말 2개가 생성됨을 확인했습니다. 페이지·콘솔 오류 0건. 운영 반영 및 실제 AI 호출은 이번 검증에 포함하지 않습니다.

중학교 정보 수업의 **추수활동**에서 문제 추상화·알고리즘 설계·순서도를 쉽고 부담 없이 적용하도록 돕습니다. 일반 실습은 도움을 받아 완성하는 성공 경험을, 수행평가 탭은 제공된 점검 기능을 활용해 올바른 알고리즘을 완성하는 능력의 평가를 지향합니다.

교육 철학은 [INTENT.md](INTENT.md), 기술·협업 원칙은 [AGENTS.md](AGENTS.md), 운영 안내는 [README.md](README.md)를 따릅니다. 종전 세부 수치와 변경 기록은 [이전 PRD 보존본](docs/PRD_HISTORY_2026-09-12.md)에 보관합니다. 보존본의 상충하는 설계나 완료 표현은 현재 요구사항으로 적용하지 않습니다. 여기서 재확정하지 않은 세부 수치는 기존 코드 현황으로 취급하며, 이번 정비가 해당 기능의 삭제를 뜻하지 않습니다.

## 2. 학습 구성

개념 → 퀴즈 → 실습을 기본 동선으로 합니다. 단원별 2차시(45분씩)를 기본 수업 흐름으로 삼되 교사가 필요한 추수활동을 선택할 수 있도록 합니다.

| 코스 | 활동과 목적 |
|---|---|
| 문제 추상화 | 맛보기와 생각 다이어트 워크북으로 현재·목표 상태, 조건, 핵심 요소 구분 |
| 알고리즘 설계 | 샌드위치 로봇 명령 조립·실행으로 알고리즘 조건과 오류 수정 경험 |
| 순서도 연구소 | 순차(라면) → 선택(놀이기구) → 반복(비밀번호) → 예시(등교) → 나만의 백지 |
| 수행평가 | 별도의 도움·제출·채점 규칙 아래 답안과 순서도 작성 |
| 교사용 화면 | 평가 시작·종료, 제출 상태·답안 확인, 성적 검토·내보내기 |

## 3. 실습과 수행평가의 확정 정책

| 항목 | 일반 실습 | 수행평가 |
|---|---|---|
| 목표 | 도움을 받으며 완성하고 효능감 얻기 | 점검 기능을 활용해 올바른 알고리즘 완성 |
| 도움 | AI 조언과 단계적 힌트, 재실행 권장 | 문제 조건 후보만 AI로 추천 가능, 풀이 생성 금지; 연결·실행 사실 안내 |
| 논리 오류 | 학생이 수정하도록 보완 안내 가능 | 학생 입력으로 한 번 실행, 목표 충족 여부는 학생이 확인 |
| 정답 제공 | AI 순서도 자동 완성 없이 학생이 직접 조립·수정 | 정답·수정 방법·고쳐야 할 논리 블록 직접 지목 금지 |
| 제출 | 나만의 백지 띵커보드 제출은 현재 답안의 AI 설계 검사 통과 후 가능 | 미완성·오류·검사 미통과여도 제출 가능 |
| 평가 | 성공 경험과 다음 시도 지원 | Part 1·2 자동 채점 + 자유 설계 4항목 각 10점, AI 초벌 제안 후 교사 확정 |

### 3.1 일반 실습

2026-09-13 수업 후 사용자 피드백으로 나만의 백지 제출 정책을 변경했습니다. 검사 → 힌트 확인 → 수정 → 재검사 → 통과 표시가 있는 띵커보드 결과물 제출 순서입니다. AI 미통과·연결 장애 때 제출과 제출용 이미지 복사·다운로드를 비활성화하고 재검사 경로를 제공합니다. 기본 조립·실행은 계속 사용할 수 있습니다. 이 변경은 수행평가의 미완성 제출에 적용하지 않습니다.

2026-09-14 사용자 피드백으로 나만의 백지 AI 설계 검사 기준을 교육적 스캐폴딩과 학생 성취감 관점에서 정비했습니다.
1. 입출력과 처리의 유연성: '음료수 선택', '동전 투입', '온도 측정' 등 일상 행동에서 자료(입출력 ▱)와 처리(직사각형 ▭) 기호 중 어느 것을 사용했더라도 의미와 흐름이 통하면 적극적으로 인정하고 통과시킵니다.
2. 단일 선택(else 없는 if) 및 일상적 반복 표현(변수 증가식 없는 루프백)을 정상적인 알고리즘으로 허용합니다.
3. 자연어 카드 1개 ↔ 순서도 2~3개 등 단계 수준(추상화 레벨)이 차이 날 때는 오류로 단정하지 않고, 순서도의 구체화를 칭찬하며 기획서 카드를 추가해 짝을 맞추도록 친절한 보완 피드백을 안내합니다.
4. AI payload에 사람이 읽을 수 있는 텍스트 실행 흐름 요약(`flowchartFlowSummary`)을 추가하고 파서 마크다운 정규식을 보완하여 형식 에러를 방지합니다.
5. 학급 규모(11개 반) 및 재검사 수요를 고려하여 일일 AI 사용량 한도를 Firestore 규칙 상한인 1,000회로 상향하고, 사용량 도달/제한 시 원인을 명확히 알 수 있도록 액셔너블 한국어 안내 메시지를 강화합니다.
6. AI 설계 검사 결과 캐싱: 피드백 모달을 닫은 뒤 순서도/기획서에 수정이 없다면 불필요한 AI 호출과 토큰 낭비를 방지하기 위해 마지막 검사 결과를 즉시 재표시합니다. 캐시 결과 상단에는 "마지막 AI 검사 결과" 안내와 함께 필요 시 명시적으로 재호출할 수 있는 "AI에게 새로 검사 요청" 버튼을 제공합니다. 캔버스나 기획서가 변경되면 캐시는 즉시 무효화됩니다.
7. 실습-평가 전환 시 순서도 공방 작업대 및 처방전 복원: 학생/교사가 수행평가(Part 3 또는 CBT 17번)에 진입했다가 상단 메뉴를 통해 일반 실습(나만의 백지)으로 복귀할 때, 평가 격리 모드(`assessmentWorkspace.leave()`)가 정상 해제되어 캔버스 툴바의 `[처방전 채우기]` 버튼이 노출되고 처방전 모달(`prescription-modal`)이 정상 동작하도록 네비게이션 생명주기를 보장합니다. 또한 1코스(문제 추상화)에서 처방전을 작성한 경우, 나만의 백지 처방전 모달의 '⭐ 나만의 문제 직접 입력' 탭 진입 시 해당 상태가 자동으로 채워지도록 연계를 강화했습니다.

AI 의견, 구조 검사, 실행 결과를 구분합니다. AI 미응답을 합격으로 대체하거나 AI 의견을 공식 정답 인증으로 표현하지 않습니다. 순서도나 자연어 기획을 수정하면 이전 검사 결과를 무효화합니다. **이번 요청으로 AI 설계 검사 통과를 나만의 백지 제출 조건으로 다시 적용합니다.**

### 3.2 수행평가

새 자유 설계 회차의 흐름은 **자기 문제의 현재·목표 상태 정리 → 필요한 조건 선택·작성 → 자연어 알고리즘 작성 → 순서도 조립 → 실행·수정 → 제출**입니다. 이전 고정 주제 회차는 당시 문항과 채점 기준을 보존합니다.

* Part 3은 학생이 자기 문제를 분석하는 빈 처방전을 제공합니다. 현재·목표 상태는 학생이 직접 쓰고, 조건도 직접 입력하거나 AI의 제약 조건 후보 중 필요한 것을 골라 수정합니다. AI가 행동 순서나 완성된 풀이를 작성하지 않습니다. 실습과 같은 순차·선택·반복 카드를 직접 추가·수정·재배열하며 세 구조를 모두 쓰도록 요구하지 않습니다. 일반 실습의 AI 처방전은 유지합니다.
* 처방전은 답안과 함께 저장·복원하고 교사 답안 화면에 표시합니다. 승인된 자유 설계 기준은 문제·조건의 명확성, 자연어 알고리즘의 논리성, 자연어와 순서도의 일치, 순서도 구조·동작의 타당성 각 10점입니다. 문장력·단계 수·AI 사용 여부를 채점 기준으로 추가하지 않으며 미작성도 제출을 막지 않습니다.
* 평가가 시작되면 다른 코스·교사 화면·전체 메뉴로의 앱 내 이동을 막고 Part 1~3 전환은 허용합니다. 같은 탭 새로고침 시 진행 중인 평가로 복귀합니다. 제출 저장에 실패하면 잠금을 유지하며 저장 성공 후 해제합니다. 대기 중에는 이동할 수 있습니다. 다른 브라우저 탭·외부 사이트까지 차단하는 기능은 아닙니다.
* 각 파트 하단에도 이전·다음 이동 버튼을 제공합니다. 미응답이 있어도 이동할 수 있고 작성 개수와 건너뛰기 안내를 표시합니다. 모든 문항에 응답했을 때도 자동으로 이동하지 않습니다.
* 수동 최종 제출은 Part 3에 한 번 방문한 뒤 허용합니다. 현재 파트와 방문 상태는 같은 탭 새로고침 후 복원하며 교사의 재시험 초기화 시 함께 지웁니다. 학생이 Part 1·2에서 상단 최종 제출을 클릭할 경우 클릭을 무반응으로 막지 않고, Part 3 확인 안내 메시지를 표시하며 자동으로 Part 3 탭으로 전환하여 자연스러운 작성을 유도합니다. 시간 만료·교사 종료에 따른 자동 제출은 방문 여부와 무관하게 진행합니다. 미완성 답안도 제출할 수 있고, 실제 저장 성공 응답 후에만 완료로 표시합니다.
* 새 평가의 객관식 보기에서 영어 병기를 제거하고 제목은 `N번 문제`로 표시하여 정답 단서를 없앱니다. 단답형 5·6번은 변수·디버깅 용어 대신 선택 조건에 따른 결과와 반복 횟수를 묻습니다. 문항 수·배점·시간은 유지합니다. 회차별 문항 버전을 보존하여 기존 답안을 새 기준으로 재채점하지 않습니다.

* 연결 누락과 실행이 막힌 위치를 하이라이트 등으로 표시합니다. 색상 외에도 짧은 안내를 제공합니다.
* “이 단계에서 실행을 이어갈 수 없어요”처럼 관찰된 사실을 안내합니다. 중단 위치와 근본 원인이 같다고 단정하지 않습니다.
* 학생용 실행은 실습과 같은 입력·판단 상호작용으로 한 번의 흐름을 진행합니다. 입출력 기호가 입력을 요구하면 값을 받으며, 자연어 판단은 학생이 예·아니요를 직접 선택할 수 있습니다. 반복 구조가 입력으로 돌아오면 다시 값을 받습니다. 종료 도착을 정답으로 단정하지 않습니다. 이전 회차의 지정 검사 입력은 기존 참고 점수 계산에만 남기고 학생 실행 버튼에서 여러 번 자동 재생하지 않습니다.
* 평가 중 일반 실습용 AI 풀이 조언이 유입되지 않아야 합니다. 실행 엔진이 확인한 사실을 기반으로 피드백합니다.
* **대기실 기본 OFF 및 오픈 대기실만 입장**: 학급 세션의 기본 상태를 `ended`로 설정하여 교사가 '평가 준비'를 명시적으로 실행하지 않은 반에는 학생이 임의로 들어와 대기하는 문제를 차단합니다. 학생 로비 화면에서 선택된 학급의 실시간 세션 상태(대기실 열림 / 평가 진행 중 / 대기실 닫힘)를 배지로 실시간 안내합니다.
* **학생 대기실 나가기 및 좌석 자동 반환**: 번호·이름 오입력으로 잘못 입장한 학생을 위해 대기실 화면에 [대기실 나가기] 버튼을 제공합니다. 클릭 시 확인 후 대기 화면에서 빠져나와 번호·이름을 다시 입력할 수 있으며, 자신이 점유했던 대기 좌석을 서버/로컬에서 즉시 삭제·반환하여 진짜 해당 번호 학생이 '다른 기기에서 사용 중' 오류 없이 정상 입장할 수 있도록 보장합니다. 시험이 시작(in_progress)된 후에는 임의 퇴장이 차단됩니다.
* **교사용 대기실 닫기 제어 및 안전 2단계 일괄 닫기**: 교사 관제탑에서 학급이 '입장 대기(waiting)' 상태일 때 [대기실 닫기] 보조 버튼을 노출하여 교사가 대기실을 즉시 안전하게 닫을 수 있습니다. 또한 상단 학급 선택 바에 [모든 대기실 닫기] 버튼을 배치하여, 시험 중(in_progress)인 학급은 안전하게 보호하고 '입장 대기' 상태인 학급만 2단계 확인 질문(의도 확인 ➔ 대상 학급 목록 확인)을 거쳐 일괄 종료(ended)할 수 있도록 지원합니다.
* **교사용 빔프로젝터 스크린 점수 블라인드 보호 모드**: 교단 전면 화면에 학생 현황판을 띄워둘 때, 학생들이 보기를 바꿔가며 점수 변동으로 정답을 유추(Trial & Error 치팅)하거나 불필요한 성적 노출이 발생하는 것을 원천 차단합니다. 기본값으로 스크린 점수 숨김 모드가 활성화되어 좌석 카드에는 문항 풀이 진행률(예: '풀이중 8/16문항')만 노출되고 실시간 점수는 숨겨집니다. 필요한 경우 교사가 툴바의 [스크린 점수 숨김] 토글 버튼을 통해 점수 표시 모드로 전환할 수 있습니다. 개별 학생 카드를 클릭하여 열리는 상세 검토 모달에서는 블라인드 모드와 무관하게 언제나 정/오답과 상세 점수를 확인할 수 있습니다.
* **교사용 학생 상세 모달 내 순서도 2D 캔버스 시각화**: Part 3 채점 시 교사가 다이어그램을 보지 못하고 텍스트 태그와 AI 초벌 채점에만 의존하던 문제를 해결하기 위해, 순수 HTML5 Canvas API 기반의 고해상도 순서도 미니어처 렌더링 엔진을 모달 내에 탑재합니다. 엔트리 4대 기호 표준 색상(단말 🟣, 자료 🟢, 판단 🟠, 처리 🔵), 분기선(예/아니오), 화살표, 루프백 반복선이 선명하게 그려지며, 복잡한 순서도를 크게 검토할 수 있는 [크게 보기 (확대 팝업)] 라이트박스를 함께 지원합니다.
* **교사용 좌석 비우기 (유령 좌석·번호 오입력·결시 즉시 반환)**: 번호를 잘못 입력했거나 중복 로그인된 유령 좌석, 또는 결시 학생 좌석에 대해 교사가 학생 상세 모달에서 [좌석 비우기 (퇴장 처리)]를 실행할 수 있습니다. 교사 권한으로 Firestore 문서를 삭제하여 즉시 빈자리(미접속)로 반환하며, 진짜 해당 번호 학생이 '다른 기기에서 사용 중' 오류 없이 정상 입장할 수 있도록 보장합니다. 학생 화면이 켜져 있는 경우 좌석 초기화를 실시간 감지하여 대기/로비 화면으로 안전하게 복귀시킵니다.
* **교사용 현재 답안 정상 제출 및 평가 종료 시 일괄 마감 (S-PLAN 국가수준 CBT 표준)**: 기기 꺼짐, 배터리 방전, 창 닫기로 인해 미제출 상태(`in_progress` 또는 `waiting`)로 멈춰 있는 학생에 대해 교사가 개별 학생 모달에서 [현재 답안으로 정상 제출]을 눌러 즉시 마감할 수 있습니다. 또한 교사가 [평가 종료] 버튼을 클릭할 때 미제출 학생이 남아 있는 경우 안내 대화상자를 거쳐 현재까지 저장된 답안을 바탕으로 일괄 정상 제출(`submitted`) 처리 후 평가를 안전하게 종료합니다. 제출된 답안은 기존 자동 채점 로직을 통해 점수가 산출되며 성적 누락을 방지합니다.
* **교사용 백그라운드 AI 자동 초벌 채점 큐 및 빈 답안 0점 최적화**: 실전평가(V4) 학생이 제출(`submitted`)하면 교사가 일일이 학생 모달을 열어 수동 클릭하지 않아도, 교사 관제탑 백그라운드 큐(`AssessmentAutoReviewQueue`)에서 순차적으로 Solar AI 초벌 채점(`review.proposal`)을 자동 요청합니다. 서버의 교사 토큰 Rate Limit(분당 10회)을 엄격히 준수하기 위해 최소 6.8초 안전 간격을 보장(분당 최대 ~8.8회 호출)하여 단일 큐의 요청 빈도를 낮춥니다. 다른 탭·동시 요청·일일 한도에 따른 429까지 방지하는 것은 아닙니다. 또한 Part 3 순서도 블록이 0~1개이고 기획서 내용이 없는 빈 답안은 AI API를 호출하지 않고 클라이언트에서 즉시 0점 제안(`savePart3Review(..., 'proposal')`)으로 저장하여 API 토큰과 비용을 100% 절감합니다. 교사 최종 확정 원칙(`review.confirmed`)은 영구적으로 유지되며 교사가 모달에서 점수와 근거를 확인하고 [교사 점수 확정 저장]을 누르기 전까지 총점이나 공식 성적에 임의 반영되지 않습니다. 관제탑 상단에 실시간 진행 현황 배지가 표시되며, 좌석 바둑판의 학생 카드에 `AI제안` / `확정` 배지가 시각적으로 구분 표시됩니다.
* **성적표 CSV 내보내기 개선 (NEIS 양식 친화)**: Part 1(객관 30점) + Part 2(단답 30점)의 합산값인 **`지필소계/60`** 열을 신설하여 지필 평가 영역 합산을 한눈에 확인할 수 있도록 지원합니다. 응시상태를 한국어(`제출완료`, `풀이중`, `대기중`)로 명확히 표기하며, 긴 ISO 제출시각 문자열을 한국 시간 기준 **`YYYY-MM-DD HH:mm`** 형식으로 포맷팅하여 표시 길이를 줄입니다. 실제 엑셀 열 너비와 NEIS 가져오기는 별도 확인합니다.
* **Firestore 읽기/쓰기 요청 절감 (2026-09-18 구현 완료)**: 학생 원격 임시저장(`syncStudentProgress`) 디바운스를 0.5초에서 2.5초(2500ms)로 최적화하고 스마트 변경 감지(Dirty Check)를 적용하여 변경 없는 중복 원격 쓰기를 줄입니다. 순서도 캔버스 뷰포트 패닝(화면 이동)은 로컬 draft에만 저장하고 Firestore 쓰기를 유발하지 않으며, `listenSession`에서 `{includeMetadataChanges: true}`를 제거하여 2중 리스너 발화를 방지합니다. 브라우저 초안과 원격 저장은 별개입니다. 지연 저장 중 아직 전송되지 않은 변경은 PC 종료·초기화 때 복구되지 않을 수 있습니다. 종전 27명 기준 2,000~4,000건 수치는 실측 근거가 명시되지 않아 보장값으로 사용하지 않으며 교실 사용량은 별도 측정합니다.
* 항목별 점수·근거·불확실성을 제안으로 저장하며 교사가 답안을 보고 점수를 확인·수정하여 확정합니다. AI 오류·해석 불가·미확정은 0점이 아닌 채점 대기로 표시합니다. 확정 전 총점과 CSV 최종 점수도 채점 대기로 남깁니다.
* **오류 수정 이유 한 문장, 추가 성찰 보고서, 힌트 사용 횟수·수정 과정에 따른 별도 채점이나 감점을 추가하지 않습니다.** 기존 단답형 문항을 삭제한다는 뜻은 아닙니다.
* 풀이 완성 여부와 제출 가능 여부를 구분합니다. 제출 마감·권한·저장 상태는 별도 운영 규칙을 따릅니다.

### 3.2.1 KERIS 국가수준 CBT 단일 문항 집중형 평가 인터페이스 — 운영 배포 완료

2026-09-15 사용자 요청에 따라 기존 3단 탭 방식(Part 1, 2, 3 전체 노출)의 평가 화면을 **KERIS 국가수준 초·중학생 AI·디지털 리터러시 검사 표준 규격의 1문항 집중형 CBT 인터페이스**로 개편하였습니다.

1. **기본 모드 및 뷰 전환**:
   - 수행평가 화면 진입 시 단일 문항 모드가 기본값(`isCbtMode = true`)으로 작동합니다.
   - 우측 상단의 `[모아서 보기 / 한 문제씩 보기]` 토글 버튼을 통해 중2 학생 눈높이에 맞추어 1문항 집중 화면과 영역별 전체 문항을 모아보는 화면을 손쉽게 전환할 수 있습니다.
2. **KERIS 표준 OMR 네비게이션 팔레트 (좌측 사이드바)**:
   - **안 푼 문항**: 연회색 (`#c6c7c8`, 글자 `#212529`)
   - **현재 문항**: 선명한 블루 (`#0d6efd`, 흰색 볼드 글자, 외곽 링 글로우)
   - **푼 문항**: 짙은 회색 (`#676767`, 흰색 볼드 글자)
   - **영역 뱃지**: 활성 영역 선명한 블루 (`#3d8bfd`), 비활성 영역 중립 회색 (`#6c757d`)
   - 하단에 KERIS 국가수준 검사 표준 범례(안 푼 문항/현재 문항/푼 문항) 및 바로가기 클릭 지원.
3. **1문항 뷰어 및 순차 네비게이션 (우측 메인)**:
   - 상단 36x36px SVG 번호 뱃지와 첫 줄 텍스트의 상단 광학적 정렬(Optical Alignment) 적용.
   - 데스크탑 모니터 기준 고정 높이(`calc(100vh - 150px)`) 및 헤더/푸터 양끝 고정(`justify-between`), 바디 내부 스크롤로 문항 길이와 무관하게 카드 높이 및 하단 버튼 위치 100% 불변 고정.
   - 독립된 컴팩트 시스템 안내 띠지(`.cbt-notice-strip`): *"정답을 선택(입력)한 후, 하단의 '다음 문항' 버튼을 클릭하세요."*
   - 하단 푸터 바: `[< 이전 문항]`, `[17문항 중 X문항 응답 완료]`, **`[다음 문항 →]`**(1~16번) / **`[다음 단계: 순서도 조립 →]`**(17-1) / **`[답안 검토 및 최종 제출]`**(17-2) 버튼으로 심리적 불안 없는 순차 풀이 지원.
4. **17번 문항 (Part 3) 2단계 순차 워크플로우**:
   - **1단계: 문제 분석 및 계획 수립** — 좌측 [도전 과제 & 채점 기준] 카드(문제 상황, 🚩 시작 상태, 🎯 목표 상태, 📌 4대 채점 기준) + 우측 [나의 문제 해결 계획 (문제 정의 및 규칙)] 카드(시작·목표 상태 가로 2열 그리드 및 지켜야 할 규칙) ➔ 하단 메인 푸터 버튼 단일화(중복 버튼 제거).
   - **2단계: 순서도 조립 및 실행 검증** — 상단 '1단계 계획 요약 확인(시작 상태, 목표 상태, 지켜야 할 규칙)' 미니바 + 순서도 캔버스 공방 + `[1단계 계획으로 돌아가기]` 양방향 전환 지원.
   - **OMR 사이드바 접기/펼치기 및 캔버스 자동 확장**: OMR 상단 접기 버튼(`[<]`), 메인 헤더 열기 버튼(`[> 문항 목록]`) 지원. 특히 **17-2단계 진입 시 사이드바가 자동으로 접혀** 넓은 캔버스 작업 공간을 즉시 확보.
5. **기기별 뷰포트 여백 최적화**:
   - **11~12인치 디벗 (갤럭시탭 S8/S9, WQXGA 2560x1600, 16:10 뷰포트 1280x800)**:
     - 평가 중 상단 네비게이션 바 숨김 및 글로벌 헤더 1열 슬림화, 컴팩트 마진/패딩 적용.
     - 800px 화면 높이 안에서 스크롤 없이 문항 지문, 4개 선택지 보기, 하단 `[다음 문항]` 버튼이 한눈에 100% 표시되는 스크롤 제로(Zero-Scroll) 달성.
     - 손가락 터치에 편리한 36~44px 터치 타깃 보장.
   - **24인치 모니터 (1920x1080 데스크탑)**:
     - 메인 카드 높이 고정 및 하단 버튼 위치 완벽 고정, 최대 1200px 중앙 정렬로 편안한 시선 이동 및 쾌적한 폰트/간격 유지.
6. **데이터 무결성 및 호환성**:
   - Firestore 제출 데이터 형식(`answers.part1, part2, part3`) 및 자동 채점/교사 검토 로직과 100% 동일하게 호환.
7. **2026-09-15 2차 현장 피드백 반영 (컴퓨터실 모니터 가독성 & 사용성 13대 고도화)**:
   - **좌측 OMR 팔레트 겹침 해소**: 고정 픽셀 및 확대 애니메이션 제거, 반응형 정사각 타일(`aspect-square`) 및 외곽선 링 강조(`outline`)로 이웃 타일 침범 없이 정갈하게 정돈. 사이드바 너비 `w-60`~`w-64`로 확장.
   - **Part 3 사이드바 17-1/17-2 분할**: 목록 버튼을 `17-1. 분석`, `17-2. 조립` 2개로 분할하고 어색한 40점 텍스트를 제거.
   - **문제 지문 폰트 대형화 & 가독성 줄바꿈**: 문제 텍스트를 `text-lg/xl font-extrabold`(18~20px)로 시원하게 키우고 단락 줄바꿈(`\n\n`)으로 문장별 가독성 강화.
   - **지문 내 조건 카드 상자(`.cbt-condition-box`) 시각화**: 10번, 13번, 15번 등 알고리즘 규칙/흐름을 수능/국가검사 스타일의 독립된 카드 박스로 구조화 분리.
   - **안내문구 독립 띠지(Notice Strip) 분리**: 지문 인라인에 섞여 있던 안내문을 지문 아래 별도의 정갈한 컴팩트 안내 띠지(`.cbt-notice-strip`)로 분리.
   - **주관식 문항 번호 단일화 & 노란 박스 제거**: `<16번 문제 (6번)>`에서 혼란을 유발하는 괄호를 제거하고 `<16번 문제>`로 통일. 정답 입력창 상단에 획일적으로 반복되던 노란색 안내 박스를 삭제하고 입력창 가시성 대폭 확대.
   - **버튼 문구 안심 개편**: 오해를 유발하던 "제출하고 이동하기"를 1~16번 `[다음 문항 →]`, 17-1 `[다음 단계: 순서도 조립 →]`, 17-2 `[답안 검토 및 최종 제출]`로 개편.
   - **17-1 용어 및 레이아웃 정돈**: '처방전' 탈피 ➔ `나의 문제 해결 계획`, `🚩 시작 상태 (현재 상황)`, `🎯 목표 상태 (해결 결과)`로 교과 표준 용어화하고 가로 2열 그리드로 높이 압축. 채점 기준(40점)은 좌측 카드 하단으로 이동.
   - **17-2 요약 바 규칙(조건) 표시**: 1단계 계획 요약에 시작 상태, 목표 상태, 지켜야 할 규칙(조건)을 모두 한눈에 표시.
   - **맥락 기반 스마트 '조건 아이디어 받기'**: 학생이 작성한 현재·목표 상태의 키워드(온도, 조명, 시간, 수분, 청소 등)를 분석하여 맞춤형 제약 조건 후보를 생성하고, 클릭 한 번으로 추가되는 인터랙티브 칩 버튼(`.cbt-condition-chip`) 제공.
8. **2026-09-17 CBT 2차 UI/UX 고도화 (지필 2열 분할·슬림 레일·캔버스 섬 밀봉 완료)**:
   - **지필고사형 좌우 2열 분할 레이아웃 (`.cbt-question-grid`)**: 9번 문항처럼 조건이나 규칙이 긴 경우 보기가 화면 아래로 밀려 스크롤이 발생하던 문제를 해결. 화면을 좌우 2열(좌측: 발문 + 컴팩트 조건 상자 + 질문 / 우측: 1~5번 보기 선지 or 단답형 입력창)로 분할하여 데스크탑/태블릿에서 스크롤 제로(Zero-Scroll) 및 하단 네비게이션 버튼 위치 100% 불변 고정 달성. 모바일(768px 미만)에서는 자연스럽게 1열로 수직 전환. 우측 선지 컨테이너에 `margin: auto 0;` 안전 정렬을 적용하여 작은 화면에서도 1번 보기가 위로 잘려 스크롤되지 않는 flex-center 오버플로우 버그를 원천 차단.
   - **문항목록 사이드바 슬림 레일(Rail) 스무스 축소 트랜지션 & 사용자 선호 유지**: 접기(`<`) 버튼 클릭 시 사이드바가 갑자기 사라지지 않고 56px 슬림 레일 바로 부드럽게(`transition 0.3s cubic-bezier`) 축소. 레일에는 `>` 펼치기 버튼, 세로쓰기 '문항 목록' 타이틀, 현재 문항 번호 뱃지(`eval-cbt-rail-cur-badge`), 미니 응답 비율(`eval-cbt-rail-ratio`)이 항시 유지되어 언제든 즉시 펼칠 수 있음. 특히 학생이 직접 접어둔 경우 문항을 이동하거나 새로고침해도 접힌 상태가 유지되며, 17-2단계(순서도 조립)에서 자동 축소된 후에도 복귀 시 학생의 원래 사이드바 선호 상태(`userSidebarCollapsed`)로 자연스럽게 복원.
   - **순서도 조립(17-2) 캔버스 섬 완벽 밀봉 및 100% 부모 피팅**: 도트 그리드 배경이 둥근 모서리 섬 밑으로 삐져나오던 현상을 `#eval-cbt-workspace-host` 및 `#fc-level3-view`, `.entry-studio-container`에 `height: 100% !important`, `min-height: 0 !important`, `overflow: hidden !important`, `rounded-2xl` 적용으로 완벽 차단 밀봉. 1단계 요약 바를 인라인 아코디언 컴팩트 스타일로 개편하고 캔버스 하단의 중복 버튼 줄을 정리하여 캔버스 유효 조립 공간을 극대화.
9. **2026-09-17 CBT 3차 UI/UX 정밀 고도화 (교육과정 표준 용어 통일·17-1 광학 정렬·17-2 캔버스 확장·조건 카드 표준화 & 마이크로 인터랙션 완료)**:
   - **교육과정 표준 용어 통일**: '시작 상태' 표기를 2022 개정 교육과정 성취기준 표준에 맞추어 `🚩 현재 상태 (주어진 상황)` 및 `① 현재/목표 상태 정의`로 전면 일원화하고, 17-2 상단 요약 바 및 채점 가이드까지 100% 동기화.
   - **17-1 계획 수립 카드 상단 광학적 정렬 (Optical Alignment)**: 좌측 [도전 과제 & 채점 기준] 카드와 우측 [나의 문제 해결 계획] 카드의 시작 높이가 18px 어긋나던 마진 상속 버그(`#eval-plan-guide`의 `margin-block: 18px;`)를 `margin-block: 0 !important;` 및 패딩 균형(`p-5`)으로 교정하여 두 섬의 상단 시작 위치를 1px 오차 없이 완벽 수평 일치.
   - **모든 문항 조건/규칙 카드형 표준화 (`.cbt-condition-box`)**: 7번, 10번, 15번 등 문항 지문에 조건이나 규칙이 제시된 모든 문항에 대해 연한 블루 틴트 배경, 클립보드 아이콘 뱃지(`지켜야 할 규칙 / 조건`), 16px+ 볼드 폰트의 독립된 조건 카드로 일관되게 렌더링. 단일 개행(`\n`) 분할 정규식 보강 및 중복 `[규칙: ]` 래퍼 태그 자동 정제.
   - **15번 등 조건 박스 시각 계층 및 여백 최적화**: 조건 카드의 불필요한 공백을 줄이고 본문과 어울리는 크기(`1.025rem`, `font-bold`)로 조정하여 문제 전체가 하나의 완성된 블록으로 자연스럽게 어우러지도록 개선.
   - **17-2 순서도 조립 캔버스 스크롤 및 작업 공간 확장**: 1~16번 및 17-1번은 스크롤 제로(Zero-Scroll) 및 하단 버튼 위치 고정을 철저히 유지하되, 17-2단계(순서도 조립)에서는 `#eval-cbt-body`에 `overflow-y-auto`를 동적 허용하고 캔버스 최소 높이를 640px+로 확장하여 넓고 쾌적한 블록 조립 공간 제공.
   - **17-2 캔버스 모서리 잔여 음영/테두리 결함 제거**: `#eval-cbt-workspace-host` 내부의 `.entry-studio-container`에 남아 있던 이중 보더 및 드롭 섀도우를 제거하여 도트 그리드 캔버스가 깔끔하게 모서리 라운드에 밀착되도록 마감.
   - **토스/애플 스타일 마이크로 인터랙션 (Bouncy Touch & Smooth Motion)**: OMR 문항 버튼, 선택지, 하단 액션 버튼에 부드러운 스프링 터치 반응(`active:scale-96`, `cubic-bezier(0.34, 1.56, 0.64, 1)`)을 부여하고, 문항 전환 시 은은한 페이드인 애니메이션(`.cbt-smooth-enter`)을 적용.
   - **단위 테스트 확대**: 신규 기능 및 예외 케이스를 커버하는 3개 테스트 추가, 총 75개 테스트 100% 통과.
10. **2026-09-17 CBT 4차 UI/UX 정밀 고도화 (좌우 섬 높이 100% 일치·순서도 캔버스 언클램프·단답형 자동 포커스 & Enter 키 지원)**:
   - **문항목록 섬과 문제 섬 높이 100% 일치 (Pixel-Perfect Island Alignment)**: `#eval-cbt-sidebar`와 `#eval-cbt-main`에 공통 데스크탑 높이 규칙(`height: calc(100vh - 150px) !important; min-height: 600px !important; max-height: 860px !important;`)을 적용하고, 컨테이너 min-height 충돌 및 하단 10px 패딩 상속을 제거하여 1~16번과 17-1번에서 좌우 두 섬의 상단(Top)과 하단(Bottom)이 1px 오차 없이 완벽하게 수평 일치하도록 교정.
   - **순서도 조립(17-2) 섬 높이 고정 해제 (Unclamped Canvas Workspace)**: 17-2단계 진입 시 `#eval-cbt-container`에 `cbt-step2-unclamped` 클래스를 활성화하여 메인 섬의 860px 높이 제한 및 overflow hidden을 해제. 캔버스 작업 공간 `#eval-cbt-workspace-host`의 높이를 `720px`로 넉넉하게 확장하여 자연어 기획서 폼, 자연어 카드 안내 블록, 기호 보관함 툴박스가 밑으로 잘리지 않고 온전하게 노출되도록 보장하며, 화면 크기에 맞춰 페이지 전체가 자연스럽게 스크롤되도록 지원. 17-1단계나 다른 문항 복귀 시 즉시 고정 높이 모드로 복원.
   - **단답형(11~16번) 자동 포커스 & Enter 키 연속 풀이 지원**: 단답형 문항 진입 시 정답 입력창(`cbt_inp_${q.id}`)에 자동으로 포커스가 들어가고 커서가 맨 뒤로 이동하여 마우스 조작 없이 키보드로 즉시 입력 가능. 정답 입력 후 `Enter` 키 입력 시 다음 문항으로 자동 이동하여 지필고사처럼 매끄러운 응시 흐름 제공.
   - **단위 테스트 확대**: 신규 기능 검증 테스트 2종 추가, 총 77개 테스트 100% 통과.
11. **2026-09-17 CBT 5차 정밀 고도화 (17-2 요약 아코디언 기본 열림·일상생활 문제 AI 채점 기준 보정 & 조건 추천 일상 맥락 반영·유연한 단말 기호 검증 및 구체적 오류 안내)**:
   - **17-2 순서도 조립 진입 시 1단계 계획 요약 아코디언 기본 열림 (`<details open>`)**: 17-2로 넘어왔을 때 1단계에서 자신이 세운 현재 상태, 목표 상태, 지켜야 할 규칙을 잊지 않고 한눈에 대조하면서 순서도를 조립할 수 있도록 기본 상태를 펼침(`open`)으로 변경.
    - **중2 학생 일상생활 고민 맞춤형 AI 채점 기준 캘리브레이션 (`api/assessment.js`)**: 중2 학생들이 작성하는 문제 상황(예: '졸린 상태' ➔ '꿀잠 자는 상태', '심심함' ➔ '게임 완료')이 단순하다는 이유로 AI가 감점(6~7점)하지 않고, 현재-목표 상태가 성립하고 판단 가능한 조건 및 타당한 순서도가 구성되었다면 9~10점 만점을 적극 부여하도록 프롬프트 채점 기준 완화 및 명문화.
    - **조건 아이디어 추천 프롬프트 일상생활 맥락으로 전면 개편 (`api/assessment.js`)**: 복잡한 기계 센서 대신 중2 학생들의 실제 일상 고민(수면, 식사, 등교, 숙제, 공부, 운동, 스마트폰 사용 시간, 날씨 등)에 어울리는 구체적인 수치와 판단 기준을 추천하도록 프롬프트 가이드라인 조정.
    - **순서도 단말 기호 텍스트 검증 유연화 및 구체적 단절 위치 안내 (`js/core/flow-validation.js`)**:
      - 시작 단말 기호: `시작`, `출발` 등 유연하게 허용 (`/^(?:시작|출발)/`)
      - 종료 단말 기호: `종료`, `끝`, `마침`, `도착` 등 학생들의 자연스러운 표현 유연하게 허용 (`/^(?:종료|끝|마침|도착)/`, 문장 부호 제거)
      - 경로 단절 시 막연히 "종료로 이어지는 길이 없습니다"라고만 하던 오류 메시지를 개선하여, 어떤 기호에서 끊겼는지 구체적 블록 이름(`'창문 닫기' 단계에서 종료로 이어지는 길이 없습니다`) 또는 판단 기호의 분기 누락 여부(`판단 기호('...')의 '예' 또는 '아니오' 분기 중 하나가 종료로 이어지지 않습니다`)를 친절하게 안내.
    - **단위 테스트 추가**: 17-2 요약 아코디언 기본 open 속성 검증 및 단말 기호 유연 판정/구체적 단절 에러 메시지 검증 테스트 2종 추가, 총 79개 테스트 100% 통과.
12. **2026-09-18 CBT 6차 정밀 고도화 (단일 단락 및 인라인 조건·알고리즘 상자(`.cbt-condition-box`) 분리 렌더링)**:
    - **배경 및 문제**: 14번, 15번 등 문항 지문이 줄바꿈(`\n\n`) 없이 단일 단락으로 작성되었거나, 조건식(`[나이 >= 14 이고 나이 < 19]`)이나 알고리즘 실행문(`알고리즘(...)`)이 일반 평문 지문과 한 줄로 뒤섞여 있는 경우, 조건 상자(`.cbt-condition-box`)로 분리되지 않고 빽빽한 일반 텍스트로 노출되어 중2 학생들의 가독성을 저해하던 현상 발견.
    - **해결책 (`js/labs/lab-eval.js`의 `formatCbtPrompt`)**:
      - **인라인 알고리즘 분리 (`알고리즘(...)` / `알고리즘[...]`)**: `귤 14개를 3개씩 상자에 담는 알고리즘(...)` 형태를 감지하여 상단에 발문(`귤 14개를 3개씩 상자에 담는 알고리즘입니다.`), 독립된 `.cbt-condition-box`에 알고리즘 본문(`남은 귤 >= 3 인 동안 [상자수 = 상자수 + 1, 남은 귤 = 남은 귤 - 3]`), 하단에 질문(`알고리즘이 종료되었을 때, 최종적으로 완성된 [상자수](숫자만)를 쓰시오.`)으로 3단 광학 구조화 분리.
      - **인라인 조건식 분리 (`[조건이/조건은/규칙: [식] 입니다]`)**: `청소년 요금 적용 조건이 [나이 >= 14 이고 나이 < 19]입니다.` 형태를 감지하여 상단 발문(`청소년 요금 적용 조건입니다.`), 조건 상자에 수식(`나이 >= 14 이고 나이 < 19`), 하단에 본 질문(`이 조건의 거짓(아니오) 분기에 해당하는 나이 중...`)으로 명확히 시각 분리.
      - **판단 기호 및 조리법/규칙 인라인 분리**: `스마트 에어컨 알고리즘에 [식] 라는 판단 기호가 있습니다` 및 `[라면 조리법] '내용'와 같이...` 등 인라인 문항 패턴 전면 지원.
      - **기존 데이터 및 채점 무결성 보존**: Firestore 답안 구조 및 채점 로직을 일절 건드리지 않고 프론트엔드 렌더링 파서만 고도화하여 100% 하위 호환성 유지.
    - **검증**: CBT round 6 단위 테스트 추가, 91개 전체 단위 테스트 100% 통과.

### 3.3 수행평가 문항 버전 4 전환 — P0 운영 배포 및 V4 운영 적용 완료

2026-09-15 사용자 요청에 따라 V3의 핵심 개념과 난이도 설계를 폐기하지 않고, **문항 완성도와 평가 무결성을 함께 높이는 V4**로 전환해 현재 운영 중이다. V3은 이미 시작했거나 끝난 회차의 답안·성적·일반 연습용 참고 자료로 보존하며, V4를 적용해도 과거 회차를 새 기준으로 재채점하지 않는다.

* V4는 문제 인식 및 분석, 추상화, 복잡한 문제를 작은 문제로 나누는 문제 분해, 해결 절차의 순서화, 알고리즘의 조건, 순서도 4대 기호, 순차·선택·반복, 경계값 판단과 실행 추적이라는 교육 목표를 계승한다. 단순 용어 암기는 줄이고, 학생이 지문 속 조건·실행 결과·기호 역할을 근거로 판단해야 하는 문항 비중을 높인다.
* P0에서는 V4 문항·정답·유사 정답·출제 의도를 서버 환경 변수로 분리하고, 학생에게는 정답을 뺀 배정 문항만 전달한다. 제출 뒤 담당 교사만 서버 채점 결과와 문항 검토 자료를 확인한다. 2026-09-15 Firestore 기본 DB의 V4 답안 규칙과 Vercel Production 코드를 실제 반영했으며, 이후 V4 문항·운영 환경 변수·회차 선택을 적용해 현재 운영 중이다. 실제 비공개 값은 이 문서에 기록하지 않는다.
* 모든 배정 조합은 30분·Part 1 30점·Part 2 30점·Part 3 40점의 현재 평가 구조와 동등한 난이도 구성을 유지한다. 같은 학습 목표를 여러 방식으로 묻더라도 한 학생에게 사실상 같은 답을 반복 요구하지 않는다.
* 문항은 정답 하나가 지문에서 분명히 도출되는지, 보기가 같은 범주·문장 길이·그럴듯함을 갖추는지, 불필요한 배경지식·긴 독해·함정 표현으로 성취를 왜곡하지 않는지를 교사 검토와 자동 점검으로 확인한다.
* 선생님은 교사 전용 검토 자료와 교사 화면에서 V4 전체 문항·정답·출제 의도를 확인·수정할 수 있어야 한다. V4의 정답, 학생별 문항 배정 비밀값, 자동 채점 기준은 학생 배포 JavaScript·공개 Git 저장소·학생 Firestore 문서에 포함하지 않는다. 학생에게는 본인에게 배정된 문제와 제출에 필요한 안내만 전달한다. 구체적인 비공개 저장 방식은 운영 부담과 시험 전 문항 공개 범위를 확인한 뒤 구현한다.
* V4 운영을 계속 확대하거나 새 회차로 전환하기 전에는 새 문항의 교육과정 대응표, 정답·유사 정답 검토표, 배정 조합별 난이도·중복 검사, 학생/교사 대표 흐름, 직접 Firestore 변경 시도 거부 검사를 남긴다. 이는 운영 중인 V4의 후속 수용 기준이며, 현재 교실 전체 검증 완료를 뜻하지 않는다. 자세한 기준은 [V4 준비 계획](docs/EVALUATION_V4_PLAN.md)을 따른다.
* **2026-09-18 교사 상세 조회 보완:** `js/core/classroom.js`의 V4 성적·문항 검토 요청이 화면 표시명(`2학년 1반`)이 아닌 API 학급 ID(`2-1`)를 전달하도록 수정했다. 관련 권한·평가 API 14개 테스트와 운영 화면의 기존 제출 답안 조회를 확인했으며, 서버 점수와 Part 1·2 검토 자료가 표시되었다. 이 수정은 기존 답안·Firestore 자료를 변경하지 않는다.
* **2026-09-18 V4 객관식 답안 표시 보완:** 교사 상세 화면에서 서버의 0-based 객관식 답안 인덱스를 1-based 선지 번호와 괄호 안 선지 문구로 표시하고, 모의평가와 같은 `학생 선택`·정답/오답 배지 위계를 적용했다. 저장 형식·채점·기존 답안은 변경하지 않았다.
* **2026-09-18 V4 학생 점수 누르는 동안 표시 보완:** 제출한 학생 본인만 서버의 Part 1·2 객관·단답 자동채점 소계(60점)를 조회할 수 있도록 `student-score` 요청을 추가했다. 학생 결과 화면에서는 `점수 보기`를 누르는 동안에만 참고 점수와 Part 1·2 소계를 표시하고, 손을 떼면 점수 문구를 지운다. API는 정답·문항·AI 근거·다른 학생 자료를 반환하지 않으며, Part 3 AI 제안은 교사 확정 전 학생의 최종 점수로 표시하지 않는다. 기존 답안·배점·채점 기준은 변경하지 않았다.
* **2026-09-18 V4 학생 점수 확인 UI 및 마스킹 인터랙션 고도화:** 제출 완료 화면에서 '점수 보기' 텍스트의 과도한 중복과 하단 패널에 의한 레이아웃 덜컹거림을 개선했다. 기본 상태에서는 대형 점수판과 Part 1·2 세부 카드에 `🔒 • • / 60점`, `🔒 •• / 30점`으로 마스킹 처리되어 보호되고, `[👁️ 누르고 있는 동안 점수 확인]` 버튼을 꾹 누르고 있을 때 해당 카드에서 직접 점수가 드러나며 손을 떼면 즉시 다시 마스킹된다. 불필요하고 긴 안내문구를 정리하고 하단 중복 텍스트 박스를 스크린리더용(`sr-only`)으로 전환하여 카드 높이와 시각적 위계를 정갈하게 정돈했다.
* **2026-09-21 성적표 멀티 시트 엑셀(`.xlsx`) 다운로드 및 학생 답안/정답 확인 모달 구축, 관제탑 실시간 점수 연동, 결시생 추가 응시 복원:**
  1. **멀티 시트 엑셀(`.xlsx`) 생성기 (`js/core/excel-export.js`)**:
     - 외부 라이브러리(SheetJS 등) 없는 순수 Vanilla JS 기반 OpenXML ZIP(`.xlsx`, PK0304 Store 방식) 모듈 구축.
     - **Sheet 1 [학급종합]**: 전체 학급 학생 성적 종합표. 학급명 `2-6`이 엑셀에서 날짜(`02월 06일`)로 자동 변환되는 문제를 인라인 문자열(`<c t="inlineStr"><is><t>...</t></is></c>`)로 원천 차단. AI 초벌채점 용어를 **`1차 채점`**으로 일원화.
     - **Sheet 2 ~ N [개별 학생 성적표]**: 학생수만큼 생성되는 `01_강수아`, `02_고서누` 등 개별 시트.
       - **지필평가(Part 1 & 2) 좌우 2단 배치**: 좌측(A~C열) Part 1 객관식(Q1~Q10, 내 선택 vs 정답, ⭕/❌ 3점), 우측(D~F열) Part 2 단답형(Q11~Q16, 내 입력 vs 정답, ⭕/❌ 5점) 및 지필평가 배점/채점 기준 안내 카드(D12:F15 병합) 배치.
       - **Part 3 알고리즘/순서도 서식 및 1차 채점 고도화**:
         - 계획 요약: 현재 상태, 목표 상태, 지켜야 할 조건을 3개 영역으로 명확히 구조화 분리.
         - 교사 확정 전이라도 AI 1차 채점 점수가 소계 및 총점에 온전히 합산되어 0점으로 오인되지 않도록 보장 (`resolvePart3ProposalScore`).
         - Part 3 공식 4대 루브릭(기준 1: 문제/조건 명확성 10점, 기준 2: 자연어 논리성 10점, 기준 3: 자연어-순서도 일치 10점, 기준 4: 순서도 구조/동작 10점) 평가 기준 및 학생 답안/AI 피드백 1:1 매핑.
         - D~F열 병합 너비 확장(11컬럼 체계) 및 동적 행 높이(`part3RowHeight`, 최소 38pt 이상) 적용으로 내용 잘림 방지.
         - Firestore review shadowing 해소: 클라이언트 review 객체와 서버 `class-grades` 응답을 안전하게 병합하여 1차 채점 누락 방지.
       - **A4 1장 무수정 맞춤 인쇄 보장**: 총 행 수를 정확히 23행으로 압축하고 `<pageSetUpPr fitToPage="1"/>` 및 `fitToWidth="1" fitToHeight="1"` 인쇄 설정을 적용하여 무수정 원클릭 인쇄 시 A4 1장에 완벽히 수용. 셀 너비 및 라벨 셀 `wrapText="1"` 확충으로 텍스트 잘림 현상 원천 해소.
     - **마지막 Sheet [나이스(NEIS) 양식]**: 지필소계/60, 순서도(1차)/40 등 교육행정정보시스템 성적 입력 시스템에 드래그하여 복사/붙여넣기할 수 있는 전용 시트.
     - **관제탑 단일화**: 기존 분리되었던 성적표 다운로드 버튼을 **`[📊 성적표 엑셀 다운로드]` 단일 버튼**으로 통합.
  2. **교사 관제탑 좌석 바둑판 실시간 서버 성적 연동 (`js/core/classroom.js`)**:
     - `currentLiveClassGrades` 맵을 도입하여 `class-grades` 서버 배치 채점 API 결과를 좌석 카드에 실시간 매핑.
     - 좌석 카드 점수와 엑셀 다운로드에 서버 채점 결과를 사용하도록 연결. 단, 최초 구현에는 클라이언트 캐시의 평가 회차 구분이 없어 회차 전환 시 이전 점수가 잠시 표시될 수 있었으며, 2026-09-23 수정 내역에서 보완.
  3. **학생용 문항별 답안/정답 확인 모달 (`student-eval-review-modal`)**:
     - 학생 제출 완료 화면에서 `[📋 문항별 내 답안과 정답 확인]` 버튼 제공.
     - Part 1 객관식(내 선택 vs 정답, O/X, 배점, 해설), Part 2 단답형(내 입력 vs 인정정답, O/X, 배점, 해설), Part 3 순서도(나의 계획 + 1차 채점 4대 기준별 점수 및 피드백) 상세 제공.
     - **교실 치팅 방지 보호**: 시험 진행 중(`in_progress`)에는 상세 정답이 공개되지 않고 안내문과 참고 점수만 표시되며, 교사가 시험을 종료(`ended`)한 후에 전체 정답과 해설이 열람됨.
  4. **학생 화면 Part 3 1차 채점 실시간 반영**:
     - 교사 관제탑의 백그라운드 큐가 순서도 AI 분석을 완료하면, 학생 제출 화면에 `Part 3 순서도 설계 1차 채점` 카드(`X점 / 40점`) 및 `총 참고 점수`(`X점 / 100점`)가 실시간 폴링을 통해 즉시 업데이트됨.
     - 확정 전 1차 채점 점수임을 명확히 알리는 안내 뱃지 및 문구 제공.
  5. **결시생 개별 30분 추가 응시 복원 및 기기 무관 접속 보장 (`lab-eval.js`, `eval-service.js`, `classroom.js`)**:
     - 로비의 조기 차단을 제거하고 `joinWaitingRoom` 트랜잭션을 통한 `makeupAllowed` 권한 판정으로 일원화.
     - 시험 종료 시 0점으로 자동 마감(`status: 'submitted'`)된 결시생이라도 좌석 모달에서 `[결시생 개별 30분 추가 응시 허용]` 시 안전하게 초기화(`forceReset: true`)하고 30분 권한 부여.
     - 추가 응시 승인 시 `ownerUid: null`로 초기화하여 교실 PC, 도서관 컴퓨터, 노트북 등 어느 기기에서 접속하더라도 "다른 기록에 연결됨" 오류 없이 즉시 바인딩 로그인 허용.
  6. **시험 종료 및 시간 만료 시 방 입장 원천 차단 및 좌석 보호 (`eval-service.js`, `firestore.rules`)**:
     - 마감 시간 경과 및 세션 종료 시 비인가 진입 및 좌석 덮어쓰기를 DB/클라이언트 다층 방어로 차단.
  7. **검증**: 전체 단위 테스트 117개 100% 통과, `tools/check.cjs` 71개 스크립트 정적 검사 통과.
* **2026-09-18 V4 문제은행 자연어화·표현 정돈 및 교사 전용 백업 기능 구축:** V4 80문항 중 `A = A - B`, `상자수 = 상자수 + 1`, `합계 = 합계 + 숫자`, `사탕 = 사탕 - 1` 등 텍스트 코딩식 대입 연산자를 중2 학생 눈높이에 맞춘 직관적인 자연어/블록 표현(`남은 사탕에서 5개 빼기`, `상자수 1 증가, 남은 귤 3개 감소`, `[합계]에 [숫자]를 더한다`, `[숫자]를 1 증가시킨다`)으로 전면 개정했다. 또한 오해를 유발하던 `[나이](숫자만)를 쓰시오.` 식의 괄호 표현을 `나이를 숫자만 쓰시오.` 등 자연스러운 한국어 문장으로 일원화했다. 정답·채점 로직·학생 답안 무결성을 100% 유지하며, 80문항 JSON(73.8KB)을 gzip-base64(24.7KB)로 압축하여 Vercel 환경 변수 한도(64KB) 내 안전하게 운영 서버(`EVAL_BANK_V4_JSON`)에 주입했다. 아울러 로그인한 교사가 언제든 최신 V4 80문항 JSON을 안전하게 다운로드할 수 있도록 API 엔드포인트(`export-bank`)와 은은한 UI 링크(`[⬇ 백업]`)를 신설했다.
* **2026-09-22 컴퓨터실 시계 오차(Clock Skew) 방어 및 학생 관리 모달 2단 개편 (구현 및 검증 완료):**
   1. **컴퓨터실 시계 오차(Clock Skew) 원천 방어 (`api/evaluation.js`, `ai-service.js`, `eval-service.js`, `lab-eval.js`)**:
      - **원인 분석**: 특정 교실 PC의 윈도우 로컬 시계가 15분 빠르게 설정되어 있어 학생 타이머가 15분 적게 뜨고 조기 자동 제출되던 문제 원천 방어.
      - **해결**: `api/evaluation`에 경량 `ping` 엔드포인트를 추가하고 모든 API 응답 및 HTTP Header(`Date`)를 통해 `serverTimeOffset`을 자동 계산. 클라이언트는 `getNow() = Date.now() + serverTimeOffset`을 사용하여 PC 로컬 시계 오차와 무관하게 서버 표준 시간 기준으로 동기화된 타이머 및 마감 판정 수행.
      - **Sanity Guard**: 시험 시작 직후 로컬 시계 왜곡으로 조기 자동 마감되는 현상을 2중 방어.
   2. **선생님 스케치 반영: 학생 관리 모달 좌우 2단 분할 개편 (`index.html`, `classroom.js`)**:
      - 모달 하단에 세로로 길게 나열되던 조치 버튼 목록을 전면 폐지하고, 데스크탑 `max-w-6xl` 크기의 **좌우 2단 레이아웃(좌측: 학생 상태 요약 & 응시/비상 조치 액션 패널, 우측: 학생 답안 검토 & 채점)**으로 전면 개편.
      - 좌측 패널: 미니 프로필 카드(좌석 번호, 상태 뱃지, 학생 이름, 시간 정보) + 상태별 스마트 액션 그룹.
      - **제출 완료(`submitted`)**: `[🔄 제출 취소 및 복귀 (+10분)]` (추천 메인), `[제출 취소 및 복귀 (+5분)]`.
      - **풀이 중(`in_progress`)**: `[시간 +10분]`, `[시간 +5분]`, `[풀던 답안 유지 재접속 허용]`, `[현재 답안으로 정상 제출 마감]`.
      - **결시/미응시**: `[개별 30분 추가 응시 허용]`.
      - **주의 조치(하단 분리)**: `[답안 초기화 (완전 백지화)]`, `[좌석 비우기 (퇴장 처리)]`.
   3. **제출 취소 및 풀던 답안 100% 보존 복귀 (`reopenStudentExam`)**:
      - 조기 제출된 학생의 답안(Part 1 객관식, Part 2 단답형, Part 3 순서도)을 100% 안전하게 보존하며 시험장으로 복귀(`status: 'in_progress'`).
      - 추가 시간(`${addedMinutes}분`)을 즉시 부여하고, `makeupAllowed: true`, `allowReconnect: true`를 세팅하여 새 PC/브라우저에서도 재접속 허용.
      - `archives/` 서브컬렉션에 `{ kind: 'student-reopen', ... }` 감사 기록을 남겨 데이터 유실 방지.
   4. **학급 전체 및 개별 시간 연장 (`extendClassSessionTime`, `extendStudentTime`)**:
      - 관제탑 상단 툴바에 `[⏱️ 전체 5분 연장]` 버튼 배치 (진행 중일 때 노출).
      - 학생 모달에서 개별 학생에게 원클릭 `+5분 / +10분` 시간 부여.
      - 학생 브라우저 실시간 리스너(`listenSession`, `listenStudent`)가 시간 연장을 감지하여 타이머를 동적으로 갱신.
   5. **검증**: `tests/eval-time-extension.test.cjs` 포함 전체 단위 테스트 122개 100% 통과, `tools/check.cjs` 72개 스크립트 정적 검사 통과.

* **2026-09-23 관제탑 실시간 점수의 평가 회차 격리 (구현·검증·운영 반영 완료):**
  - V4 서버 점수 캐시를 `attemptId`에 묶고 평가 회차·유형 변경 시 비우며, 현재 세션·학생·캐시의 회차 ID가 모두 같을 때만 좌석 카드에 사용.
  - 배치 채점과 학생 상세 모달의 늦은 응답은 요청 당시 회차 및 관제실 세대(generation)와 대조해 폐기하고, 회차별 요청을 분리.
  - 엑셀 내보내기는 시작 시 학생 목록을 고정하고 서버 응답 회차가 바뀌면 혼합된 성적표를 만들지 않도록 중단.
  - `tests/classroom-score-grid.test.cjs` 10개와 전체 단위 테스트 127개, `tools/check.cjs` 72개 스크립트 정적 검사가 통과. 로컬 Edge 브라우저에서 V4와 V3의 오래된 캐시 상황을 확인했고 페이지 예외·콘솔 오류는 0건.
  - PR #5가 main 병합 SHA `0c0c76e`로 통합됐고 Vercel Production 배포 성공 및 공개 운영 주소에서 수정 JS 일치를 확인. 자세한 근거와 브라우저 검증 제한은 [배포 기록](docs/DEPLOYMENT.md)에 기록.
  - 답안·확정 성적·배점·채점 기준·Firestore·보안 규칙은 변경하지 않음.

### 3.3.1 V4 단답형 동등 표현 인정과 종료 회차 정정 — 구현·검증 완료 / 운영 적용 전

2026-09-24 교사 승인: 종료된 V4 전체(현재·보관 회차)를 비교한다. 알고리즘의 공식 명칭 원칙과 기존 허용답을 유지하고, 기호 명칭의 명시된 예외·수업 용어·맥락상 같은 표현을 문항별로 추가한다. 실제 문항과 인정 목록은 비공개로 관리한다.

* **은행:** 운영 교사 내보내기 원본의 단답형을 모두 검토하고 교사 피드백에 따라 허용 표현을 추가·제외했다. 기존 허용답과 문항 본문·ID·보기·배점·난이도·배정은 유지한다. 문항당 답안 상한만 12→32로 늘리고 공백 제거·영문 소문자화 후 전체 일치 판정은 유지한다. 실제 인정 목록과 규모는 비공개 자료에서 확인한다.
* **영향 비교:** 종료된 V4의 현재·보관 회차를 읽기 전용으로 비교했다. 학생별 결과, 영향 규모, 정정 예정 점수와 최신 계획 식별자는 Git 제외 비공개 검토 자료에서만 관리한다. 아직 운영 정정은 하지 않았다.
* **정정 도구:** 검토 계획 해시·종료 회차·답안의 최신 상태를 확인하고, 학급별 트랜잭션에서 원본 학생 스냅샷과 변경 사유를 보관한다. 현재 학생의 점수/정정 메타데이터만 갱신하며 원본 답안과 Part 3는 유지한다. 중복 적용·답안 변경·점수 하락·기존 확정 총점 자동 변경을 막고 별도 감사 기록을 남기는 복구 경로를 제공한다. 기존 보관 원본·접근 규칙은 변경하지 않는다.
* **화면:** 정정된 학생 자료가 향후 새 회차 시작으로 보관되면 단답형 변경 전후를 표시한다. 회차·제출 시각·원본 객관식/단답형이 일치할 때만 표시한다. 현재 교사/학생 조회와 성적표는 기존 API의 현행 은행 계산을 사용한다.
* **검증:** 전체 단위 테스트 137개, 정적 검사, 격리된 로컬 Edge 보관 화면 검사 통과. 비공개 인정답·거부 표현의 판정과 합성 배정 1,000회 불변을 확인했다. 운영 API·직접 DB의 담당 교사 허용/담당 밖 거부/비로그인 거부를 읽기 전용으로 확인했다. 실제 학생 요청과 운영 정정 후 점수 일치는 미검증이다.
* **남은 작업:** 운영 병합·배포 승인 후 최신 비공개 검토본으로 은행 교체와 대상 DB 정정을 함께 적용하고 재조회한다. 현재 API는 현행 은행으로 재계산하므로 배포만으로 점수가 바뀌는 점을 고려해 순서를 맞춘다. 보관 변경 대상이 생기면 별도 정정 표시·기준 복원을 검토해야 하며 현재 도구는 이를 거부한다. 세부 절차·복구·검증 제한은 [운영 정정 절차](docs/V4_OBJECTIVE_CORRECTION.md)를 따른다.

### 3.4 V4 보안·보관·유지보수 방향 — 운영 적용 후 보완 방향

2026-09-18 현재 V4 실전평가는 운영에 적용되어 있다. 아래 내용은 V4 활성화 자체가 아니라, 운영 중인 V4의 회차 재현·보관·검증을 보완하기 위한 방향이다. 상세 근거·비교 기준·수용 조건은 [V4 계획 8~11절](docs/EVALUATION_V4_PLAN.md#8-다음-순서와-활성화-조건)에 통합한다.

* **최우선: 회차 재현.** 평가 당시 문항·보기 순서·배정·정답/허용 답·배점·채점 기준을 고정하고 수정본은 새 회차에 적용하는 방향이다. 자동 계산과 교사 확정 성적을 구분하며 조회·문항 업데이트만으로 확정 성적이 바뀌지 않아야 한다. 현재와 보관된 V4 회차의 조회·복구까지 완료 조건에 포함한다. 현재 P0의 재배정 API만으로 이를 구현 완료로 표시하지 않는다.
* **비공개 보관.** 비밀 설정과 문항 콘텐츠 관리의 분리를 권장한다. 교사 접근성·이력·회차 재현·백업/복구·비용·PC 간 접근으로 저장 후보를 비교한다. 파서의 문자열 길이 상한은 Vercel 배포 용량 보장이 아니다. 보관 서비스·기간·계정·전달 방식·비밀값 교체·성적 정정 절차는 별도 결정한다.
* **검증 분리.** API 권한과 Firestore 직접 접근을 각각 시험한다. 문법·모의 API·에뮬레이터 규칙·분리된 실제 환경·교실 검증을 구분한다. V3의 정답 노출 한계가 V4 기반 배포로 해결되었다고 표현하지 않는다.
* **점진적 유지보수.** 관련 기능 변경 시 화면·실행·통신·채점 역할을 분리하고 Vanilla JS를 유지한다. 전체 재작성·프레임워크 전환은 포함하지 않는다.
* **개발 순서.** 회차 재현·비공개 보관 설계부터 확인한다. V3 진단·V4 청사진은 계속하되 실제 비공개 자료 작성·교체 전 보호 위치를 합의하고, 다음 V4 회차 확대 전에는 문항·보관·권한·제출·복구 검증을 완료한다. 기존 V3을 재채점하거나 진행 중 V4를 임의로 V3으로 전환하지 않는다. 소규모 검증 뒤 교실 사용 범위를 확대한다.

위 내용은 현재 V4 운영 적용 사실과 별개로 남은 보완 방향·향후 완료 조건이며, 모든 운영 검증이 완료되었다는 뜻은 아니다. 운영 중단 시 복구 가능한 코드·은행·배정·채점 버전 조합도 검토한다.

* **교사용 보관 회차 답안 조회 — PR #7 main 병합 및 Vercel 체크 성공 (2026-09-23):**
  - **진입점과 탐색:** 교사용 클래스룸에 `실시간 수행평가 | 이전 평가 기록 | 단원별 과제 취합표` 탭을 추가했다. 학급 선택은 유지되며 조회를 관제 툴바의 평가 조작과 분리했다. 탭은 줄바꿈을 허용하고 방향키·Home·End 키로 이동한다.
  - **회차 목록:** `EvalService.listArchivedSessions()`가 현재 선택된 학급에서 `kind: new-session` 회차만 읽어 최신 평가일순으로 표시한다. 학생별 초기화·재접속 감사 기록은 제외한다. `questionVersion`과 평가 시작·준비·보관 시각을 표시하고, 같은 날짜에 같은 유형의 회차가 여러 개면 시각을 덧붙인다. 회차 문서 ID나 보관 내용은 수정하지 않는다. 학생 스냅샷은 회차를 연 뒤 요청한다.
  - **학생 답안:** `EvalService.getArchivedSessionStudents()`가 선택한 회차의 학생 스냅샷만 가져온다. 데스크톱은 학생 목록과 답안을 나란히 보여주고, 899px 이하에서는 목록과 상세를 한 화면씩 전환한다. 번호·이름 검색, 제출 상태, 저장 점수, 답안, 저장된 교사 확정 검토 내용을 표시한다. 답안 값·이름은 HTML로 안전하게 렌더링한다.
  - **읽기 전용과 기록 한계:** 조회 화면에는 읽기 전용 표시만 있고 성적 변경·재채점·초기화 동작이 없다. 저장된 점수와 확정 기록을 그대로 보여주며 새 채점은 하지 않는다. 점수가 없거나 채점 대기 중이면 저장 상태를 구분해 표시한다. V4 문항 본문이 스냅샷에 없으면 현재 문항을 대신 끼워 맞추지 않고 답안 원문이 보관되지 않았다고 안내한다. 회차 완전 재현·복구와는 별도다.
  - **빈 상태·오류·호환성:** 아직 회차가 없으면 다음 평가 준비 후 보관되는 시점과, 막 종료한 회차는 실시간 탭에서 볼 수 있음을 안내한다. 권한 오류·연결 실패·사라진 회차는 한국어 안내와 재시도 동작을 제공한다. 로컬 시연에서는 최초 빈 평가를 빈 회차로 보관하지 않으며, 학급 ID가 확인되는 구형 시연 스냅샷도 조회한다.
  - **권한·보존:** 교사 인증 후 선택 학급의 기존 기록만 조회한다. 기존 Firestore 규칙과 자료 구조·보존 기간을 바꾸지 않았고, 읽기 요청은 회차·학생 문서에 쓰지 않는다.
  - **검증:** `node tools/check.cjs` 73개 스크립트 통과, `node --test tests/*.test.cjs` 130개 통과. 로컬 시연 브라우저에서 검색·V4 원문 부재 안내·소계/확정 검토 표시·읽기 전용 무변경과 1440/1024/768/390px 레이아웃 및 탭 키보드 이동을 확인했다. 브라우저 검사에서는 외부 CDN을 대체 응답으로 처리했으므로 실제 CDN 스타일, 운영 로그인·Firestore 권한 규칙, 운영 학생 자료는 검증하지 않았다. PR #7의 GitHub 검증과 병합 커밋 Vercel 상태는 성공했다. 배포 근거와 확인 범위는 [배포 기록](docs/DEPLOYMENT.md)에 기록한다.

같은 날 선생님의 명시적 승인으로 AGENTS의 공통 원칙을 보완한다. 공개/비공개 자료의 관리 범위, 기존 답안·확정 성적 보존, 작은 수정에도 영향에 맞는 확인, API/직접 DB 검사 구분, 운영 기준/작업 브랜치 구분을 적용한다. 당시 문서와 AGENTS 개정의 검토 후 main 병합을 승인받았으며, V4 운영 적용 여부와 별개로 아래 보완 검증은 남아 있다.

### 3.5 모의 평가 → 실제 평가 운영 일정 — 2026-09-15 합의 / 2026-09-18 현재 상태 정정

**현재 상태 정정(2026-09-18, 선생님 확인):** V4도 운영 웹에 적용되어 실제 평가 운영에 사용 중입니다. 아래의 “V4는 다음 주 목표”, “V3는 현재 기본 운영 버전”은 2026-09-15 당시 계획 기록이며 현재 상태로 읽지 않습니다. V3는 모의평가 및 기존 회차 호환 경로로 유지합니다. V4의 교실 전체 동시 사용·회차 재현·보관 복구는 이 확인만으로 검증 완료 처리하지 않습니다.

* **이번 주(2026-09-14~09-20)는 V3로 모의 수행평가**, **다음 주(2026-09-21~09-27)는 V4로 실제 수행평가를 진행하는 목표**로 변경했다. 정확한 평가 날짜·대상 반·사전 점검 시간은 아직 전달받지 않았다.
* 이전의 “이번 주 V3와 다음 주 V4를 같은 실제 수행평가에 혼용”하는 가정은 이 합의로 대체한다. 모의 결과의 활용·보관·성적 반영을 임의로 정하지 않으며 기존 답안·기록을 삭제하거나 재채점하지 않는다.
* 다음 주는 목표 일정이며 V4가 준비되었다는 뜻은 아니다. 실제 문항·비공개 보관·회차 재현·권한·제출·교사 확인을 검증한 뒤 적용한다. 준비가 부족하면 상태와 대안을 설명하고 선생님과 일정을 다시 판단한다.
* 선생님은 Part 3 AI 채점 개선에도 관심을 밝혔으나 구체적인 불만·기대 기준·모델 변경 방향은 차후 답변하기로 했다. 지금 채점 기준·모델·실습 도움·배점·시간을 변경하는 승인이 아니다.
* V4 실전평가가 현재 운영에 적용되어 있으며, V3는 모의평가·기존 회차 호환용으로 유지한다. 과거 배정·저장·제출 오류의 수정·배포 기록이 있다. 이번 토의에서 실제 교실 평가를 다시 검증한 것은 아니다. V3 모의와 V4 운영에서는 입장·문항 표시·새로고침·저장·제출·교사 확인과 학생의 어려움을 관찰한다.
* V4 문항 전환과 AI 채점 개선은 별도 작업으로 구분한다. 현재 AI 초벌 채점 API의 V3 회차 제한, 실습/채점의 모델 설정 공유, 지시문 버전 구분은 다음 구현 전 점검 항목이다. 자세한 역할 분담과 미결정 사항은 [V4 계획 12절](docs/EVALUATION_V4_PLAN.md#12-v3-모의--v4-실제-평가와-ai-채점-토의-기록)을 따른다.

### 3.6 실전평가(V4) 80문항 은행 구축 및 모의평가(V3) AI 채점 토큰 절약 최적화 — 2026-09-17 구현 및 검증 완료

선생님의 1, 2단계 실행 승인 및 핵심 지침(모의평가 AI 채점 비활성화로 토큰 낭비 방지, 실전평가 80문항 이원목적분류표 도메인 균형 배정, 프론트엔드 한글 표준 용어 일원화)에 따라 다음 작업을 완료하였습니다.

1. **실전평가 80문항 비공개 문제은행 구축 (`scratch/eval_bank_v4.json`)**:
   - **Part 1 (객관식 50문항)**: D1~D5 5개 대영역별 각 10문항 (하 4문항, 중 4문항, 상 2문항).
   - **Part 2 (단답형 30문항)**: S1~S6 6개 세부 영역별 각 5문항 (하 1~2문항, 중 2~3문항, 상 1~2문항).
   - **순수 컴퓨팅 사고력 중심**: 단순 국어 문맥 맞히기 배제, 공식 기호 명칭(판단, 처리, 자료, 단말) 엄수(도형명 오답 처리), 미학습 용어(모델, 무한 루프, 증가, 0으로 나누기) 배제, 74번 누적합 루프 난이도 캘리브레이션(`1+2+3=6`).
   - Git 제외 규칙으로 실수로 추적되는 것을 줄이고 Vercel 환경 변수(`EVAL_BANK_V4_JSON`)로 제공합니다. 제외 규칙만으로 유출 방지·영구 보관을 보장하지 않습니다.
2. **도메인 균형 및 난이도 보장 배정 엔진 (`server/evaluation-bank.cjs`)**:
   - **Part 1 (10문항 배정)**: D1~D5 5개 대영역에서 무조건 **2문항씩 균등 추출**. 난이도 구성: **하 4문항(12점) + 중 4문항(12점) + 상 2문항(6점) = 총 30점**.
   - **Part 2 (6문항 배정)**: S1~S6 6개 세부 영역에서 무조건 **1문항씩 균등 추출**. 난이도 구성: **하 2문항(10점) + 중 2문항(10점) + 상 2문항(10점) = 총 30점**.
   - 학생별 지필 총점 60점과 난이도·영역별 문항 수를 맞춥니다. 구성 일치와 학생이 체감하는 난이도의 동등성은 별개입니다.
   - 기존 도메인 없는 픽스처와의 100% 하위 호환성 유지.
3. **AI 토큰 절약 최적화: 모의평가(V3) 비활성화 & 실전평가(V4) 전용 활성화**:
   - `api/assessment.js`: `session.questionVersion !== 4`인 경우 AI 초벌 채점 거부(409 반환). 모의평가(3)에서 불필요하게 유료 API 토큰이 소모되던 현상을 원천 차단.
   - `js/core/assessment-auto-review.js`: `this.questionVersion !== 4`일 때 백그라운드 자동 AI 채점 큐 동작 중단.
   - `js/core/assessment-review-ui.js`: 모의평가(3) 회차는 교사 직접 평가 안내 표시, 실전평가(4) 회차에만 Solar AI 채점 버튼 노출.
4. **사용자 친화적 한글 용어 표준화**:
   - V3/V4 개발 약칭 대신 교사 및 학생 화면에 **'모의평가 (자유 설계)'** 및 **'실전평가 (비공개 문제은행)'** 표준 한글 용어 일원화.
   - 교사 관제탑 헤더에 회차 종류 실시간 뱃지(`teacher-session-version`) 제공.
5. **검증 결과**:
   - 80개 전체 유닛 테스트 100% 통과 (`tests/*.test.cjs`).
   - 1,000회 학생 배정 시뮬레이션 전수 통과 (모든 학생 동일 난이도, 영역별 정확한 배분 확인).

### 3.7 교사 관제탑 평가 유형(모의평가 / 실전평가) 선택 드롭다운 UI — 2026-09-17 구현 및 검증 완료

선생님과의 UI/UX 협의에 따라 거창한 모달 팝업 대신 조작이 빠르고 교단 화면에 군더더기 없이 어우러지는 **헤더 드롭다운(셀렉트 박스)** 방식을 적용하였습니다.

1. **평가 유형 선택 드롭다운 (`#teacher-session-version-select`)**:
   - 위치: 교사 관제탑 헤더의 `[평가 준비]` 버튼 바로 왼쪽에 일체감 있게 배치.
   - 옵션: `📗 실전평가 (80문항 / AI 채점)` (기본값) / `📘 모의평가 (실습 / AI 끔)`.
   - 스타일: 어두운 헤더 톤앤매너에 맞춘 다크 슬레이트 배경, 포커스 링, 높이(42px) 및 패딩 정렬.
2. **상태 기반 자동 잠금 및 동기화**:
   - `준비 전` / `평가 종료` 상태: 자유롭게 유형 선택 가능.
   - `입장 대기` / `평가 중` 상태: 시험 도중 모드 오인 변경을 원천 차단하기 위해 **자동 비활성화(disabled)** 및 툴팁 안내.
3. **스마트 확인창(Confirm) 및 맞춤 피드백**:
   - `[새 평가 준비]` 클릭 시, 선생님이 선택하신 모드를 명확히 짚어주는 맞춤형 확인창 표출 (실전평가: 80문항 비공개 배정 및 AI 채점 ON 안내 / 모의평가: 자유 실습 및 AI 토큰 절약 안내).
   - 세션 생성 후 피드백 문구 및 상단 상태 뱃지(`teacher-session-version`)와 100% 실시간 연동.
   - `tests/rounds.test.cjs`: `prepareSession`의 questionVersion 3/4 수신 및 기본값 4 검증 테스트 추가.
   - 81개 전체 단위 테스트 100% 통과 (`node --test tests/*.test.cjs`).

### 3.8 교실 비상 대책 및 재응시·결시생 관리 방침 — 합의 및 코드 구현, 운영 검증 별도

선생님과의 `/grill-me` 인터뷰를 통해 실제 컴퓨터실의 순간복구(롤백) 환경 및 결시생 발생 시나리오에 대비한 2대 핵심 재응시 방침을 확정하였습니다.

1. **컴퓨터실 PC 순간복구(롤백) 대응: 2대 재응시 버튼 분리**:
   - **문제 배경**: 학생 컴퓨터가 꺼진 후 재부팅되면 로컬 스토리지와 익명 로그인 세션이 롤백되어 새 UID가 발급되므로, `'이 번호는 다른 응시 기록에 연결되어 있습니다'` 에러가 발생함.
   - **해결책**: 교사 관제탑에서 학생 좌석 클릭 시 조치 버튼을 명확히 2개로 분리 제공:
     - ① `[🔄 풀던 답안 유지하며 재접속 허용]`: 서버에 저장된 기존 답안·진행 상태를 유지하며 새 접속 UID를 허용하는 경로입니다. 아직 전송되지 않은 로컬 변경은 포함하지 않습니다.
     - ② `[🆕 답안 초기화 후 처음부터 다시 풀기]`: 답안을 완전히 백지화하고 1번 문항부터 새롭게 시작.
2. **결시생(지각/결석) 개별 30분 추가 응시 지원 (2026-09-21 고도화 완료)**:
   - **문제 배경**: 정규 수업이 종료(`ended`)된 후 결시생 1명을 위해 반 전체를 '새 평가 준비'하면 기존 26명의 제출 답안이 아카이브로 넘어가 관리가 복잡해짐. 또한 시험 종료 시 0점 자동 마감(`status: 'submitted'`)된 결시생이 재응시할 방법이 막히거나, PC가 바뀌었을 때 UID 불일치 오류가 발생함.
   - **해결책**:
     - 정규 평가가 끝난 상태에서, 교사가 미응시 빈 좌석 또는 이미 0점 마감된 결시생 카드를 클릭하여 `[⏱️ 결시생 개별 30분 추가 응시 허용]`을 활성화.
     - `forceReset: true` 옵션을 통해 기존 0점 마감 기록을 안전하게 초기화하고 `makeupAllowed: true`, `makeupDurationMinutes: 30` 부여.
     - **임의 기기(어느 PC든) 접속 보장**: 승인 시 `ownerUid: null`로 초기화하여 결시생이 교실 PC, 도서관 컴퓨터, 교단 노트북 등 어느 기기에서 접속하더라도 "다른 기록에 연결됨" 오류 없이 즉시 바인딩 로그인 허용.
     - 학생이 대기실에 입장하여 `[개별 평가 시작하기 (30분)]`을 누르는 순간 학생 개인의 30분 타이머가 독립적으로 가동.
     - 다른 학생의 제출 답안은 해당 처리의 쓰기 대상에서 제외하며, 추가 응시 완료 시 27명 통합 성적표로 합산 산출.
   - **통합 성적 관리**: 추가 응시 학생의 답안 제출 및 채점 결과는 해당 학급의 단일 27명 나이스(NEIS) 엑셀/CSV 명렬표에 그대로 병합되어 교사의 행정 부담을 최소화합니다.

### 3.9 실전평가(V4) 학생 입장 보안 규칙 충족 및 로비 선제적 인증 — 2026-09-17 수정 및 검증 완료

1. **실전평가(V4) 대기실 입장 거부 원인 및 해결**:
   - **문제 원인**: `firestore.rules`에서 V4는 클라이언트가 답안에 문항 배정 정보(`answers.assignedQuestions`)를 저장하는 것을 엄격히 금지함 (서버 단독 추출 원칙). 그러나 클라이언트 `js/core/eval-service.js`에서 `[3, 4].includes(...)` 조건으로 인해 V4 세션에서도 `assignedQuestions` 필드를 student 문서의 `answers` 내부에 저장 시도하여 `permission-denied` (권한 오류)가 발생했음.
   - **해결책**: 클라이언트 문항 배정 및 backfill 로직을 모의평가(`sessionData.questionVersion === 3`) 전용으로 엄격히 제한. 실전평가(4)에서는 클라이언트가 `answers.assignedQuestions` 필드를 일절 생성·저장하지 않도록 하여 Firestore 보안 규칙을 100% 충족함.
2. **로비 학급 상태 배지 '상태 확인 불가' 회색 배지 방지**:
   - **문제 원인**: Firestore 보안 규칙 상 `classrooms/{classId}` 세션 문서는 익명 학생(`anonymousStudent()`) 이상의 권한이 있어야 `listenSession` 조회가 허용됨. 이전에는 대기실 입장 버튼을 누르기 전까지 익명 로그인이 호출되지 않아 로비에서 학급 선택 시 `permission-denied`가 발생하며 '상태 확인 불가'로 떨어짐.
   - **해결책**: `js/labs/lab-eval.js`의 `openLobby` 및 `checkSelectedClassStatus`에서 선제적으로 `authService.student()`를 호출하여 익명 인증 토큰을 확보한 뒤 Firestore 리스너를 연결하도록 개선. 로비 진입 즉시 '대기실 열림 (입장 가능)' 녹색 배지가 정상 표출됨.
3. **실전평가 문항 조회 API 학생 번호 2자리 패딩(`studentNum` 정규화)**:
   - **문제 원인**: 교사가 [평가 시작]을 눌러 학생이 `requestSecureEvaluationQuestions(classId, studentNum)`을 호출할 때, 학생 화면 내부 변수(`this.studentNum`)가 숫자 1(`1`)로 전달되어 `api/evaluation.js`의 `STUDENT_NUM` 정규식(`/^(?:0[1-9]|1[0-9]|2[0-7])$/`, 2자리 문자열 필수) 검사에서 400 Bad Request (`'요청 내용을 확인해 주세요.'`) 에러가 발생하며 시험 진입이 차단됨.
   - **해결책**:
     - 클라이언트 `js/core/ai-service.js`: `requestSecureEvaluationQuestions/Grade/Review`에서 `String(Number(studentNum)).padStart(2, '0')`으로 2자리 포맷팅 강제.
     - 서버 `api/evaluation.js`: `validTarget(body)`에서 `studentNum`이 숫자이거나 1자리 문자열이라도 1~27 범위 내 정수이면 자동으로 `'01'`~`'27'`로 정규화(Normalize)하도록 보강하여 완벽한 호환성 확보.
4. **단위 테스트 및 무결성 검증**:
   - `tests/rounds.test.cjs`: V4 환경에서 student 문서의 answers에 `assignedQuestions` 키가 절대 포함되지 않음을 검증하는 전용 테스트 추가.
   - `tests/evaluation-api.test.cjs`: 숫자 1 및 문자열 '1' 등 unpadded `studentNum`이 정상 정규화되어 문항을 수신함을 검증하는 단위 테스트 추가.
   - 총 85개 단위 테스트 100% 통과.

## 4. 기술·화면 방향

프런트엔드는 HTML/CSS/Vanilla JS를 유지합니다. 현재 Tailwind CDN·로컬 CSS·외부 SDK를 사용합니다. 기본 환경은 **Vercel 웹 배포 + Firebase 연동 + 서버를 통한 AI 호출**입니다. 학생에게 개발 도구 설치나 빌드를 요구하지 않습니다. 파일 더블 클릭이나 Netlify 폴더 업로드만으로 서버 기능까지 동작할 것을 요구하지 않습니다.

Node.js 서버리스 API와 개발용 자동 검증 도구를 허용합니다. 프레임워크 전환은 현재 범위에 포함하지 않습니다. 향후 구조 개선은 순서도 데이터·실행·검사, 화면 조작, 외부 통신, 모드별 정책을 분리하고 실습·평가의 공통 엔진 사용을 지향합니다. 구체적인 코딩은 구현 계획을 설명한 뒤 진행합니다.

순서도는 단말(보라), 자료(초록), 판단(주황), 처리(파랑)와 직각 연결선을 사용합니다. 학생이 블록과 연결을 직접 구성합니다. 백지 공방의 자연어 기획·기호 보관함·캔버스·실행 정보 영역을 유지하되 고정 열 수나 픽셀 정렬보다 주요 조작과 작업 영역의 가시성을 우선합니다. 최종 지원 화면 범위는 교실 환경 확인 후 확정합니다.

개념·퀴즈·학습 지도는 최대 1,200px로 중앙 정렬하여 넓은 화면에서 양쪽 여백을 둡니다. 실습·순서도 작성은 가용 폭을 사용합니다. 수행평가 Part 1·2는 읽기 폭을 제한합니다. Part 3은 `나만의 백지` 편집기를 공유하여 자연어 기획서·세로 기호 보관함·캔버스·접을 수 있는 실행 패널을 제공합니다. 좁은 화면에서는 세로로 배치하며 문제 안내도 접을 수 있습니다. 실행·한 단계 조작과 최근 안내는 실행 패널이 접혀 있어도 접근할 수 있습니다.

### 4.1 상단 탐색과 공통 UI 정비 — 구현, 검증 기록 별도

2026-09-12 사용자가 문서 지침의 구현과 배포를 승인했습니다. 변경 범위와 검증 근거는 [UI 개편 검토서](docs/UI_REVIEW.md)를 확인합니다.

* 교사용 클래스룸 진입점을 학생 코스 메뉴에서 분리하여 우측 상단 관리 영역으로 옮깁니다. 아이콘과 짧은 `교사용` 이름을 함께 제공하고, 홈 화면의 중복 진입점은 정리하여 일관된 경로를 유지합니다.
* 전체 메뉴는 학습 단계별 바로가기 기능이 있어 유지하며 교사용 버튼 옆에 배치합니다. 호버 자동 열기·닫기를 제거하고 버튼 클릭, Enter, Escape, 외부 클릭과 초점 이동으로 제어합니다. 펼침 상태와 현재 메뉴를 접근성 속성으로 표시합니다.
* 상단은 최대 1,600px의 공통 폭을 사용합니다. 창 폭 1,350px 이하에서는 학습 메뉴를 둘째 줄에 배치합니다. 중복 홈 메뉴는 제거하고 이름이 있는 로고 버튼으로 돌아갑니다. 430px 이하에서는 네 학습 메뉴를 두 열로 배치합니다. 교사용·전체 메뉴의 이름은 작은 화면에서도 유지합니다.
* 실제 헤더 높이를 관찰하여 실습 캔버스의 남은 높이를 계산합니다. 순서도 단계 메뉴도 줄바꿈을 허용하고 작업 영역이 버튼을 덮지 않게 합니다. 평가 중 이동 잠금·남은 시간·제출 동작은 유지합니다.
* 홈의 큰 홍보 배너와 중복 소개를 줄이고 세 학습의 개념·퀴즈·실습 진입점을 제공합니다. 기존에 연결 대상이 없던 심화 탐구 진입점은 제거합니다. 수행평가는 별도 안내 행에서 입장합니다.
* 공통 시각 기준은 본문 16px, 읽기 최대 폭 1,200px, 기본 모서리 12px, 중립색 배경과 제한된 파란 강조색입니다. `css/studio-ui.css`의 CSS 변수로 관리하며 외부 폰트 다운로드 대신 시스템 글꼴을 기본 대안으로 사용합니다. 개념 설명 배너의 그라데이션·그림자를 줄이고 교육용 기호와 색은 보존합니다.
* 캔버스 조작 안내는 기호를 덮지 않게 작업 영역 밖에 표시합니다. AI 도움 버튼은 전체 이름이 보이도록 배치하고 감소된 움직임 설정을 지원합니다. 교사·평가 도구 모음은 좁은 창에서 줄바꿈합니다.

### 4.2 순서도 작업 공간 보완 — 운영 배포 완료

2026-09-12 사용자 승인에 따라 `codex/flowchart-workspace-polish`에서 구현했습니다. 기준 버전은 `df293d8`이며 검증 범위와 화면은 [작업 공간 검토서](docs/WORKSPACE_UI_REVIEW.md)에 기록합니다. 2026-09-13 사용자 승인으로 아래 4.3·4.4와 함께 main에 병합하고 운영 배포했습니다. [배포 기록](docs/DEPLOYMENT.md)을 확인합니다.

* 세 학습 코스의 작은 아이콘과 선택된 탐색 버튼에 제한적인 색상 구분을 적용합니다. 수행평가는 간격과 테두리로 분리하며, 로고의 홈 이동 및 평가 중 이동 잠금을 유지합니다.
* 결과 화면은 큰 점수와 작은 `교사 검토 전` 표시를 분리합니다. `자동 채점 참고 점수`로 안내하며 배점과 채점 로직은 변경하지 않습니다. V4는 학생이 `점수 보기`를 누르는 동안에만 Part 1·2 참고 점수를 표시하고, 휴대전화 폭에서는 세 부분 점수를 세로 행으로 표시합니다.
* 순서도 다섯 단계의 공통 상단 여백은 20px, 탐색 영역 최소 높이는 58px, 본문 간격은 12px입니다. 같은 창 폭에서는 단계 전환 전후 기준선이 유지되며, 좁은 창에서는 단계 버튼이 줄바꿈합니다.
* 세로 기호 보관함과 직접 드래그·더블클릭 추가 경험을 유지합니다. 백지 공방의 기획서 폭은 300~320px, 기호 보관함은 100px입니다. 짧은 제목·버튼은 끊지 않고 자연어 본문은 단어 단위 줄바꿈을 우선합니다.
* 실행 패널은 최초에 접어 두고 학생이 직접 펼칩니다. 펼친 폭은 240px, 접힌 띠는 44px이며 `실행 과정`을 바로 선 세로 글씨로 표시합니다. 띠 전체와 키보드로 펼칠 수 있습니다. 900px 이하의 기존 세로 배치에서는 가로 접기 버튼으로 전환합니다.
* 실행·정지·한 단계 조작과 최근 실행 메시지는 패널을 접어도 보입니다. 실행 시 패널을 자동으로 펼치지 않고 작성한 블록 좌표도 변경하지 않습니다. 실행 기록 초기화 시 요약 메시지도 초기화합니다.
* 기본 드래그 스냅샷 대신 Canvas API로 네 기호의 모양을 그려 미리보기에 사용합니다. 실제 배치 중심은 도형 크기·화면 이동·확대 배율을 반영합니다. 새 기호를 휴지통에 놓는 동작이 기존 선택 블록을 삭제하지 않도록 구분합니다.

### 4.3 수행평가 문항·작성·이동 보완 — 운영 배포 완료

2026-09-13 사용자 승인에 따라 `codex/assessment-guided-workspace`에서 구현했습니다. 기준 버전은 앞선 작업 공간 보완 커밋 `432a4fd`이며 후속 자유 설계 개선과 함께 운영 배포했습니다. 검토 절차와 당시 검증 근거는 [수행평가 검토서](docs/ASSESSMENT_UI_REVIEW.md)에 기록합니다. 새 회차의 문항과 실행 방식은 아래 4.4가 우선합니다.

* 실습의 편집기 DOM과 기호 드래그·연결·블록 편집·화면 이동·정렬을 공유합니다. 평가 진입 전 실습 상태를 별도로 보존하고 평가 이탈 시 복원하여 두 답안이 섞이지 않게 합니다. 기존 평가 전용 편집 코드는 제거했습니다.
* 평가에서는 AI 검사·처방전 자동 생성·예시 불러오기·외부 공유를 숨기고 호출도 차단합니다. 실행 패널에는 평가 실행기의 입력·진행 단계·중단 사실·예상/실제 결과를 표시합니다. 해석할 수 없는 자연어는 교사 검토 대상으로 남기며 수정 풀이를 생성하지 않습니다.
* 순차·선택·반복 카드의 필드와 순서를 저장·복원하고 교사 화면에도 구조별로 표시합니다. 이전의 짧은 텍스트 단계도 읽을 수 있습니다. 설계나 순서도 편집 후 이전 실행과 하이라이트를 초기화합니다.
* 이 단계에서 구현한 문항 버전 2는 이전 회차 호환용으로 보존합니다. 당시 새 회차는 아래 4.4의 버전 3을 사용했으며, 현재는 V4가 기본이고 V3도 선택할 수 있습니다. 버전 없는 기존 회차는 버전 1로 유지하며 기존 회차의 시작·재시험이 문항을 바꾸지 않습니다. 교사 참고 점수는 학생 답안이 주장하는 버전이 아닌 교사 관리 회차의 버전으로 계산합니다.
* 기본 검사 14개, 기존 브라우저 시나리오 23개, 작업 공간 UI 5개, 수행평가 UI 9개를 로컬 모의 환경에서 검증합니다. 통과 기록은 검토서와 테스트 결과 파일을 기준으로 하며 운영 Firebase·Google 인증·학급 동시 접속을 재검증한 것으로 간주하지 않습니다.

### 4.4 자유 설계·단일 실행·AI 초벌 검토 — 운영 배포 완료

2026-09-13 `codex/assessment-open-design`에서 `24bdc46`을 기준으로 구현했습니다. 당시 위 4.3의 고정 주제 방식은 이전 회차용으로 보존하고 새 회차에 버전 3을 도입했습니다. 현재 기본 회차는 V4이며 V3는 모의평가로 유지합니다. 구체적인 한계와 검증 기록은 [자유 설계 검토서](docs/ASSESSMENT_OPEN_DESIGN_REVIEW.md)에 기록합니다.

사용자의 병합·배포 승인으로 제품 `0d777f4`와 DB 조회 도구 `c456626`을 main `ade1f7b`에 병합했습니다. 운영 파일 일치, 세 창 폭의 기본 동작, API 메서드·인증 차단을 확인했습니다. 실제 Solar 채점 품질과 교사·학생 공동 운영은 이번 배포 검사 범위에 포함되지 않습니다. 기존 평가 자료를 바꾸지 않았으므로 새 기능을 시험하려면 기존 평가 종료 후 새 평가를 준비합니다.

* 객관식의 같은 행에 놓인 두 문항은 질문 길이가 달라도 보기 버튼의 시작 높이를 맞춥니다. 9번 정답의 `입출력` 괄호 설명을 포함해 보기의 괄호 부연을 제거했던 당시 구현입니다. 2026-09-14 후속 수정으로 기호 이름 뒤 괄호 설명을 보존하도록 변경했으며 현재는 [배포 기록](docs/DEPLOYMENT.md)의 V3 연동 수정이 우선합니다.
* 네 고정 주제 대신 현재·목표 상태와 조건을 입력하는 상단 처방전을 제공합니다. 조건 추천 API는 일반 실습의 풀이 생성 API와 구분하고 학생이 선택·수정하도록 합니다. 로컬 시연은 실제 AI 호출 없이 명시적인 예시만 제공합니다.
* 기존 공통 실행기로 입력값 한 번의 흐름을 확인합니다. 입력 대기 다음에 분기 선택 대기가 연속해서 나오는 경우에도 연속 실행 의도를 유지합니다. 구조 오류·중단 위치는 공통 실행 패널에 표시합니다.
* 교사용 AI 검토 API는 인증된 교사 역할을 서버에서 확인하고 현재 회차의 제출 답안을 Firestore에서 직접 읽습니다. 학급·번호를 모델에게 전달하지 않고 현재·목표·조건·카드·기호·연결만 보냅니다. 답안 본문에 학생이 직접 쓴 개인정보까지 자동 제거하는 기능은 아닙니다.
* AI 출력은 네 항목의 정수 0~10점과 근거 형식을 검사하고 총점은 서버에서 계산합니다. 답안 내 지시는 데이터로 취급하도록 고정 프롬프트를 사용합니다. 이 조치가 모델의 판단 정확성이나 지시문 공격 완전 차단을 보장하지는 않습니다.
* AI 제안과 교사 확정을 분리 저장하고 당시 회차·답안과 묶습니다. 확정 저장은 교사의 명시적 검토 확인 후에만 가능하며, 답안·회차가 달라지면 오래된 결과를 거부합니다. 실제 Solar 품질과 운영 인증·비용·동시 접속은 이번 모의 검사로 검증한 것이 아닙니다.

### 4.5 교사 평가 상태와 단일 조작 버튼 — 운영 배포 완료

2026-09-13 사용자가 구현·간단한 검증 후 main 병합과 운영 배포까지 승인했습니다. `codex/teacher-session-control`에서 구현했습니다. [교사 제어 검토서](docs/TEACHER_CONTROLS_REVIEW.md), [다른 PC 인수인계](docs/HANDOFF.md)를 확인합니다.

제품 `363a49a`를 main `dfdf7e0`에 병합·푸시했습니다. Vercel Production 성공과 기존 도메인의 파일 15개 일치·주요 화면 동작을 확인했습니다. [운영 배포 근거](docs/DEPLOYMENT.md)를 확인합니다.

* 학급 상태와 조작을 분리합니다. 준비 전 → 입장 대기 → 평가 중 → 평가 종료에 따라 같은 위치의 버튼이 평가 준비 → 평가 시작 → 평가 종료 → 새 평가 준비로 전환됩니다. 성적표 CSV는 보조 버튼으로 유지합니다.
* 입장·제출 인원과 평가 중 남은 시간을 상태 영역에 표시합니다. 입장 기록 수를 실시간 접속자 수라고 부르지 않습니다. 평가 종료는 채점 완료와 구분합니다.
* 종료는 확인창을 거치고 윤곽선 버튼으로 표시합니다. 새 회차 준비에도 이전 답안을 보관하고 다시 입장해야 한다는 확인을 제공합니다. 시작은 추가 확인창 없이 수행하며 처리 중에는 재클릭·학급 변경을 잠급니다.
* 서버 처리 성공 전에는 완료 상태를 표시하지 않습니다. 실패는 상태 영역에서 안내하고 재시도할 수 있습니다. 상태 구독 실패 시 시작·종료 대신 다시 연결을 제공합니다. 학급 전환·교사 화면 이탈 때 이전 구독과 타이머를 해제합니다.
* 다른 교사 화면의 회차 변경을 구독하며, 시작·종료·준비 요청은 당시 회차·상태와 서버 기록을 트랜잭션에서 비교합니다. 오래된 화면의 종료 요청이 새 회차를 마감하지 않게 합니다.
* 제한 시간이 지나면 화면에 평가 종료로 표시합니다. 이 표시만으로 DB를 쓰지 않으며 새 평가 준비를 선택했을 때 종료되지 않은 이전 회차를 마감한 뒤 보관·준비합니다. 기존 학생 자동 제출 정책과 배점·시험 시간은 유지합니다.
* 기본/API 검사 21개·교사 UI 검사 8개·학생 평가 회귀 10개를 통과했습니다. 운영 계정으로 실제 시험을 시작·종료한 검사는 아니며 배포 후 읽기 전용 확인을 별도로 기록합니다.

### 4.6 튜터·교사 진입·일반 실습 제출 개선 — 운영 반영 완료

2026-09-13 수업 후 사용자가 다섯 개선 사항의 계획·구현을 요청했습니다. 기준 `a8dbad3`, 작업 브랜치 `codex/tutor-teacher-entry`. [변경·계정 설정·검증](docs/TUTOR_TEACHER_ENTRY_REVIEW.md)을 확인합니다.

* 튜터는 48px 로봇 아이콘만 보이고 마우스·키보드 초점에서 펼쳐집니다. 클릭·터치로 열며 열린 상태와 움직임 감소 설정을 지원합니다.
* 나만의 백지 휴지통은 하단 14px에 배치하고 좁은 창에서는 튜터·실행 도구와 겹치지 않게 공간을 분리합니다.
* 교사용 버튼에서 Google 로그인과 비밀번호 기반 임시 로그인을 선택합니다. 임시 계정은 개인 Google 계정이 필요 없는 Firebase 비밀번호 계정이며, 브라우저가 교사 역할을 생성하거나 비밀번호를 저장하지 않습니다. 실제 계정 등록과 제공자 활성화는 후속 강사 계정 작업에서 완료했습니다. 현재 문구·담당 반 권한은 6절과 [강사 계정 검토서](docs/INSTRUCTOR_ACCESS_REVIEW.md)를 따릅니다.
* 이미 교사로 로그인한 사용자의 수행평가 진입은 교사 화면으로 연결합니다. 새로고침 후에도 현재 Firebase 사용자와 교사 역할을 확인합니다.
* 일반 실습 나만의 백지는 AI 설계 검사 통과 후 제출하도록 변경합니다. 이전 3.1 정책을 이번 사용자 지시로 대체하며 수행평가의 미완성 제출·배점·시간은 유지합니다.

## 5. 저장·운영·접근 제어 요구사항

다음은 목표 요구사항입니다. 현재 상태는 6절을 확인합니다.

* 브라우저 임시 작업은 sessionStorage를 사용하고 학생 정보·작업을 localStorage에 저장하지 않습니다. Firebase 서버 저장은 학생·교사 권한을 구분합니다.
* 서버의 저장 성공 응답 전에는 제출 완료를 표시하거나 답안을 잠그지 않습니다. 실패 시 작성 내용을 유지하고 재시도 경로를 제공합니다.
* 새로고침·동일 학생 재접속이 기존 답안을 초기화하지 않아야 합니다. 다음 차시 이어하기 방식과 서버 보관·삭제 기간은 아직 미정입니다.
* 교사의 평가 종료·재시험 허용이 학생 상태에 반영되어야 합니다. 답안 유지 재접속과 초기화·개별 추가 응시는 3.8·8.1절을 따릅니다. 추가 시간 정책과 예외 처리는 별도 협의합니다.
* 교사 권한, 학생별 답안 접근, 제출 후 수정 제한은 서버에서 검증합니다. 클라이언트 PIN만으로 권한을 보장하지 않습니다.
* AI 비밀 키는 서버에서 관리합니다. 브라우저 키 직접 호출 경로는 운영 배포 기준에 맞게 정비해야 합니다. 프록시 접근 제어와 호출 제한도 필요합니다.
* 학생 입력·AI 출력은 안전하게 표시하고 연결·저장 상태는 실제 결과를 반영합니다.
* 자동 채점에는 문항별 기준과 근거가 있어야 합니다. 기호·연결선 개수만으로 순서도 논리 정답을 보장하지 않습니다. 교사는 기존 기준으로 최종 결과를 검토합니다.

## 6. 현재 구현과 검증 상태

### 2026-09-14 강사 계정과 담당 학급 권한

* 후속 문구 개선: 로그인 선택은 ‘Google 로그인’ / ‘비밀번호 로그인’, 입력란은 ‘교사 비밀번호’로 통일합니다. 로그인 화면에서 담당 반을 강조하지 않으며 버튼은 단어 중간에서 줄바꿈하지 않습니다. 사용자가 지정한 운영 비밀번호로 변경하며 값은 코드·문서에 기록하지 않습니다. 담당 반 권한은 유지합니다.

* 사용자 승인: 선생님의 Google 계정은 전체 11개 반, 강사님의 비밀번호 계정은 10·11반을 관리합니다. 기존 ‘임시 로그인’은 ‘교사 비밀번호 로그인’으로 표시하며 강사님은 이메일 입력 없이 비밀번호로 접속합니다.
* `teachers/{uid}`는 `enabled: true`와 함께 `allClasses: true` 또는 `classIds: ['2-10','2-11']`를 명시합니다. 범위 없는 역할은 접근을 허용하지 않습니다. 역할 등록·변경은 관리자만 수행합니다.
* 화면은 담당 학급만 표시하고 첫 담당 반을 선택합니다. Firestore가 학급 운영, 학생 답안·성적, 보관 자료 접근을 같은 범위로 제한합니다. AI 평가 API도 답안 조회·사용량 예약 전에 해당 반 권한을 확인합니다. 학생의 익명 인증 대기실·자기 답안 저장 경로는 유지합니다.
* 공유 PC에서 취소한 교사 로그인이 늦게 성공하면 해당 인증도 해제합니다. 구독 권한 오류가 발생하면 학생 목록·답안 창·평가 상태를 비우고 재연결 또는 재로그인을 안내합니다.
* 구현 브랜치 `codex/instructor-class-access`, 제품 `a1737d2`, 운영 병합 `b0bd14e`. **운영 DB·웹 배포 완료.** 실제 권한 검사 13건과 운영 강사 UI 4흐름을 통과했습니다. 선생님의 Google 팝업 재로그인과 실제 동시 수업은 별도 확인 대상입니다. [배포 기록](docs/DEPLOYMENT.md)과 [강사 계정 검토서](docs/INSTRUCTOR_ACCESS_REVIEW.md).

### 2026-09-14 의미 기반 설계 점검 — 운영 반영 완료 / 체험 서버는 로컬 전용

* 자연어의 파란 순차 카드는 제어 구조이며 처리 기호 개수와 대응시키지 않습니다. 입력·출력은 자료 기호로 표현할 수 있고 입력 자체의 값 저장 때문에 별도 처리 기호를 강제하지 않습니다. 여러 단계와 기호의 대응은 실행 의미를 기준으로 검토합니다.
* 구조 검사는 실제 연결 그래프의 시작·종료·도달 가능성·분기·방향성 순환을 확인합니다. 화면의 위아래 배치와 기호 개수로 의미 일치나 반복 종료를 판정하지 않습니다. AI에는 자연어 원문, 기호 ID·내용, 연결 방향·분기와 확인된 구조 사실을 분리해 전달합니다.
* 구조 검사와 AI의 명시적 통과 판정이 모두 필요합니다. 응답 형식 오류는 재검사를 안내하며 통과로 간주하지 않습니다. 기존 답안 변경 시 통과 무효화·수행평가 도움 제한을 유지합니다.
* 실행 과정의 변수 영역은 내용에 맞춰 늘어나고 콘솔은 최소 140px를 확보합니다. 변수가 더 많으면 변수 영역 안에서 스크롤합니다. 좁은 화면에서는 실행 패널 높이를 별도로 확보합니다.
* 선택 실행하는 로컬 체험 서버(`--ai`, 127.0.0.1:4174)는 실제 AI와 로컬 전용 비밀번호 로그인을 제공합니다. 학생 자료·평가·교사 권한은 로컬 시연이며 운영 교사 계정 등록과 별개입니다. 서버 내부 익명 인증으로 기존 AI API를 호출하므로 실제 사용량이 발생합니다.
* 검증: `46b644b` 기반 `codex/semantic-flowchart-review`, Windows/Edge, 2026-09-14 KST. 실제 Solar 5개 사례의 기대 판정, 모의 AI UI 3흐름, 실제 AI·로컬 로그인 UI 3흐름, 기존 브라우저 회귀 검사를 통과했습니다. 입력→출력은 처리 기호 없이 통과하고, 잘못된 실행 순서·반대 분기는 보완으로 판정했습니다. 임의의 모든 자연어에 대한 정확성을 보장하지 않습니다. [세부 결과와 보안 범위](docs/SEMANTIC_REVIEW_LOCAL_PREVIEW.md).

**상태: 지정 사례 검증 후 2026-09-14 운영 병합 `b0bd14e`로 반영 완료. 로컬 체험 서버는 운영에서 활성화하지 않습니다.**

상태 용어: **계획**(합의됐으나 미반영), **구현됨·검증 필요**(코드 존재), **오류 확인**(재현 또는 코드 근거 있음), **검증 완료**(특정 버전·환경·사례의 통과 근거 있음). 일부 정상 동작을 전체 기능의 검증 완료로 확대하지 않습니다.

2026-09-12 원본 감사의 기준은 Git `cacfc5e`이며 [감사 보고서](audit/AUDIT_REPORT.md)와 [발견 사항](audit/findings.json)을 보존합니다. 이후 변경은 [구현 검토서](docs/IMPLEMENTATION_REVIEW.md)와 [배포 기록](docs/DEPLOYMENT.md)에 별도로 기록합니다. 원본 35건을 모두 해결했다고 간주하지 않습니다. 실제 학생 권한 검사 13개와 AI 응답 1회는 확인했습니다. 교사 Google 로그인은 사용자가 성공을 확인했으며, 학급 동시 접속은 아직 미검증입니다.

| 영역 | 현재 상태 | 주요 차이·근거 |
|---|---|---|
| 개념·퀴즈·실습·화면 전환 | 수정 / 로컬 검증 | 화면 중첩 제거, 캔버스 높이·툴바, 개념 문구·색상 보완 |
| 순서도 실행·검사 | 수정 / 지정 사례 검증 | 한글 변수 보존, 누락 분기 중단, 잘못된 수식 거부 |
| AI 및 실습 제출 | 수정 / 모의 장애 검증 | 나만의 백지는 이번 수업 피드백에 따라 AI 통과 후 제출로 변경, 장애 안내·수정 후 검사 무효화 |
| 수행평가 피드백 정책 | 구현 / 제한된 표현·입력 검증 | 구조 문제·중단 위치 표시, 예상/실제 결과 비교, 정답 수정 방법 미제공 |
| 수행평가 저장·복구·채점 | 수정 / 로컬·모의 검증 | 저장 확인 후 완료, 초안 복원, 제출 재시도, 답안 기반 재계산 |
| 교사 권한·실시간 운영 | 학생 원격 권한 검사 통과 / 사용자 교사 로그인 확인 | Google·익명 공급자 활성화, 학생 권한 검사 13개; 교사 UID 역할 연결 완료, 학급 공동 운영 별도 검증 필요 |
| Vercel·Firebase 운영 | V3/V4 운영 적용 / 제한된 운영 검증 | V4 운영 적용은 선생님 확인, 실제 익명 로그인·학급 조회·AI 응답 기록 존재, 27명 동시 사용 미검증 |

현 코드의 평가 구성은 30분, 객관식 10문항 30점, 단답형 6문항 30점, 알고리즘 설계·순서도 40점입니다. V4 실전평가도 현재 운영에 적용되어 있으며 V3는 모의평가·기존 회차 호환용으로 유지합니다. 11개 반·27명은 현재 설정값이며 전국 공통 기준이 아닙니다. 온실·자판기·청소년 요금·횡단보도 테마의 동등 난이도와 채점 타당성은 미검증입니다. Part 3의 제시 방식과 작성 흐름을 변경했으며 문항 수·배점·시간은 유지합니다. 자연어 설계에 필요한 시간을 고려한 평가 시간 조정은 교사 확인 후 결정합니다.

## 7. 구현 후 확인할 완료 조건

아래는 수용 기준이며 통과 기록이 아닙니다. 수정한 기능마다 날짜·Git 버전·환경·사례·결과를 기록합니다.

1. 나만의 백지에서는 현재 답안의 AI 설계 검사 통과 후에만 제출용 결과물을 내보낼 수 있고, 미통과·AI 장애에는 재검사 경로가 제공된다.
2. 평가에서 연결 누락·실행 중단·결과 불일치를 구분하고 정답·수정 방법을 노출하지 않는다. 미완성 답안도 제출할 수 있다.
3. 저장 실패 시 완료 표시 없이 답안이 유지된다. 새로고침·재접속 복구가 동작한다.
4. 무권한 교사 조작·다른 학생 답안 접근·마감 후 변경이 서버에서 거부된다.
5. 빈 그래프·끊긴 분기·잘못된 조건·반복 종료·입출력 오류를 포함해 실행 및 채점 결과를 확인한다.
6. 편집 후 검사 결과가 무효화되고, 초기화·화면 이동 뒤 이전 실행 타이머가 화면을 바꾸지 않는다.
7. 교실 대표 화면에서 주요 버튼·기호·캔버스를 사용할 수 있고 전환 후 이전 화면이 겹치지 않는다.
8. 평가에 일반 실습용 AI 풀이 도움, 추가 서술 요구, 힌트 감점이 유입되지 않는다.

## 8. 다음 구현 전 남은 결정

2026-09-15 인수인계 합의: 이번 작업은 문서·전달 준비에 집중했습니다. 2026-09-18 현재 V4 운영 적용은 선생님 확인으로 반영하며, 다음 문항 품질 작업 전에 비공개 자료의 보관 위치·PC 간 전달 방법·회차별 문항 및 배정 비밀값 보존 방식을 확인합니다. 후속 승인으로 완료된 인수인계 문서를 main에 통합하고, 다른 PC에서는 최신 main을 받은 뒤 새 작업 브랜치를 만드는 흐름을 채택합니다. 현재 상태와 재개 순서는 [인수인계](docs/HANDOFF.md)에 기록합니다.

* 다음 차시 이어하기 방식, 서버 보관·삭제 기간
* 평가 마감·재접속·재시험의 추가 예외 정책(답안 유지 재접속·결시생 추가 응시는 3.8·8.1절에 합의 및 구현 기록)
* 문항별 채점 기준, 검사 입력·예상 결과와 학생 공개 범위
* 실제 교실의 브라우저·화면 크기·동시 사용 인원 및 운영 검증 환경

위 항목 중 이미 합의된 범위는 3.8·8.1절을 따르며, 보관 기간과 추가 예외 정책 등 남은 결정은 별도로 협의합니다. 우선순위는 감사에서 확인된 권한·저장·실행·채점 오류와 합의된 모드 정책 반영이며 기능 확장은 별도 협의합니다.

### 이번 구현의 운영 전제

교사 Google 로그인·학생 학급/번호/이름 + 내부 익명 인증은 승인된 방식입니다. 교사 역할은 관리자가 `teachers/{uid}`에 `enabled: true`와 담당 범위(`allClasses: true` 또는 `classIds`)를 함께 등록하며 학생 브라우저는 역할을 만들 수 없습니다. 브라우저 인증은 세션 단위입니다. 익명 인증은 실명·학번 증명이 아니므로 번호 오입력·선점·다른 PC 복귀의 교사 복구 절차는 운영 검증에 포함합니다.

평가 시간은 저장된 마감 시각과 현재 시각의 차이로 표시합니다. 서버 규칙은 마감 이후 답안 변경을 거부하고, 이미 서버에 저장된 동일 답안을 제출 상태로 확정하는 것은 허용합니다. 미전송 변경이 있는 마감·오프라인 복구는 교사 확인이 필요하며 자동 성공 처리하지 않습니다. 재시험 전 답안을 보관하며, 마감 후 새 평가 준비는 이전 학급 기록 보관·번호 문서 초기화·새 회차 생성을 트랜잭션으로 처리합니다. 보관 기간과 기간 연장 정책은 별도 결정 대상입니다.

이전 문항 버전 1·2의 참고 점수는 객관식 30 + 단답형 30 + 지정된 순서도 검사 입력 통과 40이며 기존 교사 총점 조정을 유지합니다. 문항 버전 3은 객관식 30 + 단답형 30만 자동 계산하고 자유 설계 40점은 교사가 항목별로 확정합니다. Solar는 초벌 점수와 근거를 제안하며 확정 성적을 결정하지 않습니다. 자유 주제 간 난이도와 AI 초벌 점수의 타당성이 검증된 채점기로 간주하지 않습니다.

### 8.1 컴퓨터실 비상 복구 및 결시생 개별 추가 응시 정책 (2026-09-17 확정)

1. **PC 순간복구(롤백) 대응 답안 보존 재접속 (`[🔄 풀던 답안 유지하며 재접속 허용]`)**:
   - 컴퓨터실 PC 재부팅이나 브라우저 강제 종료 시, 학생은 새 익명 UID를 발급받습니다.
   - 보안 침범 방지: 타 학생 좌석 가로채기를 방지하기 위해 교사가 관제탑 모달에서 명시적으로 승인한 경우에만 새 UID로의 재바인딩(`allowReconnect: true`)이 허용됩니다.
   - 서버 저장 답안 보존: 기존 `answers`와 `progress`를 유지하며 재접속하도록 구현되어 있습니다. `assignedQuestions`는 V3 경로에만 저장합니다. 미전송 로컬 변경의 복구는 보장하지 않으며 실제 재접속·권한·마감 시나리오 검증은 별도입니다.
   - 답안 백지화 초기화(`[🆕 답안 초기화 후 처음부터 다시 풀기]`) 버튼과 UI/UX를 분리하여 교사의 조작 실수를 원천 방지합니다.

2. **결시생(지각/결석) 개별 30분 추가 응시 (`[⏱️ 결시생 개별 30분 추가 응시 허용]`, 2026-09-21 고도화 완료)**:
   - 학급 전체 평가가 이미 마감(`ended`)된 상태에서도, 교사는 실시간 관제탑에서 해당 결시생의 빈 좌석 또는 이미 0점 마감된 좌석 카드를 클릭하여 개별 추가 응시를 승인(`allowStudentMakeup`, `forceReset: true`)할 수 있습니다.
   - **기기 무관 접속 보장**: 승인 시 `ownerUid: null`로 초기화되어 결시생이 교실 PC, 도서관 컴퓨터, 교단 노트북 등 어느 기기에서 접속하더라도 UID 충돌 없이 즉시 바인딩 로그인됩니다.
   - 30분 개별 타이머: 학생이 대기실에 입장하여 `[개별 평가 시작하기 (30분)]`을 누르는 순간부터 30분 카운트다운이 독립적으로 작동합니다.
   - 기존 응시 학생 성적 보존: 다른 학생 문서를 변경하지 않는 경로로 구현하며, 학급 전체 세션 자동 마감 시 추가 응시생은 제외되어 온전한 30분을 보장받습니다.
   - 통합 성적 관리: 추가 응시 학생의 답안 제출 및 채점 결과는 해당 학급의 단일 27명 나이스(NEIS) 엑셀/CSV 명렬표에 그대로 병합되어 교사의 행정 부담을 최소화합니다.
