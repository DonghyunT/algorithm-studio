# 운영 배포 기록

> 읽는 기준(2026-09-25): 아래는 날짜별 운영 반영 기록입니다. 최신 변경은 최상단 기록을 기준으로 읽고 과거 기록의 미배포·검사 수·무변경 설명을 현재 상태로 재사용하지 않습니다. 현재 기능은 [PRD](../PRD.md), 작업 위치와 남은 일은 [HANDOFF](HANDOFF.md)를 따릅니다. 배포 상태·운영 로그인·DB 확인의 범위는 각 기록에 구분합니다.

## 2026-09-25 CBT 순서도 조립 UI 개선, 평가 결과 화면 0점 수정 및 리뷰 모달 UX 개편 운영 배포

- **CBT Part 3 순서도 조립(Step 2) 화면 최적화**:
  - 자연어 기획서 항목이 많을 때 화면이 무한히 늘어나 브라우저 배율을 축소해야 하던 문제를 해결하기 위해, 좌측 자연어 기획서 패널에 `max-h-full overflow-y-auto` 독립 스크롤을 적용.
  - 우측 순서도 조립 작업 영역을 640px 고정 높이(`h-[640px]`) 및 배경 드래그 팬(Pan) 이동으로 최적화하여 쾌적한 조립 환경 제공.
  - 좌측 문항 번호 목록(팔레트 섬 `#eval-palette-island`)에 `self-start`를 적용하여 문항 간 균일한 높이 유지.
- **평가 제출 완료 화면 점수 표시 정합성 복구**:
  - 제출 완료 화면(`#eval-screen-result`)에서 실제 점수가 계산되었음에도 초기 HTML 기본값인 `0점`으로 남아있던 문제 해결.
  - 플레이스홀더를 `-- / 60점`으로 교체하고, 재접속/새로고침/대기실 복귀 시 `calculateScores()`와 `renderResult()`를 보장하여 정확한 지필 소계 표시.
  - `gradeEvaluation` 채점 비교 시 문자열/숫자 엄격 일치(`===`)로 인한 타입 불일치 방지 및 단답형 인정 정답 폴백 강화.
- **문항별 리뷰 모달 데스크톱 와이드스크린 UX 전면 개편**:
  - 와이드스크린 디스플레이에 최적화된 `max-w-4xl`, `max-h-[84vh]` 레이아웃 적용으로 가로세로 안정적인 비율 확보.
  - 상단에 총 문항 수, 정답 수, 정답률(%)이 포함된 시각적 결과 요약 배너 추가.
  - 문항 지문 가독성 강화(`text-sm font-bold text-slate-900`), `⭕ 정답`/`❌ 오답` 뱃지 및 '내가 선택한 답' vs '실제 정답' 비교 카드 디자인 세련화.
  - 닫기(X) 버튼 및 하단 생기부 반영 안내 푸터 배치.
- **검증**:
  - 전체 단위 테스트 170개(100%) 통과 (`node --test tests/*.test.cjs`).
  - 정적 문법 검사 92개 스크립트 통과 (`node tools/check.cjs`).
  - 로컬 프리뷰 서버(포트 4173) HTTP 200 검증 완료.
  - 데모 모드 학생·V3 모의평가·V4 실전평가 흐름은 [인수인계](HANDOFF.md)에 완료로 기록했습니다. 이 항목에는 창 폭별 화면 검사나 페이지·콘솔 오류 결과가 별도로 기록되지 않았습니다.
- **Vercel Production 배포 확인 (2026-09-25 KST)**:
  - GitHub의 `Vercel` 상태 검사는 성공했고, Vercel 배포 메타데이터에서 `dpl_DV2vPntg98HGmw59M5y4i877UYwE`가 `READY`, 대상 `production`으로 확인되었습니다.
  - 배포된 소스는 `main`의 `6acb25141cb177cf26496318d4ebbfa03cf16584` (`feat: merge CBT flowchart workspace UX improvements and review modal fixes`)와 일치합니다. Vercel은 `algorithm-studio-ten.vercel.app`, `algorithm-studio-donghyunt.vercel.app`, `algorithm-studio-git-main-donghyunt.vercel.app` 별칭을 반환했습니다.
  - [배포 상세](https://vercel.com/donghyunt/algorithm-studio/DV2vPntg98HGmw59M5y4i877UYwE) · [기본 운영 주소](https://algorithm-studio-ten.vercel.app/).
  - **검증 경계:** Production 배포 상태와 별칭은 확인했습니다. 운영 페이지를 브라우저에서 열어 최신 화면·반응형 동작·콘솔을 검사한 것은 아닙니다. 위 테스트 및 데모 흐름은 로컬 검증 기록입니다.

## 2026-09-24 Part 3 실제 재채점·잠정점수 반영 완료

- 사용자가 기존 Upstage Solar·사용자 소유 비공개 Preview 경로를 명시 승인한 뒤 기존 대상의 채점과 점수 교체를 완료했다. 재시작 시 완료된 답안은 재사용했으며 완전 미작성은 로컬 계산했다. 원래 AI 결과와 근거 대조 조정은 비공개 보존했다.
- 회차별 트랜잭션으로 교체 전 자료를 서버 감사 사본에 함께 저장했다. 적용 후 모든 대상의 원본 답안·지필·채점 외 필드 불변, 서버 백업 원본 일치, 새 점수 일치를 재조회했다. 보관 원본은 수정하지 않았다.
- 운영 담당 교사 화면에서 여섯 항목·잠정점수·성적표 다운로드를 확인했다. 해당 브라우저 검증은 페이지/콘솔 오류 0, 학생 쓰기 0, AI 호출 0이었다. 독립 OpenXML 리더로 실제 다운로드 총점과 환산을 확인했다. 보관 지필 점수가 없는 자료는 총점·환산 대기를 유지한다.
- 실제 감점 근거에서 정상 반복·표현·항목 귀속의 오판을 발견해 지침을 보완했다. 합성 검증 총 40회 중 마지막 네 사례 전부 기대값 일치, 전체 회귀 169개·정적 검사 92개 통과. [PR #16](https://github.com/DonghyunT/algorithm-studio/pull/16)의 [검사](https://github.com/DonghyunT/algorithm-studio/actions/runs/35952286350) 성공 후 main `26daad2089c3021358811fcb6e6e832be8b2c943`에 병합했다. Vercel `dpl_5ERn5YFrqv6hBhLnteAXzAwQoQS2` Production Ready·커밋 일치와 고정 주소의 주요 파일 일치, API/직접 Firestore의 담당 교사 허용·담당 밖 거부를 재확인했다.
- 이번 작업의 나머지 비공개 검증 Preview 세 개를 제거했다. 임시 관리자 검증 경로는 운영에서 404이며 실제 재채점용 임시 배포도 남겨두지 않았다. 점수 원본 감사 사본과 비공개 검증 근거는 보존했다.
- 적용 자료와 복구 해시는 Git 제외 `scratch/part3-v2/`에 있으며 다른 PC로 자동 전달되지 않는다. 서버에 원본 감사 사본이 존재한다. 이후 교사 정정/답안 변경이 생긴 자료를 자동 복원하지 않는다. 학생 수·성적·개별 답안은 공개 기록에 포함하지 않는다.

## 2026-09-24 Part 3 채점 기준 개편 — 최초 운영 배포, 당시 학생 재채점 보류

[PR #15](https://github.com/DonghyunT/algorithm-studio/pull/15), 병합 `fe13b21ebb16cb6f84ac598a704fb6318fe845cf`로 운영 반영했다. 새 실전평가용 6항목/40점·최저 6점, 잠정점수/교사 정정, 성적표 전용 30점 짝수 올림, 보관 회차 출력과 별도 정정, 원본 복구 도구를 구현했다. 기존 실전평가와 모의평가는 기준 버전을 유지한다.

- 정적 검사 91개 스크립트 및 전체 자동 검사 169개 통과. 소수 환산·9,216개 점수 조합·빈 답안·권한 경계·캐시 일치·원본/지필 불변·적용과 복원·추가 정정 이후 복원 거부를 포함한다.
- 로컬 Edge 전체 회귀 24개 및 새 교사 6항목 입력/저장 실패/정정/320~1440px 조작, 기존 보관 화면 검사를 통과했다. 페이지·콘솔 오류 0. 합성 XLSX는 독립 OpenXML 리더에서 6항목·병합·환산값·상태를 확인했다.
- 실제 Solar 합성 시험은 36회다. 초기 오판을 보완하고 마지막 외딴 블록/정상 답안 추가 네 번이 기대값과 일치했다. 실패 기록과 해석 한계는 [검증 기록](PART3_RUBRIC_CALIBRATION.md)을 따른다.
- 실제 학생 답안의 재채점과 서버 점수 교체는 **미실행**이다. 자동 승인 검토가 기존 Upstage Solar와 사용자 소유 비공개 Vercel Preview를 통한 답안 내용 처리에 목적지 동의를 요구하여 사용자 질문을 남겼다. 승인 전에는 실행하지 않는다. 로컬 복구 스냅샷은 Git 제외 위치에 보존했다.
- 임시 관리자 검증 API는 제품 API에서 제거했고 도구 템플릿만 남겼다. Firestore 규칙·운영 문제은행은 변경하지 않았다. 운영 권한/실자료 채점 검증은 로컬 검사로 대체했다고 표현하지 않는다. 실제 NEIS 업로드·Excel 인쇄는 미검증이다.
- [PR 검사](https://github.com/DonghyunT/algorithm-studio/actions/runs/35926510910) 성공, Vercel `dpl_B6tviZ6Vj4ukrtCpcaQQtknC9EMn` Production Ready와 병합 커밋 일치 확인. 고정 운영 주소의 HTML 및 주요 JS 5개가 로컬과 일치했다. ping 200, 비로그인 학생 점수·AI 요청 401, 제거한 임시 API 404를 확인했다.
- 기존 담당 교사 계정으로 운영 API의 담당 학급 읽기 200·담당 밖 학급 403, AI API의 담당 밖 요청 403, 직접 Firestore의 담당 학급/보관 읽기 200·담당 밖 보관 읽기 403을 확인했다. 이 검사에서 학생 점수 쓰기와 AI 호출은 0회다. 실제 학생 본인의 정상 접근은 모의 API 검증이며 새 운영 학생 로그인을 수행하지 않았다. 비공개 근거는 `scratch/part3-v2/production.json`이다.
- 작업 중 만든 이전 합성시험용 Preview 세 개는 제거했고 최신 비공개 검증 Preview는 재개를 위해 유지했다. 복구는 제품 커밋 revert/이전 배포 복원과 학생 점수 복원을 구분한다. 현재는 실제 점수를 바꾸지 않았으므로 학생 점수 복원이 필요하지 않다.

## 2026-09-24 평가 점수 조회 절감 운영 배포

[PR #13](https://github.com/DonghyunT/algorithm-studio/pull/13), main merge `64255cc5f71a344d532ef342461876a132deb6e3`로 학생의 무한 점수 재조회와 미제출 학생 저장에 따른 교사 전체 성적 재조회를 줄였습니다. 학생은 서버의 `part3` 응답 구조를 읽고 기존 학생 문서 구독의 검토 변경으로 재조회합니다. 교사 점수 캐시는 제출된 Part 1·2 답안에 연결하고, 제출 취소·재제출·회차 전환·늦은 응답을 구분합니다. Part 3 제안·확정 표기를 맞추고 총점도 지필 점수와 함께 누르는 동안만 표시합니다.

- 로컬 정적 검사 79개 스크립트 및 전체 자동 테스트 152개 성공. 새 회귀 검사 15개는 불필요한 조회 억제, 제안·확정·0점 갱신, 같은 회차의 늦은 응답, 제출 취소·재제출, 제한된 재시도, 결과 화면 재방문, 구독 초기화 재진입과 V3 호환을 확인합니다.
- 모의 결과: 미제출 답안·검토 상태가 100회 변경돼도 교사 전체 성적 요청은 최초 1회입니다. 이는 테스트 시나리오의 호출 수이며 실제 운영 읽기 절감률을 뜻하지 않습니다.
- 로컬 Edge 전용 검사에서 대기 중 반복 조회 없음, Part 3 제안·확정·0점 변경, 지필·총점 누르는 동안만 공개, 미제출 답안 변경 시 전체 조회 생략을 확인했습니다. 1440/390px 결과 화면 캡처와 페이지·콘솔 오류 0건을 확인했으며, 실제 CDN을 사용하되 Firebase·API 응답은 모의로 격리했습니다. 실행 도구는 `tests/evaluation-read-efficiency-ui.cjs`, 결과는 Git 제외 `scratch/read-efficiency/`입니다.
- 기존 브라우저 회귀 24개도 성공했습니다. 평가 준비·시작·입력·저장 실패·새로고침 복구·교사 종료·제출 후 재접속·화면 잠금·창 폭을 확인했고 페이지·콘솔 오류는 0건입니다. 검사 스크립트에 명시적 V3 대기실 준비·기존 화면 모드·로컬 시간 응답을 보완했습니다. 로컬의 동기식 초기 알림이 구독 함수를 재진입하지 않도록 앱에도 보호를 추가했습니다.
- 자동 저장 주기, 저장 데이터, 점수 계산, 문항, 서버 API 및 Firestore 규칙은 변경하지 않았습니다. 운영 자료를 이용한 제출·점수 저장·AI 호출 검사는 하지 않습니다. DB 마이그레이션은 없으며 프런트엔드 커밋 되돌리기로 복구할 수 있습니다.
- [PR 검사](https://github.com/DonghyunT/algorithm-studio/actions/runs/35912999091) 성공. Vercel 배포 `dpl_GcdKGSahifvbJ621DKU3Mxpz4E9e`의 Production Ready 및 병합 커밋 일치를 확인했습니다. 관리 API의 인증 오류는 사용자 승인에 따른 CLI 재로그인 후 해소됐습니다.
- [고정 운영 주소](https://algorithm-studio-ten.vercel.app/)에서 HTML·학생 평가 JS·교사 클래스룸 JS의 HTTP 200 및 줄바꿈 정규화 후 로컬 파일 일치, API `ping` 200, 비로그인 `student-score` 401을 확인했습니다. 근거는 Git 제외 `scratch/read-efficiency/production.json`입니다. 이는 운영 파일·기본 API 확인이며 실제 학생 로그인·답안 저장·검토 확정의 실서비스 검증을 뜻하지 않습니다.
- 기존에 열린 교사·학생 창은 평가 종료 후 새로고침해야 새 코드가 적용됩니다. 실제 운영 읽기·비용 절감량은 다음 수업 사용량에서 확인해야 합니다. 이후 문서 전용 병합은 제품 코드·운영 자료를 다시 변경하지 않습니다.

## 2026-09-24 개인 보고서와 임시 파일 공개 목록 정리

[PR #11](https://github.com/DonghyunT/algorithm-studio/pull/11), main merge `b752b6b4a1cf8c1171517581296d9042741293a5`로 개인 자기점검 보고서 중복 2개와 일회성 `diff.txt`를 제거했습니다. 로컬 원본 보관과 재등록 방지는 [개발 문서](DEVELOPMENT.md)의 자료 정리 항목을 따릅니다.

- 정적 검사와 [PR 검사](https://github.com/DonghyunT/algorithm-studio/actions/runs/35909459243) 성공. 앱 코드·인증 설정·Firebase 키·평가 자료는 변경하지 않았습니다.
- Vercel 배포 `dpl_D6zEdP5Gcu1tsmMNknirfGswGkmk` Production Ready, 배포 커밋이 병합 커밋과 일치했습니다.
- 고정 Vercel 운영 주소와 GitHub Pages에서 홈 HTTP 200, 줄바꿈을 정규화한 HTML의 로컬 일치, 제거한 세 경로의 HTTP 404를 확인했습니다. 이번 검증은 파일 제공 상태 확인이며 로그인·학생 데이터 쓰기·AI 요청 검사는 수행하지 않았습니다.
- 과거 Git 커밋과 이전 Vercel 배포의 스냅샷은 삭제하지 않았습니다. 이후 문서 전용 배포는 같은 제품 파일과 제외 설정을 유지합니다.

## 2026-09-24 V4 단답형 인정 답안 확대와 승인된 성적 정정

교사의 최종 재검토 및 병합·배포 승인에 따라 [PR #9](https://github.com/DonghyunT/algorithm-studio/pull/9)를 main에 병합했습니다. 단답형의 문항별 허용 표현을 보완하고 원본 감사 저장·중복 방지·복구 도구를 적용했습니다. 교사가 제외한 추가 표현은 운영 은행에서도 제외했습니다. 실제 인정 목록·학생별 결과·운영 자료에서 도출한 규모·계획 식별자는 비공개 자료에만 남깁니다.

- 제품 병합: `a7facd9343b20b9b3d00014ab5371dc8a41bb5c8`. [PR 검증](https://github.com/DonghyunT/algorithm-studio/actions/runs/35905179944) 성공. 로컬 전체 테스트 137개·정적 검사 및 보관 표시 브라우저 검증 통과.
- 운영 배포: `dpl_57U4YyXNwSUZjgSaA1NYQim2GoLT`, [Vercel 상세](https://vercel.com/donghyun2/algorithm-studio/57U4YyXNwSUZjgSaA1NYQim2GoLT) Production Ready. [고정 운영 주소](https://algorithm-studio-ten.vercel.app/)에서 HTML·교사 화면 JS·정정 표시 helper가 검증본과 일치합니다.
- 은행 교체: 기존 `EVAL_BANK_V4_JSON`의 production 값만 최종 승인본으로 갱신하고 새 코드와 함께 배포했습니다. 타입·대상 범위를 유지했고 `EVAL_ASSIGNMENT_SECRET` 및 다른 환경 변수는 변경하지 않았습니다. 교사 `export-bank` 결과가 승인본과 정확히 일치합니다.
- 운영 자료: 적용 전 은행·세션·학생 자료가 검토 계획과 같고 진행 중 V4/추가 응시가 없음을 확인했습니다. 학급별 트랜잭션으로 원본 학생 스냅샷과 정정 이력을 보존한 후 대상 점수를 갱신했습니다. 재조회로 대상 점수·감사 원본·원본 답안·Part 3·비대상 자료·기존 보관 자료의 무변경을 확인했습니다. 기존 보관 원본이나 Firestore 규칙은 수정하지 않았습니다.
- 운영 접근: 담당 교사 허용, 담당 외 거부, 비로그인 거부를 API와 직접 Firestore 각각 재확인했습니다. 검사 계정 담당 범위 내 서버 점수가 저장 답안의 기대값과 일치합니다. 정정 대상 학급은 이 계정의 담당 범위 밖이므로 그 학급을 우회 조회하지 않았습니다.
- 운영 브라우저: 실제 CDN을 사용하는 고정 주소에서 합성 답안만 DOM에 표시하여 정정 안내·1440/390px 줄바꿈을 확인했습니다. 페이지 예외·콘솔 오류 0건이며 학생 로그인·답안 쓰기·AI 호출은 하지 않았습니다. 정정 대상 학급의 실제 교사/학생 화면 전체 및 재내보낸 엑셀·NEIS 검증은 포함하지 않습니다.

현재 교사/학생 화면은 최신 은행으로 계산하므로 새로고침하고 성적표는 다시 내보내야 합니다. 정정 대상만 모아보는 운영 UI는 없으며 비공개 완료 목록을 사용합니다. [정정 절차](V4_OBJECTIVE_CORRECTION.md), [DB 구조](DATABASE.md), [설정과 백업](EVALUATION_V4_SETUP.md)에 관련 상태를 맞췄습니다. 이 기록 이후 문서만 수정하는 배포는 제품 파일과 정답표·DB를 다시 변경하지 않습니다.

## 2026-09-23 교사용 보관 회차 답안 읽기 전용 조회 배포

보관된 모의평가·평가 회차에서 학생의 제출 답안과 저장된 점수를 다시 살펴볼 수 있도록 교사 클래스룸에 `이전 평가 기록` 탭을 추가했습니다. 조회 화면은 기존 보관 스냅샷을 읽기만 하며, 점수를 재계산하거나 보관 자료·Firestore 규칙을 변경하지 않습니다.

1. **변경 및 검토:**
   - [PR #7](https://github.com/DonghyunT/algorithm-studio/pull/7)을 `main`에 병합했습니다. 병합 커밋은 `992e0085653711bf136c7e442af5bed6ee8fa313`입니다.
   - [GitHub PR 검증 작업](https://github.com/DonghyunT/algorithm-studio/actions/runs/35831732616)은 성공했습니다. `node tools/check.cjs` 73개 스크립트, `node --test tests/*.test.cjs` 130개, 로컬 시연 브라우저 확인도 통과했습니다.
2. **배포 상태:**
   - 병합 커밋의 GitHub Vercel 상태 검사는 `success`였으며 [Vercel 배포 상세](https://vercel.com/donghyun2/algorithm-studio/Fe4tv7zr3zzhKvVrLbf8kns3iwSG)에서 확인할 수 있습니다. Git 연동 자동 배포가 완료 상태를 반환해 별도 CLI 배포는 실행하지 않았습니다.
3. **확인 범위와 한계:**
   - 로컬 시연 브라우저에서 회차·학생 검색, 저장된 소계/확정 검토 내용, 원문 부재 안내, 읽기 전용 무변경과 1440/1024/768/390px 레이아웃을 확인했습니다. 브라우저 검사는 외부 CDN 대체 응답을 사용했습니다.
   - 이번 배포 확인에서는 실제 운영 페이지의 수정 파일 HTTP 일치, 교사 로그인, 운영 Firestore 접근 권한·학생 자료를 확인하지 않았습니다. 따라서 Vercel 상태 성공은 기록하되 이 항목들의 실서비스 검증 완료로 표현하지 않습니다. 상세 기능·제한은 [PRD 3.4절](../PRD.md), 보관 구조는 [데이터베이스 설계](DATABASE.md)를 따릅니다.

## 2026-09-23 관제탑 실시간 점수 캐시의 평가 회차 격리 운영 배포

교사 좌석 점수에 다른 평가 회차의 서버 채점 캐시가 섞이지 않도록 회차별로 격리했습니다. 선생님의 병합·배포 승인에 따라 [PR #5](https://github.com/DonghyunT/algorithm-studio/pull/5)를 main에 병합했습니다.

1. **변경 내용**:
   - 기능 커밋 `2d13396`은 `currentLiveClassGrades`를 V4 `attemptId`에 묶고, 세션·학생·캐시의 회차 ID가 맞을 때만 사용합니다.
   - 이전 회차의 늦은 서버 응답과 상세 모달 응답은 버리며, 회차가 바뀐 도중 엑셀 내보내기가 시작되면 혼합된 자료를 만들지 않고 중단합니다.
   - V3 모의평가는 V4 서버 캐시를 사용하지 않고 기존 소계·검토대기 방식으로 표시합니다. 답안·확정 성적·배점·채점 기준·Firestore·보안 규칙은 변경하지 않았습니다.
2. **GitHub 및 배포**:
   - main 병합 커밋 `0c0c76e1d6df61be494a38e4a3e52dded90fdd3f` (PR #5, 2026-09-23 12:56 KST).
   - [main 검증 작업](https://github.com/DonghyunT/algorithm-studio/actions/runs/35816303373) success, [GitHub Pages 배포 작업](https://github.com/DonghyunT/algorithm-studio/actions/runs/35816303095) success.
   - Vercel GitHub Production 배포 `6606016018` 상태 `success`, 배포 대상 SHA는 main 병합 SHA와 일치. Vercel이 표시한 개별 배포 URL은 로그인으로 보호되어 있었지만, 공개 고정 운영 주소 [algorithm-studio-ten.vercel.app](https://algorithm-studio-ten.vercel.app/)에서 `js/core/classroom.js`를 HTTP 200으로 읽고 저장소 파일과 내용이 일치함을 확인했습니다.
3. **검증 범위**:
   - `node --test tests/*.test.cjs`: 127개 통과. `tests/classroom-score-grid.test.cjs`: 10개 통과. `node tools/check.cjs`: 72개 스크립트 통과.
   - 로컬 Edge 브라우저 점수판에서 이전 V4 캐시가 새 V4 회차와 V3 모의평가에 섞이지 않는 흐름을 확인했고 페이지 예외·콘솔 오류는 0건.
   - 더 넓은 `tests/browser.cjs` 실행은 완료되지 않아 통과로 계산하지 않았습니다. 운영 교사 로그인, 실제 평가 시작, 학생 자료 조회·쓰기는 수행하지 않았습니다.

## 기록 해석 보완 — 2026-09-19

과거 본문의 “100% 무결성”, “원천 차단”은 지정 사례 밖의 보장으로 사용하지 않습니다. Git 제외는 유출 방지의 충분조건이 아니고, 요청 간격은 다른 탭·일일 한도에 따른 오류까지 막지 않습니다. 저장 요청 절감은 미전송 답안 보존이나 교실 전체 사용량을 보장하지 않습니다. 최신 제한과 확인 과제는 PRD를 따릅니다. 날짜별 배포·검사 사실은 아래 원문을 보존합니다.

## 2026-09-22 컴퓨터실 시계 오차(Clock Skew) 방어, 학생 관리 모달 2단 개편 및 실시간 타이머·마감 동기화 운영 배포

선생님의 요청 및 승인에 따라 1) 특정 컴퓨터실 PC의 윈도우 로컬 시계 차이 보정, 2) 학생 관리 모달 데스크탑 좌우 2단 분할 개편, 3) 제출 취소 후 기존 답안을 유지하는 복귀 기능과 추가 시간 부여, 4) 관제탑 학급 전체·학생 개별 시간 연장, 5) 제출 완료 화면 버튼 재배치, 6) 실시간 리스너와 타이머 복구, 7) 교사 강제 제출의 실시간 반영, 8) 진행 중 평가에 늦게 입장한 학생의 시험 화면 전환을 main에 반영하고 운영 배포한 것으로 기록되어 있습니다. 제품 코드 변경은 `ea55043`, `149720b`, `83c527a`, `be8fd62`, `9782b7d`에 있으며, `375ca5c`는 PRD/HANDOFF 문서 갱신, `10fa85d`는 이 배포 기록을 추가한 문서 전용 커밋입니다. 정확한 운영 배포 커밋은 로컬 Git 이력에서 확인되지 않습니다.

1. **컴퓨터실 시계 오차(Clock Skew) 방어 알고리즘 (`api/evaluation.js`, `ai-service.js`, `eval-service.js`, `lab-eval.js`):**
   - 로컬 시계가 15분 빠르게 설정된 학생 PC에서 조기 제출되던 문제를 `/api/evaluation` 경량 ping과 `serverTimeOffset` 계산으로 해결.
   - `getNow()` 메서드로 로컬 시계 오차를 자동 보정하여 기기별 시계 왜곡에 영향받지 않는 타이머 정확도 보장.
2. **학생 관리 모달 좌우 2단 분할 레이아웃 개편 (`index.html`, `classroom.js`):**
   - 하단 세로 나열 버튼을 데스크탑 2단 분할 레이아웃으로 전면 개편.
   - 좌측 280px 패널: 학생 미니 프로필 카드(좌석, 상태 뱃지, 이름, 실시간 카운트다운 타이머) + 상태별 스마트 버튼 그룹.
   - 제출 완료 학생: `[🔄 제출 취소 및 복귀 (+10분)]` (추천 메인), `[제출 취소 및 복귀 (+5분)]`
   - 풀이 중 학생: `[시간 +10분]`, `[시간 +5분]`, `[풀던 답안 유지 재접속 허용]`, `[현재 답안으로 정상 제출 마감]`
   - 주의 조치(하단 분리): `[답안 초기화 (완전 백지화)]`, `[좌석 비우기 (퇴장 처리)]`
3. **제출 취소 및 풀던 답안 100% 보존 복귀 (`reopenStudentExam`):**
   - 조기 제출된 학생의 답안(`Part 1~3`)을 100% 보존한 채 `status: 'in_progress'`로 전환하고 추가 시간 즉시 부여.
   - `makeupAllowed: true`, `allowReconnect: true`를 세팅하여 학생 화면이 실시간으로 풀이 화면으로 복원되며 새 브라우저 재접속도 허용.
   - `archives/` 감사 컬렉션에 원본 답안과 세션 상태를 자동 아카이빙.
4. **학급 전체 및 개별 시간 연장 (`extendClassSessionTime`, `extendStudentTime`):**
   - 관제탑 상단 툴바에 `[⏱️ 전체 5분 연장]` 버튼 신설.
   - 학생 모달에서 개별 학생에게 원클릭 `+5분 / +10분` 시간 부여.
5. **제출 완료 화면 UX 개선 (`index.html`):**
   - `[📋 문항별 내 답안과 정답 확인]` 버튼을 상단 점수 카드 내부로 통합.
   - 하단 버튼을 좌측 `[🗺️ 학습 로드맵으로 나가기]`, 우측 `[로그아웃 · 사용 종료]`로 재배치.
6. **실시간 리스너 지속성 보장 및 시험장 즉시 직행 (`lab-eval.js`, `eval-service.js`):**
   - 새로고침/재접속 후 복구 진입 시 리스너가 누락되던 버그를 `attachAssessmentListeners()`로 완전 해결.
   - 개별 시간 연장 수신 시 `this.sessionStatus`와 무관하게 타이머를 즉시 재가동하고 상단 토스트 알림 노출.
   - 교사의 `[현재 답안으로 정상 제출 마감]` 수신 시 실시간으로 즉시 결과 화면 전환.
   - 이미 진행 중인 시험에 뒤늦게 입장한 학생이 대기실에 갇히지 않고 즉시 시험 화면으로 직행하도록 보장.
7. **검증:**
   - `tests/eval-time-extension.test.cjs` 전용 테스트 6종 포함 전체 123개 단위 테스트 100% 통과, `tools/check.cjs` 72개 스크립트 정적 검사 통과.
   - Vercel 프로덕션 배포 및 `/api/evaluation` ping 실서버 응답 확인 완료.

## 2026-09-21 결시생 개별 30분 추가 응시 복원 및 기기 무관(임의 PC) 접속 보장 운영 배포

선생님의 요청 및 승인에 따라 정규 평가가 종료된 후 결시생(결석/지각/미응시)이 교실 PC, 도서관 컴퓨터, 교단 노트북 등 어느 기기에서 접속하더라도 안전하게 30분 추가 응시를 진행할 수 있도록 플로우를 전면 복원하여 운영 배포했습니다. (커밋: `311ea4c`, 머지: `b08ca00`, fix: `f23e667`)

1. **학생 화면 로비 조기 차단 제거 및 트랜잭션 일원화 (`js/labs/lab-eval.js`):**
   - 브라우저 로비 단계에서 세션 종료(`ended`)만 보고 결시생 여부를 조회하기도 전에 튕겨내던 조기 차단 코드를 제거하고, `joinWaitingRoom` 트랜잭션을 통해 승인된 결시생(`makeupAllowed: true`)은 대기실로 통과시켜 `[개별 평가 시작하기 (30분)]` 버튼을 띄우고, 미승인 일반 학생은 차단하도록 일원화했습니다.
2. **0점 자동 마감 결시생 안전 초기화 및 30분 재부여 (`js/core/classroom.js`):**
   - 시험 종료 시 0점으로 자동 마감(`status: 'submitted'`)된 결시생이라도 좌석 모달에서 `[결시생 개별 30분 추가 응시 허용]` 버튼을 눌러 기존 기록을 안전하게 초기화(`forceReset: true`)하고 30분 권한을 부여할 수 있도록 개선했습니다.
3. **기기 무관(어느 PC든) 접속 보장 (`js/core/eval-service.js`):**
   - 추가 응시 승인 시 `ownerUid`를 `null`로 초기화하여, 결시생이 교실 PC뿐 아니라 도서관 컴퓨터, 노트북 등 어느 기기에서 접속하더라도 "다른 기록에 연결됨" 오류 없이 즉시 바인딩되어 정상 응시할 수 있게 했습니다.
4. **검증 및 배포:**
   - 신규 단위 테스트 `tests/eval-entry-guard.test.cjs` (7건) 포함 총 117개 단위 테스트 100% 통과, `tools/check.cjs` 71개 스크립트 정적 검사 통과.

## 2026-09-21 엑셀 개인성적표 Part 3 서식·채점·루브릭 고도화 및 Firestore shadowing 해소 운영 배포

선생님의 요청 및 승인에 따라 1) 엑셀 개인성적표 Part 3 계획 요약(현재 상태, 목표 상태, 지켜야 할 조건) 3개 영역 분리, 2) 교사 확정 전이라도 AI 1차 채점 점수가 소계 및 총점에 온전히 합산되도록 개선, 3) Part 3 공식 4대 루브릭 평가 기준 및 학생 답안/피드백 매핑, 4) 너비 11컬럼 확장 및 동적 행 높이(`part3RowHeight`, 최소 38pt 이상) 적용, 5) Firestore review shadowing 해소를 main에 병합하고 운영 배포했습니다. (커밋: `bde842d`, 머지: `5f1f4a7`, fix: `87ebe27`, polish: `4b0dd03`)

1. **Part 3 서식 구조화 및 1차 채점 점수 합산 보장 (`js/core/excel-export.js`):**
   - 계획 요약을 현재 상태, 목표 상태, 지켜야 할 조건으로 3개 행/필드로 구조화 분리.
   - 교사 확정 전이라도 AI 1차 채점 점수를 개인성적표 소계 및 총점에 온전히 합산하여 0점으로 오인되지 않도록 보장 (`resolvePart3ProposalScore`).
2. **공식 4대 루브릭 텍스트 및 학생 답안/피드백 매핑:**
   - 기준 1: 문제 및 조건의 명확성 (10점), 기준 2: 자연어 알고리즘의 논리성 (10점), 기준 3: 자연어와 순서도의 일치성 (10점), 기준 4: 순서도 구조 및 동작의 타당성 (10점) 공식 루브릭 텍스트 제공.
   - D~F열 병합 너비 확장(11컬럼 체계) 및 내용 길이에 따른 동적 행 높이 계산으로 인쇄 및 화면 열람 시 텍스트 잘림 현상 원천 차단.
3. **Firestore Review Shadowing 해소:**
   - 클라이언트 review 객체에 점수가 비어있더라도 서버 `class-grades` 응답과 안전하게 병합하여 1차 채점 점수가 엑셀에서 누락되는 현상을 완벽히 방지.
4. **검증 및 배포:**
   - `tests/excel-export.test.cjs` 등 단위 테스트 110개 100% 통과, `tools/check.cjs` 70개 스크립트 정적 검사 통과.

## 2026-09-21 교사 관제탑 좌석 바둑판 실시간 서버 성적 연동 운영 배포

선생님의 요청 및 승인에 따라 교사 관제탑 실시간 좌석 바둑판의 학생 점수가 로컬 클라이언트 임시 계산 대신 서버 V4 배치 채점(`class-grades`) 점수와 100% 일치하도록 연동하여 운영 배포했습니다. (커밋: `2fb0327`, 머지: `36a5912`)

1. **실시간 서버 성적 동기화 (`js/core/classroom.js`):**
   - `currentLiveClassGrades` 맵을 도입하여 `class-grades` 서버 배치 채점 API 결과를 좌석 카드에 실시간 매핑.
   - 관제탑 실시간 좌석 카드 점수와 엑셀 다운로드 성적표의 점수 불일치를 해소.
2. **검증 및 배포:**
   - 전체 단위 테스트 통과 및 운영 배포 확인.

## 2026-09-21 수행평가 '시험 종료' 및 시간 만료 시 방 입장 원천 차단 및 좌석 보호 운영 배포

선생님의 요청 및 승인에 따라 시험이 종료되었거나 타이머 시간이 만료된 학급에 미응시 학생(전출생 번호 등)이나 외부 사용자가 방에 입장하거나 다른 학생의 좌석을 덮어씌우지 못하도록 다층 방어 체계를 구축하여 운영 배포했습니다.

1. **시간 만료(deadlineMs 경과) 및 시험 종료(ended) 시 입장 차단 (`js/core/eval-service.js`):**
   - `isSessionOpen(session)` 판정 메서드를 도입하여 `status === 'waiting'` 또는 `status === 'in_progress' && Date.now() < session.deadlineMs`일 때만 유효 입장으로 인정합니다.
   - 마감 시간이 지났거나 세션이 종료된 경우 신규 학생의 `joinWaitingRoom` 시도를 즉시 차단(`현재 {classId}반은 수행평가가 종료되었습니다. 입장할 수 없습니다.`)합니다.
   - 기존 학생의 경우에도 이미 제출된 답안의 조회는 허용하되, 미제출 상태로 덮어쓰거나 비정상 진입하는 행위를 차단합니다. (단, 교사가 명시적으로 승인한 개별 추가 응시생 `makeupAllowed`는 정상 응시 보장)
2. **Firestore 보안 규칙(Security Rules) 강화 (`firestore.rules`):**
   - `students/{studentNum}`의 `create` 규칙에 `session.status == 'waiting' || (session.status == 'in_progress' && request.time.toMillis() < session.deadlineMs)` 조건을 부여하여, 마감 시간이 경과한 세션에 신규 학생 좌석 문서가 생성되는 것을 DB 레벨에서 원천 차단했습니다.
3. **학생 화면 로비 및 세션 리스너 차단 (`js/labs/lab-eval.js`):**
   - 로비의 학급 선택 시 세션이 마감/종료되었으면 상태 배지를 `평가 종료 (입장 불가)`로 즉시 표시하고 [대기실 입장하기] 클릭 시 안내 경고창을 띄워 차단합니다.
   - 시험 진행 중 시간이 마감되면 학생 화면에서 00:00으로 멈추지 않고 즉시 자동 최종 제출(`submitExam(true)`)하여 결과 화면으로 안전하게 전환합니다.
4. **교사 관제탑 시간 만료 자동 동기화 (`js/core/classroom.js`):**
   - 교사 관제탑에서 타이머 만료(`Date.now() >= deadlineMs`) 감지 시 백그라운드에서 미제출 학생 일괄 마감(`autoSubmitRemainingStudents`)과 세션 종료(`endSession`)를 안전하게 1회 자동 호출하여 DB 세션 상태를 `ended`로 동기화합니다.
5. **검증 및 배포:**
   - 신규 단위 테스트 `tests/eval-entry-guard.test.cjs` (6건)을 포함하여 총 108개 단위 테스트 100% 통과.
   - `tools/check.cjs` 69개 스크립트 정적 검사 통과.

## 2026-09-21 학생용 문항별 답안/정답 확인 모달(V4) 렌더링 오류 수정 및 운영 배포

선생님의 요청 및 승인에 따라 수행평가 종료 후 학생 화면에서 `[📋 문항별 내 답안과 정답 확인]` 모달을 열었을 때 데이터가 표시되지 않거나 비어있던 문제를 완벽히 해결하여 운영 배포했습니다.

1. **치팅 방지 상태 체크 및 진행 안내 보정 (`js/labs/lab-eval.js`):**
   - 서버 `api/evaluation.js`의 `in_progress` 응답 객체(`{ ready: false, inProgress: true, message: '...' }`)를 클라이언트에서 `res.inProgress || res.ready === false || res.sessionStatus === 'in_progress'`로 정확히 감지하도록 수정했습니다.
   - 시험 진행 중에는 안내 문구와 지필 채점 참고 점수를 친절하게 표시하여 불필요한 혼란을 방지합니다.
2. **V4 서버 데이터 스키마와 클라이언트 필드명 전면 일치:**
   - `openReviewModal()` 호출부에서 `res.scores`, `res.studentAnswers` 대신 `res.score`, `res.answers`, `res.part3`를 방어적으로 바인딩했습니다.
   - Part 1 객관식: 지문 필드(`desc || title || prompt`), 학생 선택(`studentAnswer || myChoice`, 선택지 텍스트 포함 렌더링), 실제 정답(`correctAnswer || answer`, 선택지 텍스트 포함 렌더링), 정오답 판정(`isCorrect`), 해설(`teacherNote || explanation`)을 통합 처리했습니다.
   - Part 2 단답형: 지문 필드(`desc || title || prompt`), 학생 입력(`studentAnswer || myInput`), 인정 정답(`answers || acceptableAnswers`), 정오답 판정(`isCorrect`), 해설(`teacherNote || explanation`)을 통합 처리했습니다.
   - Part 3 순서도: `part3` 데이터 구조(`confirmed`, `proposal`, `criteria`, `feedback`)를 안전하게 파싱하여 4대 평가 기준별 점수/피드백 및 총평을 온전히 렌더링합니다.
   - 서버 `api/evaluation.js`에서도 `student-review` 액션 시 `part3Review`에 `feedback` 필드를 누락 없이 반환하도록 보강했습니다.
3. **검증 및 배포:**
   - 신규 단위 테스트 `tests/student-review-modal.test.cjs` (2건) 및 `tests/evaluation-api.test.cjs` 보강을 포함하여 전체 102개 단위 테스트 100% 통과.
   - `tools/check.cjs` 68개 스크립트 정적 검사 통과.

## 2026-09-21 멀티 시트 엑셀 성적표(A4 맞춤 인쇄) 및 학생용 답안/정답 확인 모달 운영 배포

선생님의 요청 및 승인에 따라 1) 순수 Vanilla JS OpenXML 기반 멀티 시트 엑셀(`.xlsx`) 성적표 생성기 구축, 2) 학생 개별 성적표 지필평가 좌우 2단 배치 및 A4 1장 무수정 맞춤 인쇄(23행 압축, fitToPage) 최적화, 3) V4 실전평가 서버 채점 일괄 연동(`class-grades` API), 4) 학생용 문항별 답안/정답 확인 모달(`student-eval-review-modal`) 및 치팅 방지 락, 5) 학생 화면 Part 3 1차 채점 실시간 반영을 main에 병합하고 운영 배포했습니다.

1. **멀티 시트 엑셀(`.xlsx`) 성적표 생성기 (`js/core/excel-export.js`):**
   - 외부 라이브러리 없이 순수 Vanilla JS OpenXML ZIP(`.xlsx`, Store 방식)으로 단일 파일 내 `학급종합`, 학생수만큼의 A4 맞춤형 개별 성적표(`01_강수아` 등), 나이스 복사용 `나이스(NEIS) 양식` 시트를 동시 생성합니다.
   - 학급명 `2-6`이 엑셀에서 날짜(`02월 06일`)로 왜곡되는 현상을 `inlineStr`로 원천 차단하고, AI 초벌채점 명칭을 **`1차 채점`**으로 통일했습니다.
2. **학생 개별 성적표 A4 1장 무수정 맞춤 인쇄 및 좌우 2단 배치 최적화:**
   - 지필평가 16문항 전체의 학생 선택/입력값, 정답, 채점 결과를 온전히 보존하면서도 세로 길이를 획기적으로 줄이기 위해 좌측(Part 1 객관식 Q1~Q10)과 우측(Part 2 단답형 Q11~Q16) 2단 그리드로 개편했습니다.
   - 전체 행 수를 정확히 23행으로 압축하고 `<pageSetUpPr fitToPage="1"/>` 및 `fitToWidth="1" fitToHeight="1"` 인쇄 옵션을 적용하여 엑셀에서 바로 인쇄(Ctrl+P) 시 무수정으로 A4 1장에 완벽히 수용됩니다.
   - 셀 너비(A~F열: 14, 28, 14, 12, 26, 18) 및 라벨 셀 `wrapText="1"` 확충으로 문구 잘림 현상을 해결했습니다.
   - 우측 하단 여백(행 12~15)에 지필평가 배점 안내 카드를 병합 배치했습니다.
3. **서버 채점 점수 일괄 연동 (`class-grades` API):**
   - V4 실전평가에서 이미 제출한 반의 답안도 `class-grades` API를 통해 서버 채점 점수와 Part 1·2 문항 검토 데이터가 일괄 매핑되어 정확한 점수와 정오답이 엑셀에 반영됩니다.
4. **학생용 문항별 답안/정답 확인 모달 (`student-eval-review-modal`):**
   - 학생 제출 화면에서 `[📋 문항별 내 답안과 정답 확인]` 버튼으로 자신의 선택/입력값과 정답, 채점 결과, 해설을 열람할 수 있습니다.
   - 시험 진행 중에는 상세 정답이 공개되지 않으며, 교사가 시험을 종료한 뒤에만 열람되는 교실 치팅 방지 가드를 적용했습니다.
5. **검증 및 배포:**
   - 전체 단위 테스트 100개 100% 통과, `tools/check.cjs` 68개 스크립트 정적 검사 통과.
   - 기존 제출 답안 및 평가 데이터 무결성 100% 보존.

## 2026-09-18 처방전 팝업 HTML 중첩 오류 수정

- 사용자 승인으로 제품 `c58e95d`를 main에 fast-forward 병합하고 원격 푸시하여 기존 Vercel Git 자동 배포에 반영했습니다.
- `index.html`의 누락된 닫는 태그 1개를 복구했습니다. 숨겨진 교사 상세 팝업 내부에 처방전 팝업이 중첩되던 문제가 원인이며, 아래 이전 기록의 모달 정상 표시 주장은 정정합니다.
- 운영 고정 주소의 HTML과 로컬 수정본 일치 확인. Edge 1440×900에서 실제 표시·닫기 및 BODY 직속 배치 확인, 페이지·콘솔 오류 0건. 로컬에서는 390/768/900/1024/1440px 표시·닫기 및 예시 적용(카드 5개·단말 2개)을 검증했습니다. 실제 AI·학생 자료·평가 채점은 변경하거나 시험하지 않았습니다.

## 2026-09-18 순서도 공방 처방전 모달 네비게이션 복원 및 추상화 처방전 연계 운영 배포

수행평가(Part 3 또는 CBT 17번) 진입 후 상단 네비게이션을 통해 일반 실습(순서도 연구소 나만의 백지)으로 복귀할 때 처방전 모달이 열리지 않던 상태 누수 문제를 해결하고, Unit 1(문제 추상화) 처방전과의 직접 연계를 개선했습니다.

1. **상태 누수 해소 및 평가 작업대 자동 원상복구 (`js/core/navigation.js`):**
   - `switchUnit()`, `switchUnitStep()`, `activateLabContent()` 등 단원/실습 이동 시 목적지가 `eval`이 아닌 경우 `window.assessmentWorkspace?.leave()`를 호출하여 평가 전용 격리 모드(`assessment-workspace-mode`)를 즉시 해제하고 순서도 캔버스 작업대(`fc-level3-view`)를 원래 부모 컨테이너로 원상 복구합니다.
2. **처방전 버튼 노출 및 보안 가드 정상 해제:**
   - 평가 모드 클래스 해제로 인해 캔버스 툴바의 `[처방전 채우기]` 버튼이 정상 노출되며, `AssessmentWorkspace`의 치팅 방지 가드(`this.active`)가 정상 해제되어 처방전 모달(`prescription-modal`)이 원활하게 열립니다.
   - `AssessmentWorkspace.leave()`에 방어적 가드를 보강하여 예외 상황에서도 안전하게 원상 복구되도록 조치했습니다.
3. **Unit 1 추상화 처방전 자동 연계 (`js/labs/lab-flowchart.js`):**
   - 나만의 백지 처방전 모달의 '⭐ 나만의 문제 직접 입력' 탭 진입 시, 학생이 Unit 1 워크북에서 작성한 최신 처방전(`latestAbstractionPrescription`)이 있으면 해당 현재 상태/목표 상태를 자동으로 주입하도록 연계를 강화했습니다.
4. **검증 및 배포:**
   - `node tools/check.cjs` 구문 검사 및 `node --test tests/*.test.cjs` 94개 전체 단위 테스트 100% 통과 (복귀 후 처방전 모달 가드 해제 검증 포함).
   - Playwright 실제 브라우저 자동화 테스트를 통해 수행평가 17-2 진입 후 순서도 연구소 복귀 시 버튼 노출, DOM 위치, 처방전 모달 팝업 및 Unit 1 상태 연동 100% 정상 작동 확인.

## 2026-09-18 V4 문제은행 자연어화·문항 가독성 정밀 고도화 및 교사 백업 기능 운영 배포

선생님의 승인에 따라 1) CBT 6차 인라인 조건·알고리즘 분리 렌더링, 2) V4 문제은행 텍스트 코딩 문법의 직관적 자연어/블록 표현 전면 개정, 3) 괄호 표기 `(숫자만)`의 자연스러운 한국어 문장 정돈, 4) 교사 전용 V4 문제은행 백업 기능(`export-bank`) 구축 및 UI 버튼 반영, 5) Vercel Production 환경 변수 Gzip-base64 압축 주입 및 운영 배포를 완료했습니다.

1. **CBT 6차 인라인 조건·알고리즘 분리 렌더링 (`js/labs/lab-eval.js`의 `formatCbtPrompt`):**
   - 14번, 15번 등 문항 지문이 줄바꿈(`\n\n`) 없이 단일 단락으로 작성되었거나 평문에 인라인 수식·알고리즘이 섞여 있던 문항을 상단 발문, 중앙 독립 `.cbt-condition-box`, 하단 질문으로 3단 분리하여 시각적 위계와 가독성을 대폭 향상했습니다.
2. **V4 문제은행(80문항) 텍스트 코딩 문법 탈피 및 자연어/블록 표현 개정 (`scratch/eval_bank_v4.json`):**
   - `A = A - B`, `상자수 = 상자수 + 1, 남은 귤 = 남은 귤 - 3`, `합계 = 합계 + 숫자`, `사탕 = 사탕 - 1` 등 텍스트 코딩식 대입 연산자를 중2 학생 눈높이에 맞춘 직관적인 자연어/블록 표현(`남은 사탕에서 5개 빼기`, `상자수 1 증가, 남은 귤 3개 감소`, `[합계]에 [숫자]를 더한다`, `[숫자]를 1 증가시킨다`)으로 전면 개정했습니다.
   - 단답형 정답(2, 4, 6, 19 등)과 채점 로직, 학생 답안 무결성을 100% 보존했습니다.
3. **지문 내 괄호 표기 `(숫자만)` 자연스러운 한국어 정돈:**
   - 학생들의 오해를 유발하던 `[나이](숫자만)를 쓰시오.` 식의 어색한 괄호 표기(10개소)를 `나이를 숫자만 쓰시오.` 등 자연스러운 한국어 문장으로 일원화했습니다.
4. **교사 전용 V4 문제은행 백업 기능 (`export-bank`):**
   - 교사가 브라우저에서 최신 V4 80문항 JSON을 안전하게 다운로드할 수 있도록 교사 인증 기반 API 엔드포인트(`export-bank`) 및 UI 링크(`[⬇ 백업]`)를 신설했습니다.
   - 빔프로젝터 투사 및 학생 시선 노출을 방지하기 위해 상단 학급 선택 바의 서버 응답 표시기(`● 서버 응답 확인 중`) 옆에 은은한 텍스트 링크로 배치했습니다.
5. **Vercel Production 환경 변수 Gzip-base64 압축 주입 및 운영 배포:**
   - 80문항 JSON(73.8KB)을 gzip-base64(24.7KB)로 압축하여 Vercel 환경 변수 한도(64KB) 내에서 `EVAL_BANK_V4_JSON`을 갱신했습니다.
   - Vercel Production 배포 완료 (`https://algorithm-studio-ten.vercel.app/`).
6. **검증:**
   - `node tools/check.cjs` 구문 검사(66개 스크립트) 통과.
   - `node --test tests/*.test.cjs` 전체 단위 테스트 92개 100% 통과 (API 권한, 배정 균형, 압축 포맷, CBT 렌더링 검증 포함).

## 2026-09-18 현재 운영 상태 정정 — V4 실전평가 적용 중

선생님 확인에 따라 V4 실전평가도 현재 운영에 적용되어 사용 중인 상태로 기록을 정정합니다. V3는 모의평가 및 기존 회차 호환 경로로 유지합니다. 이 기록은 운영 적용 사실을 반영한 것이며, 회차 재현·비공개 보관·교실 전체 동시 사용·Solar 채점 타당성의 추가 검증 완료를 의미하지 않습니다.

아래 2026-09-15 기록의 “V4 문항 은행 환경 변수 등록 및 V4 회차 선택은 아직 완료가 아님”이라는 표현은 당시 시점의 상태이며, 이후 V4 운영 설정·배포 기록과 현재 운영 확인으로 대체합니다.

## 2026-09-18 V4 학생 점수 확인 UI 및 마스킹 인터랙션 고도화 운영 배포

제출 완료 화면에서 '점수 보기' 텍스트의 과도한 중복과 하단 패널에 의한 레이아웃 덜컹거림을 개선하고, 토스/애플 스타일의 프라이버시 카드 UI를 정식 반영했습니다.

1. **블라인드 마스킹 및 제자리 공개:** 평소에는 대형 점수판과 Part 1·2 세부 카드에 `🔒 • • / 60점`, `🔒 •• / 30점`으로 마스킹되어 안전하게 보호되고, `[👁️ 누르고 있는 동안 점수 확인]` 버튼을 누르고 있는 동안에만 해당 영역에서 직접 점수가 드러나며 손을 떼면 즉시 다시 마스킹됩니다.
2. **간결화 및 접근성:** 불필요하게 긴 안내문구를 정리하고, 하단 중복 텍스트 박스는 스크린리더 접근성용(`sr-only`)으로 전환하여 카드 레이아웃의 고정성과 가독성을 극대화했습니다.
3. **AGENTS.md 개정:** 정확성과 완성도를 유지하면서 토큰 효율성과 작업 소요 시간을 최적화하는 행동 규범(1.5절)을 공식 반영했습니다.
4. **검증 및 반영:** `node tools/check.cjs` 및 전체 단위 테스트 90개 100% 통과. `main` 병합 및 Vercel Production 자동 배포 완료.

## 2026-09-18 V4 학생 점수 누르는 동안 표시 개선 운영 배포

V4 제출 결과 화면에서 학생이 원할 때만 객관·단답 자동채점 참고 점수를 확인할 수 있도록 개선했습니다.

1. **서버·권한:** `/api/evaluation`에 `student-score` 동작을 추가했습니다. 익명 평가 로그인과 본인 `ownerUid`를 확인한 뒤 제출 완료 답안만 서버에서 다시 채점하고, Part 1·2 소계와 합계만 반환합니다. 정답·문항·AI 근거·다른 학생 자료는 반환하지 않습니다.
2. **학생 UI:** 제출 후 서버 채점 결과가 준비되면 `점수 보기` 버튼을 활성화합니다. 포인터 또는 키보드로 누르는 동안에만 `객관·단답 자동채점 참고 점수 X / 60점`과 Part 1·2 소계를 표시하고, 놓으면 점수 문구를 지웁니다. Part 3 AI 제안은 교사 확정 전 최종 점수로 표시하지 않습니다.
3. **검증:** `node tools/check.cjs` 통과, 인증·학급 권한·평가 API·V4 결과 UI 관련 테스트 38개 통과. 운영 정적 JS·HTML HTTP 200과 새 점수 폴링·버튼 코드 서빙을 확인했고, 새로고침한 운영 학생 탭의 콘솔 오류는 없었습니다. 읽기 전용 운영 회차에 새 V4 제출 카드가 없어 제출·초기화·채점 저장으로 자료를 만들지 않았으므로, 운영 실데이터에서 버튼을 누르는 직접 확인은 다음 V4 제출 자료에서 수행합니다.
4. **Git·운영 반영:** 코드 커밋 `559fc09`를 `main`에 fast-forward 병합하고 원격 push했습니다. 연결된 Vercel Production 고정 주소 `https://algorithm-studio-ten.vercel.app/`에서 새 정적 파일을 확인했습니다.

## 2026-09-18 V4 객관식 답안 표시 개선 운영 배포

모의평가 UI와의 일관성을 높이고, V4 객관식 서버 인덱스가 교사 화면에서 `0` 또는 `1`로만 보이던 문제를 표시 단계에서 수정했습니다.

1. **표시 수정:** 저장·채점은 기존 0-based 인덱스를 유지하고, 교사 화면은 `학생 선택: 1번 (선지 내용) / 정답: 2번 (선지 내용)` 형식으로 표시합니다. 단답형은 기존 텍스트 표시를 유지하며, 정답·오답 배지도 모의평가 UI와 맞췄습니다. 기존 답안·Firestore 자료에는 쓰기를 수행하지 않았습니다.
2. **검증:** `node tools/check.cjs`(스크립트 66개·로컬 HTML 자산) 통과, 관련 권한·평가 API 테스트 14개 통과. V4 렌더러 샘플 답안으로 선지 번호·문구와 오답 배지를 확인하고, 운영 정적 JS HTTP 200 및 새 매핑 코드 서빙을 확인했습니다. 운영 교사·학생 탭 콘솔 오류는 없었습니다. 확인 당시 운영 교사 탭은 모의평가 회차였고 V4 제출 학생 카드가 없어, 새 회차를 만들거나 답안 초기화·제출·채점 확정으로 데이터를 만들지는 않았습니다.
3. **Git·운영 반영:** 코드 커밋 `a733dc2`를 `main`에 fast-forward 병합하고 원격 push했습니다. 연결된 Vercel Production 고정 주소 `https://algorithm-studio-ten.vercel.app/`에 반영된 정적 파일을 확인했습니다. 정확한 V4 제출 카드의 시각 표시 확인은 다음 V4 제출 자료에서 추가합니다.

## 2026-09-18 교사 V4 답안 상세 조회 오류 수정 운영 배포

교사 화면에서 제출 완료 학생의 V4 Part 1·2 채점 결과와 문항 검토 자료가 표시되지 않던 오류를 수정하여 `main`에 병합하고 Vercel Production에 반영했습니다.

1. **원인 및 수정:** 화면 표시용 학급명(`2학년 1반`)이 V4 권한·평가 API가 요구하는 학급 ID(`2-1`)로 변환되지 않은 채 성적·문항 검토 요청에 전달되고 있었습니다. `js/core/classroom.js`의 두 요청만 `getClassIdFromSelected()`를 사용하도록 수정했습니다.
2. **검증:** `node tools/check.cjs` 문법·로컬 자산 검사 통과, 담당 학급·V4 평가 API 관련 테스트 14개 통과. 운영 주소의 JS에서 수정본 서빙(HTTP 200)을 확인하고, 실제 로그인된 교사 화면에서 기존 제출 답안을 다시 열어 서버 점수와 Part 1·2 문항·정답·출제 의도가 정상 표시되는 것을 읽기 전용으로 확인했습니다. 확인 과정에서 답안 초기화·채점 확정·Firestore 쓰기는 수행하지 않았습니다.
3. **Git·운영 반영:** 코드 커밋 `ed766ad`, `main` fast-forward 병합 및 원격 push 완료. 고정 운영 주소 `https://algorithm-studio-ten.vercel.app/`에서 수정된 JS와 교사 상세 조회 결과를 확인했습니다. 회차 재시험·기존 답안 보존 기능은 이번 배포 범위에 포함하지 않았습니다.

## 2026-09-17 실전평가(V4) 80문항 비공개 문제은행 구축 및 모의평가(V3) AI 채점 토큰 절약 최적화 운영 배포

선생님의 1, 2, 3단계 승인에 따라 80문항 비공개 문제은행 구축, 이원목적분류표 도메인 균형 배정 엔진 고도화, 모의평가 AI 채점 차단을 통한 토큰 절약 최적화, 프론트엔드 한글 표준 용어 일원화 및 Vercel Production 환경 변수 주입을 완료하여 정식 배포했습니다.

1. **80문항 비공개 문제은행 구축 및 Vercel Production 환경 변수 안전 주입**:
   - **Part 1 (객관식 50문항)**: D1~D5 5개 대영역별 각 10문항 (하 4, 중 4, 상 2).
   - **Part 2 (단답형 30문항)**: S1~S6 6개 세부 영역별 각 5문항 (하 1~2, 중 2~3, 상 1~2).
   - **선생님 조율안 100% 반영**: 단순 국어 맞히기 배제, 공식 기호 명칭(판단, 처리, 자료, 단말) 엄수, 미학습 용어(모델, 무한 루프, 증가, 0으로 나누기) 배제, 74번 누적합 루프 난이도 캘리브레이션(`1+2+3=6`).
   - `.gitignore`로 관리되어 GitHub에 절대 커밋되지 않으며, Vercel Production 환경 변수(`EVAL_BANK_V4_JSON`, `EVAL_ASSIGNMENT_SECRET`)로 안전하게 암호화 보관 완료.
2. **이원목적분류표 기반 도메인 균형 배정 엔진 (`server/evaluation-bank.cjs`)**:
   - Part 1은 D1~D5에서 각 2문항씩(총 10문항, 하4/중4/상2 = 30점), Part 2는 S1~S6에서 각 1문항씩(총 6문항, 하2/중2/상2 = 30점) 무작위 균등 추출.
   - 모든 학생이 예외 없이 동일한 총점(지필 60점 = 객관 30 + 단답 30)과 균형 잡힌 난이도(하6 + 중6 + 상4 = 16문항)를 배정받음. 1,000회 시뮬레이션 전수 검증 통과.
3. **AI 토큰 절약 최적화: 모의평가(V3) AI 채점 차단 & 실전평가(V4) 전용 활성화**:
   - `api/assessment.js`: `session.questionVersion !== 4`인 경우 AI 초벌 채점 거부(409 반환). 모의평가(3)에서 유료 API 토큰 낭비를 원천 차단.
   - `js/core/assessment-auto-review.js`: 실전평가(4) 회차에서만 백그라운드 AI 채점 큐 동작.
   - `js/core/assessment-review-ui.js`: 모의평가 회차는 교사 직접 채점 안내 표시, 실전평가 회차에만 Solar AI 채점 버튼 노출.
4. **사용자 친화적 한글 용어 표준화**:
   - 화면 전체에서 '모의평가 (자유 설계)'와 '실전평가 (비공개 문제은행)' 우리말 표준 명칭 적용.
   - 교사 관제탑 헤더에 회차 종류 실시간 뱃지(`teacher-session-version`) 탑재.
5. **검증 및 배포**:
   - 단위 테스트 80개 전체 100% PASS 확인 (`pass 80 / fail 0`).
   - Vercel CLI 환경 변수(`EVAL_BANK_V4_JSON`, `EVAL_ASSIGNMENT_SECRET`) Production 등록 완료.
   - `main` 브랜치 병합 및 푸시 후 Vercel Production 배포 완료 (`https://algorithm-studio-ten.vercel.app/`).
   - 라이브 사이트 실측 검증 완료 (메인 HTML 200 OK, 뱃지 렌더링 확인, API 엔드포인트 보안 인증 통과).


선생님의 실 화면 정밀 피드백을 바탕으로 CBT 단일 문항 평가 인터페이스의 시각적 완성도와 사용성을 한 단계 더 끌어올려 배포했습니다.

1. **2022 개정 교육과정 표준 용어 통일**:
   - '시작 상태' 표기를 교육과정 성취기준 및 문제 해결 표준 용어인 **'현재 상태'**(`🚩 현재 상태 (주어진 상황)`, `① 현재/목표 상태 정의`)로 전면 일원화 (17-1 본문 폼, 가이드, 채점 기준, 17-2 상단 요약 바까지 100% 동기화).
2. **문항 목록 섬과 문제 섬의 높이 100% 일치 (Pixel-Perfect Alignment)**:
   - 좌측 문항 목록 사이드바(`eval-cbt-sidebar`)와 우측 문제 섬(`eval-cbt-main`)에 동일한 데스크탑 고정 높이 규칙(`height: calc(100vh - 150px) !important; max-height: 860px !important;`)을 적용하고, 상위 컨테이너의 잔여 패딩 및 min-height 충돌을 제거하여 상단(Top: 189px)과 하단(Bottom: 939px) 모두 1px 오차 없이 완벽한 수평선 일치 달성.
3. **17-1 카드 상단 수평 광학 정렬 (Optical Alignment)**:
   - 좌측 [도전 과제] 카드와 우측 [나의 문제 해결 계획] 카드의 18px 마진 상속 버그를 교정하여 두 섬의 상단 시작 높이 완벽 일치.
4. **전체 문항 조건 카드형 표준화 (`.cbt-condition-box`) 및 시각 계층 최적화**:
   - 7번, 10번, 15번 등 조건/규칙이 있는 모든 문항에 연한 블루 틴트와 클립보드 아이콘 뱃지(`지켜야 할 규칙 / 조건`)를 갖춘 독립 카드 적용.
   - 폰트를 16px+ 볼드(`text-base`/`1.025rem font-bold`)로 시원하게 키우고 불필요한 대괄호 머리말 태그 자동 정제.
5. **순서도 조립(17-2) 섬 높이 고정 해제 및 상단 12px 밀림 결함 완벽 교정**:
   - 17-2단계 진입 시 860px 높이 제한 및 overflow hidden을 해제(`cbt-step2-unclamped`)하고 캔버스 작업 공간을 `720px`로 대폭 확장.
   - 클래식 모드에서 상속되던 `#eval-shared-workspace`의 `margin-top: 12px;`를 `margin: 0 !important;`로 제거하여 **상단 12px 빈 공간(들뜸)을 완전히 없애고, 하단의 도구/안내 문구 잘림을 0px로 완벽 밀착 마감**.
6. **단답형(11~16번) 자동 포커스 & Enter 키 지원**:
   - 단답형 문항 진입 시 정답 입력창에 즉시 자동 포커스 및 커서 배치(마우스 클릭 불필요), Enter 키 입력 시 다음 문항으로 쾌속 자동 이동.
7. **토스/애플 스타일 마이크로 인터랙션**:
   - 버튼/선지 스프링 터치 반응(`active:scale-96`, `cubic-bezier`) 및 문항 이동 시 부드러운 페이드인 트랜지션(`.cbt-smooth-enter`) 적용.
8. **검증 및 배포**:
   - CBT UI/UX 전용 단위 테스트 5건 추가, 전체 77개 단위 테스트 100% PASS 확인.
   - Playwright 실측 브라우저 검증(Top/Bottom 189/939px 일치, Q14 포커스 및 Enter 전이, 17-2 캔버스 720px 밀착) 완료.
   - `main` 브랜치 병합 및 Vercel Production 자동 배포 완료 (`https://algorithm-studio-ten.vercel.app/`).

## 2026-09-17 CBT 2차 UI/UX 고도화 (지필 2열 분할·슬림 레일 트랜지션·캔버스 완벽 밀봉) 운영 배포

선생님의 실 화면 피드백을 바탕으로 KERIS 표준 CBT 1문항 평가 인터페이스를 지필고사 2열 분할 구조와 슬림 레일 트랜지션으로 전면 고도화하고, 엣지 케이스 안전성을 대폭 강화하여 배포했습니다.

1. **지필고사형 좌우 2단 분할 레이아웃 (`.cbt-question-grid`)**:
   - 9번 문제처럼 긴 조건/규칙이 있는 문항도 좌측(발문 + 번호 뱃지 + 컴팩트 조건 상자 + 안내 띠지)과 우측(1~5번 보기 선지 or 단답형 입력창)으로 독립 분할.
   - 문제 길이가 길어져도 보기가 아래로 밀리지 않아 **스크롤 제로(Zero-Scroll)** 및 **하단 고정 버튼 위치 100% 불변 유지** 달성.
   - 우측 선지 컨테이너에 `margin: auto 0;` 안전 정렬을 적용하여 작은 화면에서도 1번 보기가 위로 잘려 스크롤되지 않는 flex-center 오버플로우 브라우저 버그를 원천 차단.
2. **문항목록 사이드바 [슬림 레일(Rail) 스무스 축소 트랜지션 & 사용자 선호 유지]**:
   - 접기(`<`) 버튼 클릭 시 사이드바가 56px 슬림 레일 바로 부드럽게(`transition 0.3s cubic-bezier`) 축소.
   - 펼치기(`>`), 현재 문항 번호 뱃지(`eval-cbt-rail-cur-badge`), 미니 응답 비율(`eval-cbt-rail-ratio`)이 항시 표시되며 즉시 원상 복구 가능.
   - **학생의 사이드바 접기/펼치기 선호(`userSidebarCollapsed`)를 기억**하여 문항 이동이나 새로고침 시에도 유지되며, 17-2단계 자동 축소 후 복귀 시에도 학생의 원래 설정으로 자연스럽게 복원.
3. **순서도 조립(17-2) 캔버스 섬 완벽 밀봉 & 작업 공간 극대화**:
   - `#eval-cbt-workspace-host` 내부 `#fc-level3-view` 및 `.entry-studio-container`에 `height: 100% !important; min-height: 0 !important; overflow: hidden !important; rounded-2xl`을 적용하여 도트 그리드가 둥근 배경 밑으로 튀어나오는 현상을 완벽 밀봉.
   - 캔버스 밑 중복 버튼 줄을 정리하여 순서도 조립 공간을 35px 추가 확보.
4. **검증 및 배포**:
   - CBT UI/UX 전용 단위 테스트 13건 포함, 전체 72개 단위 테스트 100% PASS 확인.
   - 커밋 `7ad7d07` main 병합 및 Vercel Production 자동 배포 완료 (`https://algorithm-studio-ten.vercel.app/`).
   - 라이브 사이트 다이렉트 검증으로 정적 에셋 서빙 정상 확인 완료 (`Contains rail: true, Contains 2col: true`).

## 2026-09-17 교사 관제탑 백그라운드 AI 자동 초벌 채점 큐 및 NEIS 친화적 CSV 개선 운영 배포

1. **교사용 백그라운드 AI 자동 초벌 채점 큐 (`AssessmentAutoReviewQueue`)**:
   - 학생 제출(`submitted`) 시 교사 관제탑 백그라운드 큐에서 순차적으로 Solar AI 초벌 채점(`review.proposal`)을 자동 요청.
   - 분당 10회 Rate Limit 준수를 위해 최소 6.8초 안전 간격 보장 (429 에러 원천 방지).
   - 순서도 블록 0~1개인 빈 답안은 API 미호출 0점 즉시 제안 (토큰/비용 100% 절감).
   - 관제탑 상단 실시간 진행 배지 및 학생 카드별 AI제안/확정 배지 표시. 교사 최종 확정 원칙 유지.
2. **성적표 CSV 내보내기 개선 (NEIS 양식 친화)**:
   - Part 1 + Part 2 합산값인 **`지필소계/60`** 열 신설.
   - 응시상태 한국어화(`제출완료`, `풀이중`, `대기중`) 및 시각 포맷팅(`YYYY-MM-DD HH:mm`)으로 엑셀 열 너비 깨짐(`###`) 원천 차단.
3. **검증 및 배포**:
   - 커밋 `3829d56` main 병합 및 Vercel Production 배포 완료.

## 2026-09-17 교사용 좌석 비우기, 강제 정상 제출 및 평가 종료 시 일괄 마감 운영 배포 (S-PLAN 국가수준 CBT 표준 반영)

교실 현장에서 번호 오입력, 기기 꺼짐, 학생 창 닫기로 인해 발생하는 유령 좌석 점유 및 미제출 고착 현상을 해결하기 위해 서울시교육청 S-PLAN 국가수준 CBT 감독관 매뉴얼 원칙을 적용하여 운영 배포했습니다.

1. **Firestore 보안 규칙 클라우드 배포**:
   - `firestore.rules`에서 교사(`teacherForClass(classId)`)가 세션 상태(`waiting`, `in_progress`, `ended`)에 관계없이 학생 좌석 문서를 삭제할 수 있도록 `allow delete` 규칙을 완화하고, `firebase deploy --only firestore:rules` 명령으로 `donghyun-algo` 프로덕션 DB에 배포 완료.
2. **교사용 좌석 비우기 (유령 좌석 / 결시 퇴장) 기능 (`clearStudentSeat`)**:
   - 번호 오입력 또는 중복 로그인으로 꼬인 유령 좌석을 교사가 학생 상세 모달에서 즉시 삭제하여 빈자리(미접속)로 반환.
   - 학생 브라우저가 살아있는 경우 좌석 초기화를 실시간 감지하여 대기/로비 화면으로 안전하게 복귀.
3. **교사용 현재 답안 정상 제출 기능 (`forceSubmitStudentExam`)**:
   - 창을 닫았거나 기기가 꺼져 미제출(`in_progress` 또는 `waiting`) 상태로 멈춘 학생에 대해 교사가 현재 작성된 답안을 바탕으로 즉시 `submitted`로 마감 및 자동 채점 산출.
   - 학생 브라우저가 살아있는 경우 시험 종료 및 결과 화면으로 실시간 전환.
4. **평가 종료 시 미제출 학생 일괄 마감 (`autoSubmitRemainingStudents`)**:
   - 교사가 [평가 종료] 클릭 시 미제출 학생 수를 감지하고, 안내 대화상자를 거쳐 미제출 학생 전원을 현재 답안으로 일괄 정상 제출 처리 후 평가 종료.
5. **검증 및 배포**:
   - `tests/rounds.test.cjs`에 3개 신규 단위 테스트 추가, 53개 전체 단위 테스트 통과 (PASS).
   - 제품 `ee35dd0` main fast-forward 병합 및 Vercel Production 푸시 완료.
   - 실제 운영 주소 `https://algorithm-studio-ten.vercel.app/index.html`에서 신규 기능 정적 에셋 서빙 확인 완료.

## 2026-09-15 V4 수행평가 P0 Firestore 보안 규칙 운영 게시

`codex/secure-assessment-integrity`의 V4 보안 경계를 운영 준비로 반영했습니다.

1. `donghyun-algo`의 기본 Firestore Native Standard DB(`asia-northeast3`)를 Firebase CLI로 확인했습니다.
2. Windows 시스템 신뢰 인증서를 사용하는 Node 실행으로 TLS 검증을 유지했습니다. `firebase deploy --only firestore:rules --dry-run`에서 규칙 컴파일을 확인한 뒤, 동일한 `firestore.rules`를 실제 게시했습니다.
3. V4에서는 학생이 `answers.assignedQuestions`를 저장할 수 없으며, 정답·유사 정답·배정 비밀값은 Firestore에 저장하지 않습니다. 기존 V3의 `assignedQuestions`, 화면 이탈 횟수(`blurCount`) 및 제출 흐름은 유지합니다.
4. 제품 `7c4f1ec`과 배포 기록 `c48e232`을 main에 fast-forward 병합하고 GitHub에 푸시했습니다. 기존 운영 주소 `https://algorithm-studio-ten.vercel.app/api/evaluation`는 2026-09-15 KST에 `405 POST 요청만 사용할 수 있습니다.`를 반환해 새 서버 API가 Vercel Production에 반영된 것을 확인했습니다.
5. 실제 V4 문항 은행 환경 변수 등록, V4 회차 선택과 실사용 권한 검증은 아직 완료가 아닙니다. 운영 데이터·학생 답안은 생성·수정·삭제하지 않았습니다.

## 2026-09-14 문제은행 추출 버전 3 연동, blurCount 보안 규칙 허용 및 학생 제출 실패 해소 운영 배포

사용자의 실시간 시험 테스트 보고("두 학생의 문제가 똑같은디??!", "시간이 다 지나기 전에 제출하려니까 사진과 같은 문구가 뜨네")에 따라 문제 원인을 정밀 진단하고 즉시 핫픽스를 배포했습니다.

1. **문제은행 버전 정합성 복구 (`questionVersion >= 3`)**:
   - `eval-service.js` 및 `lab-eval.js`의 문제은행 추출 조건을 `questionVersion >= 3`으로 수정하여, 운영 세션(버전 3)에서 학생별 고유 시드(`attemptId + '_' + classId + '_' + studentNum`) 기반으로 난이도 78점 동등 16문항이 학생마다 상이하게 정상 추출되도록 복구.
   - 기존 입장 학생에 대한 자동 백필(Backfill) 및 교사 모달에서의 결정론적 복원 로직 추가.
2. **화면 이탈 감지(`blurCount`) 보안 규칙 허용 및 조기 제출 오류 해소**:
   - 학생이 화면을 이탈할 때 `this.answers.blurCount`가 추가되면서 `firestore.rules`의 화이트리스트 검증 실패로 HTTP 403 `PERMISSION_DENIED`가 발생하던 버그를 해결.
   - `firestore.rules`의 `validAnswers()`에 `'blurCount'`를 추가하고, `validProgress()` 상한을 여유 있게 확장(30)하여 Firebase CLI를 통해 `donghyun-algo` 클라우드에 배포 완료.
   - 실제 라이브 DB REST 진단을 통해 `blurCount` 포함 시 403 거부되던 것이 200 정상 저장 및 정상 제출로 완벽 복구됨을 실측 검증.
3. 제품 `2283913`, `987661d`, 운영 병합 및 Vercel Production 배포 완료. 단위/회귀 테스트 44개 전체 통과.

## 2026-09-14 48문항 문제은행 고도화, 난이도 캘리브레이션 및 디벗 터치 최적화 운영 배포

사용자 승인("지금까지의 작업내용을 다른 환경에서도 테스트해보게 vercel배포까지도 완료해줄래?")에 따라 `codex/classroom-session-and-submit-polish`의 모든 변경사항을 `main`에 병합하고 Vercel Production 운영 배포를 완료했습니다.

1. **48문항 고품질 문제 은행(Question Bank) 구축 및 난이도 캘리브레이션 (`js/data/eval-question-bank.js`, `docs/QUESTION_BANK.md`)**:
   - 객관식(Part 1)과 주관식(Part 2) 정답 중복 및 힌트 누출 원천 차단 (Part 2 [하]는 실생활 레시피·기호 역할 식별 문항으로 전면 개편).
   - 4대 순서도 기호 보기 표기 100% 표준화 (`단말 기호 (타원)`, `자료 기호 (평행사변형)`, `판단 기호 (마름모)`, `처리 기호 (직사각형)`).
   - 컴퓨팅 사고력 기반 상태 추적(Trace Table) 문항 구현 (층간 이동 증분, 경계 탈출 시점의 나머지 잔여량 계산, 가중치 누적).
   - 다중 규칙 비문학 실생활 시나리오 고도화 및 **선지 동형성(모든 후보 5글자 일치, `p2_h_5` '정상감시중' 5글자 반영)**으로 글자 수 꼼수 배제.
   - 주관식 placeholder 중립화 (`"정답 입력"`으로 통일) 및 변수(Variable) 표현 완전 배제.
   - 2022 개정 교과서 알고리즘 5대 조건(입력, 출력, 명확성, 유한성, 수행가능성) 체계적 반영.
   - 슬롯별 난이도 점수(`difficultyScore`) 정밀 캘리브레이션을 통해 **전교생의 16문항 총 난이도 점수(78점) 100% 수학적 동등화** 달성.
2. **학생 시험지 화면 줄바꿈 가시성 및 카드 균형 CSS 적용 (`js/labs/lab-eval.js`, `css/studio-ui.css`)**:
   - 지문 `<p>` 태그에 `whitespace-pre-line`을 적용하여 지문의 [규칙 1], [규칙 2], [규칙 3]이 실제 줄바꿈되어 쾌적하게 렌더링되도록 개선.
   - `.eval-question-card`에 `justify-content: space-between`, 입력창 컨테이너에 `mt-auto pt-2`를 적용하여 문항 길이 편차에도 안정적인 바닥 정렬 유지.
3. **학교 디벗(갤럭시 탭 11~12인치) 터치/뷰포트 최적화**:
   - 가상 키보드 가림 방지 여백(`scroll-margin-bottom: 240px`, `interactive-widget=resizes-content`), 당겨서 새로고침 방지(`overscroll-behavior-y: contain`), 캔버스 터치 제스처 잠금(`touch-action: none`), 기호 원터치 추가, 자석 스냅(Magnet Snap), 화면 이탈 감지 안내 배너 적용.
4. **교사용 대기실 관제 및 학생 좌석 사용성 개선**:
   - 개별 대기실 닫기 및 안전 2단계 확인 일괄 닫기, 학생 자율 대기실 퇴장(좌석 점유 해제), 빔프로젝터 점수 블라인드 모드, 학생 모달 순서도 2D 캔버스 렌더링 및 크게 보기 라이트박스, 블록 실행 순서 정렬(`sortBlocksByExecution`).
5. 제품 `9efb550`, 운영 병합 `f4ede83`. Vercel Production `https://algorithm-studio-loxzopupa-donghyun2.vercel.app`, deployment `6437056775` success.
6. 고정 운영 주소 `https://algorithm-studio-ten.vercel.app/`에서 실제 정적 에셋 서빙(`difficultyScore`, `정상감시중`, `whitespace-pre-line` 포함) 정상 일치를 확인 완료. 문법 검사 58개, 단위/API 검사 43개 전체 통과.

## 2026-09-14 나만의 백지 AI 검사 캐싱 및 수행평가 운영 고도화 배포

사용자 승인("검토하고 왔어. 병합과 배포 두과자!!")에 따라 `codex/eval-and-review-polish`의 모든 변경사항을 `main`에 병합하고 운영 배포했습니다.

1. **'나만의 백지' AI 검사 결과 캐싱**: 피드백 모달을 닫은 뒤 순서도/기획서에 수정이 없으면 직전 AI 검사 결과를 0ms / 0토큰 소모로 즉각 재표시하며, 상단 알림 배지와 함께 `[AI에게 새로 검사 요청]` 버튼을 제공합니다.
2. **수행평가 대기실 기본 OFF(`ended`) 정책 및 실시간 상태 배지**: 교사가 '평가 준비'를 열기 전에는 타 반 임의 대기를 원천 차단하고, 학생 로비 화면에서 Firestore 실시간 리스너를 통해 학급별 상태(`대기실 열림 (입장 가능)` / `평가 진행 중` / `대기실 닫힘 (선생님 준비 대기)`)를 배지로 안내합니다.
3. **교사용 학생 모달 상세 답안 복원 & 실시간 스코어보드**: 문항 데이터 전역 바인딩을 복구하여 Part 1(객관 10문항) 및 Part 2(단답 6문항) 학생 입력 답안, 정/오답 판정 및 정답 해설, 배점을 완벽히 렌더링하고 모달 상단 3열 요약 스코어보드(Part 1, Part 2, 자동채점 소계) 및 좌석 카드 실시간 진행/점수를 표시합니다.
4. **수행평가 Part 3 Solar AI 채점 프롬프트 엄격성 강화**: 중2 총괄평가에 걸맞은 4단계 점수 앵커(9~10점 탁월, 7~8점 보통, 4~6점 미흡, 1~3점 불완전, 0점 미작성)를 명시하고 기획서-순서도 일치성 및 미연결 노드/분기 방치에 대한 엄격 감점을 적용하되 타당한 루프백 반복 구조는 정상 동작으로 보호합니다.
5. 제품 `b0578b8`, 문서/도구 `4de3578`, 운영 병합 `dd28f17`. 전체 57개 스크립트 문법 검사 및 37개 단위 테스트 전체 통과.


## 2026-09-14 AI 일일 사용량 1,000회 상향 및 액셔너블 에러 안내 운영 배포

사용자의 보고("지금 vercel로 접속하면 ai호출이 안되는데 토큰을 다 쓴건가?")에 따라 점검한 결과, Upstage Solar AI 토큰 소진이 아닌 Firestore 내부 일일 안전 쿼터(`ai_usage/20710`) 상한선(300회)에 도달하여 HTTP 429가 발생하고 있던 원인을 확인했습니다. 사용자 승인에 따라 다음 작업을 완료하여 배포했습니다.

1. `server/ai-quota.cjs`: 학급 규모(11개 반 × 최대 27명) 실습 및 재검사 수요를 안정적으로 지원할 수 있도록 기본 한도 및 최소 클램프를 1,000회(Firestore 보안 규칙 `firestore.rules`의 최대 허용치)로 보장. 오늘 이미 300회를 채운 Firestore 카운터에서도 즉시 700회의 추가 여유 슬롯을 확보.
2. `js/labs/lab-flowchart.js`: AI 일일 사용량 도달(`isQuotaError`) 또는 단시간 호출 집중 대기(`isRateLimit`) 시, 단순 네트워크 오류 문구로 가려지지 않고 "오늘의 AI 도움 사용량 안내" 또는 "잠시 대기 안내" 전용 배지와 함께 교사 문의 안내 및 자체 검토를 유도하는 친절한 액셔너블 한국어 UI로 개선.
3. 제품 `8d0a474`, 운영 병합 `1611d19`. Vercel Production `algorithm-studio-7ygy43s9q-donghyun2.vercel.app`, GitHub deployment `6430526286` success. 고정 운영 주소 `https://algorithm-studio-ten.vercel.app/`에서 실제 익명 인증 기반 `/api/chat` 호출 시 HTTP 200("안녕하세요! 무엇을 도와드릴까요?") 응답 및 Firestore 카운터 정상 증가(300 → 301)를 직접 확인 완료. 문법 검사 56개, 단위/API 검사 37개 전체 통과.

## 2026-09-14 나만의 백지 AI 검사 프롬프트 고도화 및 운영 배포

사용자 피드백에 따라 '나만의 백지' 실습의 Solar AI 검사 기준을 교육적 스캐폴딩과 학생 성취감 관점에서 고도화하여 배포했습니다.
1. 자연어 카드 1단계 ↔ 순서도 2~3개 등 단계 수준 차이가 날 때는 오류로 단정하지 않고 순서도의 구체화를 칭찬하며 기획서 카드를 추가해 짝을 맞추도록 친절한 보완 피드백을 안내합니다.
2. 자료(입출력 ▱)와 처리(직사각형 ▭) 기호의 모호함을 관용하여 둘 중 어느 기호를 사용해도 의미와 흐름이 통하면 적극 통과시킵니다.
3. 단일 선택(else 없는 if) 및 일상적 반복 표현(변수 증가식 없는 루프백)을 정상적인 알고리즘으로 허용합니다.
4. AI의 그래프 탐색 추론을 지원하기 위해 payload에 한 줄 텍스트 흐름 요약(`flowchartFlowSummary`)을 추가하고, 마크다운 볼드/헤딩을 유연하게 수용하도록 판정 파서 정규식을 보완했습니다.

제품 `7f5115a`, 운영 병합 `f25d652079955a253a98467e52bbea2fda6ad78c`. Vercel Production `algorithm-studio-1kq0enbfp-donghyun2.vercel.app`, deployment `6429207125` success. [GitHub 검사](https://github.com/DonghyunT/algorithm-studio/actions/runs/34798510785) success. 고정 운영 주소 `https://algorithm-studio-ten.vercel.app/`에서 새 프롬프트 및 흐름 요약 자산 일치를 확인했습니다. 문법 검사 56개, 단위/API 검사 37개 전체 통과.

## 2026-09-14 로그인 문구·비밀번호 변경

사용자 요청으로 ‘비밀번호 로그인’ / ‘교사 비밀번호’ 문구와 단어 단위 줄바꿈을 적용했습니다. 운영 강사 비밀번호를 지정한 값으로 변경하고 Git·배포 제외 `.env.instructor.local`을 동기화했습니다. 비밀번호 값은 기록하지 않으며 담당 반 권한·학생 자료는 유지했습니다.

제품 `d451627`, 운영 병합 `2ba97664fbfda13db5c42c8a62bd74263b95a6ad`. Vercel Production `algorithm-studio-dxew0psvw-donghyun2.vercel.app`, deployment `6423612288` success. 문법 검사와 로컬 UI 5흐름, 운영 UI 5흐름 통과: 320/390/420px에서 ‘로그인’ 분리 없음, 중립적 문구, 변경 비밀번호 로그인, 담당 반·복원·로그아웃 확인. 페이지·콘솔 오류 없음. [운영 결과](../tests/results/instructor-production/results.json). 후속 기록 커밋은 제품 코드가 동일합니다.

## 2026-09-14 강사 담당 반 권한·AI 점검 개선 운영 배포

**최종 보완 배포:** 공유 PC 인증 취소·권한 오류 시 학생 자료 제거를 제품 `5affdfd`, 운영 병합 `b530f1d21703fe3a7fdec01041170578a4c66efc`로 반영했습니다. Vercel Production `algorithm-studio-19tkv82od-donghyun2.vercel.app`, deployment `6423501427` success. [GitHub 검사](https://github.com/DonghyunT/algorithm-studio/actions/runs/34767303530) success. 단위/API 37개와 관련 교사 UI 18흐름, 기본 UI 24·학생 평가 10개를 재검증했습니다. 실제 운영 강사 UI 4흐름도 다시 통과했고 페이지·콘솔 오류가 없으며 최종 인증·교사 JS가 로컬 코드와 일치했습니다. Firebase 역할·보안 규칙은 최초 배포 이후 바꾸지 않았습니다. 후속 문서 커밋은 제품 코드가 동일합니다.

* 사용자 승인에 따라 기존 선생님 Google 계정은 전체 반, 강사님 비밀번호 계정은 10·11반으로 설정했습니다. Firestore 규칙 컴파일·배포와 이메일/비밀번호 공급자 활성화를 완료했습니다. 계정 상세와 비밀번호 전달 방식은 [강사 계정 기록](INSTRUCTOR_ACCESS_REVIEW.md)에 있습니다. 비밀번호 값은 Git·문서·배포에서 제외합니다.
* 제품 `a1737d2`, 운영 병합 `b0bd14e6ed143aff745457a11b047b81d36e06cf`. 직전 로컬 제품 `46b644b`와 `3842f58`도 함께 반영했습니다. 튜터 로봇 숨김·펼침, 교사 로그인 선택과 수행평가 진입, 실습 AI 통과 후 제출, 의미 기반 AI 점검, 변수·콘솔 영역 개선을 포함합니다.
* Vercel Production `algorithm-studio-gji3ourif-donghyun2.vercel.app`, GitHub deployment `6423414156`의 success와 정확한 병합 SHA를 확인했습니다. [GitHub 검사](https://github.com/DonghyunT/algorithm-studio/actions/runs/34766839896) success.
* 2026-09-14 00:53 KST [기존 운영 주소](https://algorithm-studio-ten.vercel.app/)에서 실제 강사 로그인 성공, 담당 10·11반만 표시, 다른 반 전환 거부, 새로고침 후 수행평가→교사 화면 복원, 로그아웃을 확인했습니다. 1440/1024/768/390px에서 주요 조작·가로 넘침 확인, 페이지 예외·콘솔 오류 0개. [운영 UI 결과](../tests/results/instructor-production/results.json).
* 실제 Firestore 및 운영 API 권한 검사 13개 통과. 담당 반 접근 허용, 다른 반 학생 목록·기존 답안·학급 정보·보관 문서 생성 거부, 학생 익명 대기실 유지, 다른 반 AI 평가 요청의 유료 호출 전 거부를 확인했습니다. 고유 검증 문서와 익명 계정 정리 완료. 기존 학생 답안·성적·평가 상태는 변경하지 않았습니다. [권한 결과](../tests/results/instructor-live.json).
* 운영 HTML/CSS/JS 7개가 검증본과 일치했습니다. `.env.instructor.local`, `.env.preview.local`, `tools/preview.cjs`는 운영 주소에서 404이며 로컬 체험 모드는 비활성임을 브라우저에서 확인했습니다. 브라우저용 로컬 어댑터 코드는 배포물에 있으나 로컬 서버가 주는 설정 없이는 동작하지 않습니다.
* 배포 전 단위/API 36개, 기본 UI 24·작업 공간 5·수행평가 10·교사 제어 8·튜터 로그인 6·담당 반 UI 4흐름 통과. 수행평가 검사에서 스크린샷 1건의 시간 초과가 있었으며 해당 검사를 단독 재실행해 10개 모두 통과했습니다. 앞선 실제 Solar 5개 사례는 [의미 검사 기록](SEMANTIC_REVIEW_LOCAL_PREVIEW.md)에 보존합니다. 이번 배포 검증에서 불필요한 유료 AI 호출은 추가하지 않았습니다.
* 선생님 계정은 관리자 조회로 기존 Google UID와 전체 반 역할을 확인했습니다. 선생님의 Google 팝업 재로그인 및 실제 학급 동시 수업은 이번 자동 검증 범위 밖입니다. 뒤따르는 기록 커밋은 제품·권한 규칙을 변경하지 않습니다.

## 2026-09-13 교사 평가 버튼 통합·노트북 인수인계

* 사용자가 다른 PC 인수인계 준비·안내를 먼저 마친 뒤 구현·검증·병합·배포하도록 승인했습니다. `docs/HANDOFF.md`와 README·개발 문서를 먼저 main `f174499`에 반영하고 안내했습니다.
* 제품 `363a49a`, main 병합 `dfdf7e067c9ab6cf61fd8ee610d3f9d27a020515`. 준비·시작·종료 버튼을 상태에 맞는 단일 버튼으로 통합하고 상태·시간·입장·제출 인원을 분리했습니다. [구현·검토 기록](TEACHER_CONTROLS_REVIEW.md).
* [GitHub main 검사](https://github.com/DonghyunT/algorithm-studio/actions/runs/34726788843)는 success입니다.
* 문법·참조 43개 스크립트, 기본/API 21개, 교사 UI 8개, 학생 평가 회귀 10개 통과. 페이지 예외·콘솔 오류 없음. 운영 DB 답안·회차·보안 규칙·인증·환경 변수는 변경하지 않았습니다.
* Vercel GitHub 연동 Production 배포 `algorithm-studio-7jskw5vad-donghyun2.vercel.app` 성공. GitHub deployment `6415770196`의 `success` 상태와 병합 SHA 일치를 확인했습니다. 이 PC의 Vercel CLI 조회는 ByteString 오류로 실패하여 GitHub 상태 API와 운영 파일 비교로 검증했습니다.
* 2026-09-13 08:59 KST [기존 운영 주소](https://algorithm-studio-ten.vercel.app/) 재확인: HTML·CSS·JS 15개가 제품과 일치, 교사 단일 버튼 마크업 반영, 1440/1024/390px 주요 탐색·평가 입장 동작 통과. API 메서드 405·비인증 401 확인, 페이지·콘솔 오류 0개. [운영 결과](../tests/results/teacher-production.json), [운영 홈](../tests/results/teacher-production-home.png), [좁은 창](../tests/results/teacher-production-390.png).
* 실제 교사 계정으로 운영 시험을 시작·종료하지 않았습니다. 상태 전환·동시 화면 변경·실패 복구는 로컬 시연과 트랜잭션 모의 검사로 검증했습니다. 이후 기록 커밋은 제품 파일을 변경하지 않습니다.

## 2026-09-13 작업 공간·수행평가 자유 설계 운영 배포

* 사용자가 빌드 완료 후 간단한 재검증, main 병합 및 운영 배포를 명시적으로 승인했습니다.
* 제품 커밋은 작업 공간 `432a4fd`, 수행평가 편집기·문항 `24bdc46`, 자유 설계·단일 실행·Solar 초벌 검토 `0d777f4`입니다. 읽기 전용 DB 점검 도구 `c456626`과 함께 main `ade1f7b6ebbe8069814c60e1aca9d220b4a57af5`로 병합·푸시했습니다.
* Vercel Production `dpl_4abR1eUeyny2zEZmh5fULViusUEA`, `algorithm-studio-aqf1ylexa-donghyun2.vercel.app`이 Ready이며 기존 [운영 주소](https://algorithm-studio-ten.vercel.app/) 연결을 확인했습니다. `/api/chat`과 새 `/api/assessment`가 함께 배포되었습니다.
* [GitHub main 검사](https://github.com/DonghyunT/algorithm-studio/actions/runs/34704645959)는 success입니다. 로컬 문법 검사 42개와 기본/API 검사 20개를 배포 전 재실행하여 통과했습니다. 앞선 로컬 브라우저 검증은 일반 23개·작업 공간 5개·수행평가 10개를 통과했습니다.
* 2026-09-13 01:19 KST 운영 확인: HTML·CSS·JS 15개가 로컬 제품과 일치합니다. Edge 1440/1024/390px에서 홈·개념·전체 메뉴 키보드 조작·교사 버튼 접근·평가 입장을 확인했습니다. 페이지 예외·콘솔 오류는 0개입니다. 새 API는 GET 405, 인증 없는 POST 401로 응답했습니다. [검증 결과](../tests/results/assessment-production.json), [운영 홈](../tests/results/assessment-production-home.png), [좁은 창](../tests/results/assessment-production-390.png).
* Firebase 11개 학급과 보관 자료를 읽기 전용으로 확인했습니다. `students`는 현재 회차, `archives/<회차>/students`는 이전 기록이며 회차 불일치는 없었습니다. 운영 규칙도 로컬 규칙과 일치했습니다. [DB 구조와 상세 근거](DATABASE.md), [조회 결과](../tests/results/firestore-structure.json).
* 기존 회차·답안·보안 규칙은 수정하지 않았습니다. 현재 진행 중인 이전 평가를 임의로 종료하지 않았습니다. **새 자유 설계 문항은 교사가 기존 평가를 종료하고 새 평가를 준비할 때 적용됩니다.**
* 이번 운영 검사는 Google 로그인·학생 입장·DB 쓰기·실제 Solar 호출을 수행하지 않았습니다. Solar 채점 품질은 모의 API 검사만으로 검증할 수 없으며 AI 제안과 교사 최종 확정은 분리되어 있습니다. 기존 감사 미해결 항목·동시 수업 운영·보관 기간 등은 계속 별도 검증 대상입니다.
* 이 기록을 추가하는 후속 커밋은 제품 코드와 배포 설정을 변경하지 않습니다.

## 이전 배포 이력

2026-09-12 · 사용자 승인에 따른 GitHub·Vercel·Firestore 배포

* 운영 주소: https://algorithm-studio-ten.vercel.app/
* GitHub: https://github.com/DonghyunT/algorithm-studio
* 배포 제품: `58f68af`, main 병합 `77294c124214278b69540312bb53678c94d162cc`
* Vercel 배포: `dpl_tkAqUtzRGVNdesRLcQpizVck8oZk`, Ready. 고정 운영 도메인 연결 확인.
* GitHub 검사: https://github.com/DonghyunT/algorithm-studio/actions/runs/34694833363 — success
* Firebase: `donghyun-algo`의 기본 DB. 새 `classrooms/2-1`~`2-11` 문서 생성. 기존 `eval_sessions`와 학생 테스트 자료는 그대로 보존.
* Google·Anonymous 공급자 활성화. 고정 Vercel 도메인, localhost, 127.0.0.1 승인 도메인 등록.
* Firestore 보안 규칙 컴파일·배포 완료.
* 운영 환경변수: 기존 `UPSTAGE_API_KEY` 유지, `FIREBASE_PROJECT_ID=donghyun-algo`, `AI_DAILY_LIMIT=300` 추가. 비밀 값은 기록하지 않음.

## 검증 근거

1. Node 테스트 12개 통과. 회차 보관, 새 학생 입장, 재시작 방지, 트랜잭션 실패 시 기존 답안 유지 포함.
2. 로컬 Edge 브라우저 시나리오 18개 통과. `tests/results/browser.json`.
3. 실제 Firestore 학생 권한 검사 13개 통과. `tests/results/firestore-live.json`. 새로 생성한 빈 2-11 학급에서 검사하고 임시 답안·Auth 계정을 제거한 뒤 대기 상태 복원.
4. 실제 운영 화면에서 시연 모드 꺼짐, 익명 로그인, 학급 조회 확인. API 요청 1회 HTTP 200 및 AI 응답 확인. `tests/results/production.json`, `tests/results/production.png`. 임시 인증 계정 제거 완료.

## 남은 확인

최초 배포 당시에는 교사 Google 로그인과 UID 역할 연결이 남아 있었습니다. 이후 실제 로그인 계정의 교사 역할 연결을 완료했고, 사용자가 로그인 성공을 확인했습니다. 이는 학급 전체의 공동 운영 검증을 뜻하지 않습니다.

실제 교사 브라우저와 학생 브라우저의 공동 운영, 27명 동시 사용, CSV의 NEIS 가져오기, 서버 보관 기간은 별도 확인 대상입니다. 원본 감사 35건 전체 해결이나 교실 운영 무결성을 선언하지 않습니다.

## 2026-09-12 UI 개편 미리보기

* 제품 커밋: `af3b2f5ba3ed099c76902d715ab1102c2be3508b`, 브랜치 `codex/responsive-studio-ui`.
* 변경·검증 내용: [UI 개편 검토서](UI_REVIEW.md). 문법·로컬 파일 참조 확인, 기본 기능 12개 및 브라우저 시나리오 23개 통과, 페이지 예외·콘솔 오류 0개.
* GitHub 검사: https://github.com/DonghyunT/algorithm-studio/actions/runs/34697423638 — success.
* Vercel 미리보기: https://algorithm-studio-nu4bl2v31-donghyun2.vercel.app/ — Ready, `dpl_ArkD7pHKrSxykmCdir3kxxA5KTip`.
* 미리보기는 Vercel 로그인 보호가 적용되어 있습니다. 실제 브라우저 접근 시 Vercel 로그인 화면을 확인했으며, 이 미리보기에서 Google 교사 인증·운영 DB 저장·AI 응답을 확인했다고 간주하지 않습니다.
* 운영 반영 승인: 자동 승인 검토가 별도 병합 승인을 요구한 뒤, 사용자가 “병합하고 기존 주소에 배포해주세요”라고 명시적으로 승인했습니다. 아래 운영 배포 결과가 기록되기 전까지 미리보기 검증과 운영 검증을 구분합니다.

### 운영 UI 배포 확인

* `42512574e0e4ee2472177bcd8b36770f30f7d513`에서 UI 브랜치를 main에 병합했습니다.
* Vercel Production `dpl_Ay8QMzDoNBrHXqBDaZ2GRatgd1UE` (`algorithm-studio-fmm3o4ygh-donghyun2.vercel.app`) Ready, `algorithm-studio-ten.vercel.app` 연결 확인.
* main GitHub 검사: https://github.com/DonghyunT/algorithm-studio/actions/runs/34697588279 — success.
* 운영 주소의 HTML·CSS·JS 7개가 로컬 검증본과 일치합니다. 1440/1024/390px에서 홈·개념·전체 메뉴 키보드 조작·교사용 버튼 접근·평가 입장을 확인했습니다. 시연 모드는 꺼져 있으며 이번 운영 검사는 학생 입장·DB 쓰기·AI 호출을 수행하지 않았습니다.
* 최초 운영 UI 확인에서는 페이지 예외는 없었으나 기존 `favicon.ico` 누락 404가 있었습니다. 외부 요청이 필요 없는 SVG 탭 아이콘으로 보완했습니다(`93e8f27`, main `3b0f6a6d2e016dece0818d67df1f597f31b1f131`). 보완 배포 `algorithm-studio-1ir2d5sk6-donghyun2.vercel.app` Production Ready.
* 최종 운영 재확인: 2026-09-12 22:55 KST, 7개 파일 일치·세 창 폭의 주요 동작 통과, 페이지 예외·콘솔 오류 0개. [검증 결과](../tests/results/ui-production.json), [운영 홈](../tests/results/ui-production-home.png), [좁은 창](../tests/results/ui-production-390.png).
* 보완 main 검사: https://github.com/DonghyunT/algorithm-studio/actions/runs/34697770550 — success. 이후 검증 기록만 추가한 커밋은 UI 제품 파일을 변경하지 않습니다.
