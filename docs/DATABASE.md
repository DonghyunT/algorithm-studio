# Firestore 운영 구조

갱신: 2026-09-24 · 설명 대상은 V4 단답형 정정 지원을 포함한 main 병합 커밋 `a7facd9`입니다. 아래 현재 구조와 과거 날짜별 기록을 구분합니다. 이번에는 승인된 운영 정정 및 원본 보존 재조회를 수행했습니다. 검증 범위는 [정정 절차](V4_OBJECTIVE_CORRECTION.md)와 [배포 기록](DEPLOYMENT.md)을 따릅니다.

## 현재 평가 버전과 복구 필드

기본 새 회차는 V4이며 교사는 V3 모의평가를 선택할 수 있습니다. V1·V2는 기존 회차 호환용입니다. V3의 `answers.assignedQuestions`와 달리 V4 문항은 서버 API에서 배정하며 해당 필드를 학생 답안에 저장하지 않습니다.

| 학생 필드 | 현재 코드의 용도 |
|---|---|
| `allowReconnect`, `reconnectAllowedAt` | 교사 재접속 허용. 재입장 시 소유 UID를 갱신하고 허용 플래그를 해제하며 기존 서버 답안 유지 |
| `makeupAllowed`, `makeupDurationMinutes` | 개별 추가 응시 대상·시간. `ownerUid: null` 초기화로 어느 PC/노트북에서든 로그인 허용 |
| `startedAt`, `deadlineMs` | 학생 개별 시작·마감 처리 |
| `review.proposal`, `review.confirmed` | AI 제안과 교사 확정 구분. 4대 평가 기준(`criteria.c1~c4`), 점수, 피드백 구조화 |
| `objectiveCorrection` | 승인된 단답형 정정의 기준 버전·변경 전후·증감·시각·사유·은행 해시·답안 식별 스냅샷. 현재/보관 학생의 원본 답안과 일치할 때만 표시 |

`allowStudentMakeup`은 미응시 결시생을 대기 상태로 구성하며, 2026-09-21부터 `forceReset: true` 옵션을 지원하여 시험 종료 시 0점으로 자동 마감(`status: 'submitted'`)된 결시생이라도 교사가 승인하면 기존 기록을 안전하게 초기화하고 30분 권한을 다시 부여합니다. 추가 응시 승인 시 `ownerUid: null`로 초기화되어 기기 무관(임의 PC/노트북) 로그인을 보장합니다. 풀던 답안을 보존하는 재접속 기능(`allowReconnect`)과 명확히 구분됩니다. 다른 학생 문서는 이 처리의 쓰기 대상이 아닙니다.

`archives`의 답안 보관과 V4 은행·배정 비밀값·채점 버전 보존은 별개입니다. 과거 회차의 완전한 재현·조회·복구는 [V4 계획](EVALUATION_V4_PLAN.md)의 후속 과제입니다.

## 2026-09-24 승인된 단답형 정정 기록

`tools/apply-objective-regrade.cjs`는 검토 계획의 해시와 원본 문서 갱신 시각을 확인한 뒤 학급별 트랜잭션으로 현재 학생의 `scores.part2`와 `objectiveCorrection`을 기록합니다. 객관식 점수가 이미 있으면 객관식·단답형 소계도 맞추지만 최종 총점을 새로 만들지 않습니다. 기존 `answers`, Part 3와 교사 검토는 보존합니다.

`classrooms/{classId}/archives/{auditId}`에는 `kind: objective-regrade`, 적용 시각, 기준 버전, 계획/은행 해시, 원본 세션을 저장합니다. 하위 `students/{num}`에는 `originalStudent` 전체 스냅샷과 변경 문항·점수·사유·원본 경로·갱신 시각을 저장합니다. 같은 계획은 중복 적용하지 않습니다. 복구 도구는 점수 필드만 복원하고 `objective-regrade-restore` 감사 기록을 추가합니다. 이 감사 기록들은 `kind: new-session`만 읽는 이전 평가 목록에서 제외됩니다.

`objectiveCorrection.sourceKey`는 회차·제출 시각·Part 1/2 응답을 정렬해 저장한 문자열이며 암호학적 해시가 아닙니다. 답안 일부가 중복 저장되므로 원래 학생 자료와 같은 접근 제한을 적용합니다. 다른 답안이나 재제출에는 과거 정정 표시를 붙이지 않습니다. 기존 Firestore 규칙의 학생 필드 제한과 보관 수정/삭제 금지는 유지했습니다. 현재 도구는 변경이 필요한 기존 보관 회차의 쓰기를 거부합니다.


## 2026-09-14 담당 교사 범위 적용

선생님 Google 계정에는 `enabled: true, allClasses: true`, 강사님 비밀번호 계정에는 `enabled: true, allClasses: false, classIds: ['2-10','2-11']`를 적용했습니다. `enabled`만 있는 역할은 새 규칙에서 교사 접근을 허용하지 않습니다. 신규 교사도 관리자 등록 시 담당 범위를 명시해야 합니다.

학생 답안·성적·이전 보관 기록과 학급 운영은 모두 담당 반으로 제한합니다. 학생 대기실 학급 조회는 익명 인증에 허용하며 학생은 자기 답안만 사용할 수 있습니다. AI 초벌 검토 API도 동일한 학급 범위를 적용합니다. 규칙 배포 전에 기존 선생님의 전체 권한을 명시했고, 규칙을 확인한 후 강사 계정을 활성화했습니다. 기존 학급 상태·답안·성적을 변경하지 않았습니다.

실제 강사 계정의 10·11반 목록 허용, 1반 목록·답안·학급 정보 거부, 보관 문서 생성 허용·거부, 익명 학생 기본 권한 총 12건을 확인했습니다. 고유한 검증용 보관 문서와 익명 인증 계정은 정리했습니다. 계정 준비 도구는 `tools/provision-instructor.cjs`이며 비밀번호는 Git·배포 제외 파일 `.env.instructor.local`에만 보관합니다. [상세 운영·검증 기록](INSTRUCTOR_ACCESS_REVIEW.md).

2026-09-13 운영 기록 · 프로젝트 `donghyun-algo` · 기본 데이터베이스 `(default)`. 자유 설계 코드의 운영 배포를 완료했습니다. 기존 회차·답안은 유지하며 새 필드는 새 평가 회차부터 사용합니다.

```text
classrooms                         학급 컬렉션
  2-1 ... 2-11                    학급 문서: schoolYear, status, attemptId, deadlineMs
    students                      현재 회차 응시 기록
      01 ... 27                   번호 문서: name, ownerUid, attemptId, answers, progress, status
    archives                      이전 답안 보관
      <UUID>                      kind, archivedAt, 당시 session
        students
          01 ... 27               보관 당시 학생 기록과 교사 조정 점수
teachers
  <Firebase Auth UID>             enabled, 관리자 등록 교사 역할
ai_usage
  <UTC 기준 일자 번호>             count: 여러 서버 인스턴스가 공유하는 AI 요청 수
eval_sessions                     이전 테스트 자료. 새 앱은 사용하지 않으며 보존함
```

학급 문서 11개를 미리 생성합니다. 학생 문서는 이름 없는 가짜 명부로 채우지 않고 실제 입장 시 생성합니다. 번호는 `01`~`27`로 통일하고 숫자 `num` 필드도 저장합니다. 이름을 문서 ID로 쓰지 않습니다. 현재 명부는 응시 회차 단위이며 학생의 영구 학적 관리 기능은 아닙니다.

학급·학생·답안의 구분은 개인정보 접근 권한과 일치합니다. 학생은 자기 UID로 예약한 번호 문서만 읽고 수정합니다. 다른 학생 목록·이전 답안·교사 역할 생성은 거부합니다. 교사는 등록된 역할로 학급을 운영합니다. 제출 이후 변경과 마감 이후 새 답안 변경은 거부하되, 서버에 이미 저장된 같은 답안의 제출 확정은 허용합니다.

새 평가 준비는 진행 중에는 허용하지 않습니다. 마감 후 교사가 선택하면 현재 기록을 `archives`에 보관하고 현재 번호 문서들을 비운 뒤 새 `attemptId`로 대기실을 엽니다. 보관·초기화를 하나의 Firestore 트랜잭션으로 처리합니다. 개별 재시험도 기존 답안을 먼저 보관합니다. 결시생은 별도 개별 추가 응시 경로를 사용합니다. 답안 초기화·재접속·새 회차 준비와 구분합니다. 교사 클래스룸의 `이전 평가 기록` 탭은 2026-09-23부터 해당 학급의 `kind: new-session` 보관 회차와 선택한 회차의 학생 스냅샷을 읽기 전용으로 조회합니다. 학생별 초기화·재접속 감사 기록은 목록에서 제외하며, 점수를 재계산하거나 보관 문서를 수정하지 않습니다. 보관 조회 추가 당시에는 자료 구조와 규칙을 바꾸지 않았습니다. 이후 추가된 정정 필드는 위 2026-09-24 절을 따르며 접근 규칙은 유지합니다. 기존 자료의 직접 확인은 Firestore 콘솔에서도 가능합니다.

브라우저 초안 키에도 회차를 포함하여 이전 평가 답안이 새 평가에 섞이지 않도록 합니다. 현재 2026학년도 설정이며 다음 학년도 명부·기록 이관은 별도 운영 절차가 필요합니다. 보관 기간은 아직 확정하지 않았으므로 자동 삭제를 설정하지 않았습니다.

AI는 UID별 분당 10회 제한과 공통 일일 제한을 적용합니다. `server/ai-quota.cjs`는 전달된 환경 변수 값과 무관하게 일일 한도를 1,000회로 고정하고 규칙 상한도 1,000입니다. 환경 변수만 수정해 한도를 바꿀 수 있다고 안내하지 않습니다. 카운터를 증가시킨 요청만 AI 서버로 전달하며, 사용량 저장에 실패하면 AI 요청도 중단합니다. 일자는 UTC 0시(한국 시각 오전 9시)에 바뀝니다. 실패한 AI 호출도 예약된 사용량에 포함됩니다. 이 카운터는 비용 제한이며 학급 구성원 확인이나 부정행위 방지 장치는 아닙니다.

실제 서버 학생 권한 검사 13개 결과는 `tests/results/firestore-live.json`에 있습니다. 기존 `eval_sessions`의 학생 자료는 수정·삭제하지 않았습니다. 새 학급 생성 도구는 `tools/provision-firestore.cjs`이며 기본은 조회만 하고 `--apply`에서 없는 학급만 생성합니다.

## 2026-09-13 자유 설계 회차 필드 (코드 배포 완료)

2026-09-13 당시 새 회차의 `questionVersion`은 3이었습니다. 현재 기본값은 4이며 3은 모의평가로 선택합니다. 버전 없는 회차는 1, 기존 고정 주제 개선 회차는 2로 보존합니다. 학생 답안 `answers.part3.plan`에 `current`, `goal`, `conditions`, `steps`를 저장합니다. 조건은 줄바꿈 문자열이며 카드 배열과 기호·연결은 기존 구조를 사용합니다.

학생 문서의 `review.proposal`은 AI의 네 항목 점수·근거·불확실성·모델·기준 버전·작성 시각을 보존합니다. `review.confirmed`는 교사가 확인한 네 항목 점수·교사 UID·확정 시각을 보존합니다. 둘 모두 `attemptId`와 당시 답안의 정규화 문자열 `sourceKey`를 기록하여 답안이 달라진 뒤 예전 점수를 적용하지 않습니다. 정규화 문자열은 개인 식별 필드와 캔버스 좌표를 제외한 설계·기호·연결의 스냅샷이므로 일부 답안 정보가 중복 저장됩니다.

AI 제안 저장만으로 성적은 확정되지 않습니다. 확정 점수가 없으면 Part 3·총점은 `null`/채점 대기로 취급하고 CSV에 0점으로 내보내지 않습니다. 교사 확정 후 Part 1·2 자동 계산과 합산합니다. 재시험 시 review도 초기화하며 이전 검토는 기존 답안과 함께 archives에 보존됩니다.

기존 Firestore 규칙은 학생의 `review` 필드 생성·변경을 허용하지 않으며 교사만 변경할 수 있습니다. 이번 구현에는 규칙 변경이나 운영 자료 쓰기가 없습니다. 현재 `/api/assessment`의 AI 초벌 채점은 V4 회차의 교사 역할과 제출 상태를 확인한 뒤 모델에 식별 정보 없이 답안 필드만 전달합니다. 조건 추천에는 현재·목표 상태만 보냅니다. 기존 `UPSTAGE_API_KEY`, `SOLAR_MODEL`, `FIREBASE_PROJECT_ID`, `AI_DAILY_LIMIT` 설정을 사용하며 공통 일일 쿼터를 소비합니다. API별 UID 분당 제한도 적용합니다.

## 2026-09-13 운영 구조 읽기 전용 점검

11개 학급을 조회했습니다. `2-1`에는 현재 회차 학생 1개와 이전 회차 보관 문서 1개가 있으며, 그 보관 문서 아래 `students`에는 당시 기록 2개가 있습니다. `2-3`에는 현재 대기 회차 학생 1개가 있고 다른 학급은 비어 있습니다. 현재·보관 학생의 회차 ID가 각각 소속 회차와 일치하며 구조 불일치는 발견하지 못했습니다. `2-1`은 기존 진행 중 상태이고 다른 학급은 대기 상태입니다. 이번 점검으로 종료·초기화하지 않았습니다.

`students`와 `archives`가 같은 학급 문서 아래 보이는 것은 정상입니다. 전자는 현재 답안, 후자는 이전 회차를 보관합니다. 보관 문서 안의 `students`는 중복 명부가 아니라 해당 시점의 답안 스냅샷입니다. 두 단계의 학생 기록은 용도와 접근 권한이 다릅니다.

운영 Firestore 규칙 원문은 로컬 `firestore.rules`와 일치했습니다. 보관 자료는 교사만 읽을 수 있도록 정의되어 있습니다. 이는 규칙 원문 확인이며 이번에 실제 학생 계정으로 모든 거부 사례를 다시 실행했다는 의미는 아닙니다. 기존 `eval_sessions` 문서 2개도 보존했습니다. [조회 결과](../tests/results/firestore-structure.json), 재실행 도구 `tools/audit-firestore-structure.cjs`.
