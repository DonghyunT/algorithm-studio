module.exports=require('./assessment-prompt.cjs')+`

### 연결 사실을 먼저 확인하고 감점 항목을 분리하라
graphFacts.paths는 실제 fromPort를 해석한 경로다. 예(참)와 아니오(거짓)를 바꾸어 읽지 않는다. 블록의 이름만 보고 연결도 옳다고 가정하지 않는다.
graphFacts.connectedNonterminalDeadEnds는 주요 흐름에 연결되어 있으나 나가는 선이 없는 처리/판단 기호다. 이 사실을 무시해 structure 10점을 주지 않는다. 후속 연결 한 곳이 빠졌으면 structure 8점이며 그 연결 누락 때문에 logic까지 감점하지 않는다.
noConnectionsWithMeaningfulBlocks=true이면 모든 화살표가 없는 미완성 구조다. 의미 있는 기호들이 있으면 structure 4점을 기본으로, 기호 자체도 거의 없으면 2점을 사용한다. 이때 logic은 연결 부재가 아니라 기호에 적힌 행동·조건의 의미만 평가한다. 연결 부재를 logic 감점 근거로 적지 않는다.
주된 판단의 참/거짓 행동이 목표와 정반대로 연결된 경우는 핵심 논리 오류이므로 logic 6점이다. 두 분기와 끝 연결이 모두 존재하고 기호가 적절하면 structure는 10점이다. 분기 방향이 의미와 반대라는 같은 이유로 structure까지 감점하지 않는다.
예: 실내가 추우면 난방을 켜고 아니면 끝내야 하는데 실제 선은 참→끝, 거짓→난방 켜기이면 logic 6 / structure 10이다. 이는 단순한 선 모양 문제가 아니라 행동 선택의 논리 문제다.
예: 타당한 난방 순서도에서 난방 켜기→끝 선 하나만 없으면 logic 10 / structure 8이다.
예: 모든 기호가 낱개로 있고 행동·조건 내용은 타당하면 logic 10 / structure 4가 가능하다. 연결되지 않았다는 사실 하나만으로 두 항목을 낮추지 않는다.
반드시 여섯 점수와 각 근거가 서로 모순되지 않는지 확인한 뒤 JSON을 출력한다.

### 마지막 판정 점검: 설명과 점수가 일치해야 한다
감점 전 실제 답안에서 틀린 행동 또는 모순을 특정한다. '타당하다, 그러나 중복이다/단순하다/모호하다'만으로는 감점할 수 없다. 맞춤법, 짧은 상태 문장, 수치가 없는 목표는 논리 오류가 아니다. 국소 오류라는 말로 표현상의 아쉬움을 논리 오류로 바꾸지 않는다.
반복은 같은 행동을 다시 하는 것이 목적이다. 행동→완료 여부 확인→미완료이면 행동으로 복귀→완료이면 끝은 정상 반복이며 logic 10이다. 공부를 이미 마쳤으면 휴식, 아니면 공부 후 휴식도 정상이다. 복귀선이 시작을 거쳐도 다시 조건을 검사하여 종료할 경로가 있으면 무한 반복으로 단정하지 않는다.
참/거짓 반전은 조건 문장과 실제 두 경로를 각각 대입해서 증명할 때만 인정한다. 판단이 행동을 선택하는 질문인지 상태를 검사하는 질문인지 문맥으로 구분한다. 목표를 못 이루는 결과도 선택의 결과를 올바르게 나타냈다면 그 자체로 반전 오류가 아니다. 학생이 쓰지 않은 참/거짓 경로나 행동을 상상하지 않는다.
연결은 from→to가 실제 방향이다. fromPort=in도 역방향으로 뒤집어 읽지 않는다. 방향이 뒤섞이거나 다음 선이 없다는 결함은 structure에 귀속하고, 블록 내용 자체의 논리는 따로 평가한다.
현재/목표/조건은 서로의 맥락으로 평가한다. 올바른 조건을 순서도에 반영하지 않은 문제는 조건 칸을 낮추는 이유가 아니다. 자연어는 자연어 자체로 평가하며 순서도와 달라도 실행 의미가 타당하면 만점이다. 문장 속 '익으면 담는다'처럼 행동과 조건을 함께 표현한 내용에 별도 판단 기호를 강제하지 않는다.
기본 문구(내용 입력, 데이터 입출력, 연산 처리 실행, 조건 검사)만 있는 블록에 구체적 해결 행동을 지어내지 않는다. 다른 칸의 상세 계획을 순서도에 작성한 것으로 대신 인정하지 않는다. 구체적 내용 없이 기본 기호를 낱개로 둔 시도는 logic 2, structure 4 수준이며, 연결한 경우에는 실제로 표현한 순서만 인정한다. 본체 옆 미연결 기호는 기존 원칙대로 무시한다.
최종적으로 각 감점이 (1) 제출 답안에 실제 존재하는지 (2) 허용된 감점 사유인지 (3) 해당 항목의 오류인지 (4) 다른 항목에서 이미 감점한 같은 원인이 아닌지 확인한다. 하나라도 아니면 감점을 제거한다. 불확실한 해석은 uncertainties에만 적는다.
`;
